{:native/negative-checks 10,
 :source/after-checksum
 "790cbf376f32584417a5c979368f81a74d6b7d4539bce018bcf7fa8257212c7d",
 :native/candidate-checks 53,
 :authority/revision "fe54cc866473a888bfbd4ce2d3de6d8011f09f72",
 :test/before-checksum
 "273fe1ee295ddb27656ebf3b704c29544930f7895d0f161c59373ca26bd90d0f",
 :test/before
 "(ns lang.base.emit-helper-test\n  (:require [lang.base.emit-helper :as helper]))\n\n^{:refer lang.base.emit-helper/+default+}\n(Test/check\n [{:name \"retains callable configuration through a Var pointer\"\n   :test (fn [] (type (get-in helper/+default+ [:default :module :key-fn])))\n   :expected :std.native.Var}])\n\n^{:refer lang.base.emit-helper/default-emit-fn}\n(Test/check\n [{:name \"prints forms through the archive default emitter\"\n   :test (fn [] (helper/default-emit-fn '(outer :value) {} {}))\n   :expected \"(outer :value)\"}])\n\n^{:refer lang.base.emit-helper/pr-single}\n(Test/check\n [{:name \"escapes one single-quoted target string\"\n   :test (fn [] (helper/pr-single \"a'b\\\\c\\n\\t\"))\n   :expected \"'a\\\\'b\\\\\\\\c\\\\n\\\\t'\"}])\n\n^{:refer lang.base.emit-helper/get-option}\n(Test/check\n [{:name \"gets path-specific options before common defaults\"\n   :test (fn [] [(helper/get-option {:default {:common {:space \" \"}}\n                                      :block {:space \"\"}} [:block] :space)\n                 (helper/get-option {:default {:common {:space \" \"}}} [:block] :space)])\n   :expected [\"\" \" \"]}])\n\n^{:refer lang.base.emit-helper/get-options}\n(Test/check\n [{:name \"merges common defaults with path-specific options\"\n   :test (fn [] (helper/get-options {:default {:common {:space \" \" :end \")\"}}\n                                     :block {:space \"\"}} [:block]))\n   :expected {:space \"\" :end \")\"}}])\n\n^{:refer lang.base.emit-helper/form-key-base}\n(Test/check\n [{:name \"classifies archive tokens, forms, and persistent data\"\n   :test (fn [] [(helper/form-key-base :key)\n                 (helper/form-key-base 'symbol)\n                 (helper/form-key-base '(call))\n                 (helper/form-key-base [1])\n                 (helper/form-key-base nil)])\n   :expected [[:keyword :token true]\n              [:symbol :token true]\n              :expression\n              [:vector :data]\n              [:nil :token]]}])\n\n^{:refer lang.base.emit-helper/basic-typed-args}\n(Test/check\n [{:name \"assigns archive type modifiers to the following symbols\"\n   :test (fn []\n           (vec (map (fn [item] [(meta item) item])\n                     (helper/basic-typed-args '(:int left :const :int right)))))\n   :expected [[{:- [:int]} 'left]\n              [{:- [:const :int]} 'right]]}])\n\n^{:refer lang.base.emit-helper/rest-arg-form?}\n(Test/check\n [{:name \"recognizes only the canonical rest-argument form\"\n   :test (fn [] [(helper/rest-arg-form? '(:.. rest))\n                 (helper/rest-arg-form? '(:.. rest extra))\n                 (helper/rest-arg-form? '(:.. :not-symbol))])\n   :expected [true false false]}])\n\n^{:refer lang.base.emit-helper/rest-arg-symbol}\n(Test/check\n [{:name \"returns the rest binding or nil for ordinary arguments\"\n   :test (fn [] [(helper/rest-arg-symbol '(:.. rest))\n                 (helper/rest-arg-symbol '(:.. :not-symbol))])\n   :expected ['rest nil]}])\n\n^{:refer lang.base.emit-helper/key-member?}\n(Test/check\n [{:name \"finds keys by value in archive option collections\"\n   :test (fn [] [(helper/key-member? #{:symbol :vector} :vector)\n                 (helper/key-member? #{:symbol} :vector)])\n   :expected [true false]}])\n\n^{:refer lang.base.emit-helper/index-of-value}\n(Test/check\n [{:name \"returns a value index or the archive missing sentinel\"\n   :test (fn [] [(helper/index-of-value [:left :right] :right)\n                 (helper/index-of-value [:left] :missing)])\n   :expected [1 -1]}])\n\n^{:refer lang.base.emit-helper/drop-last-one}\n(Test/check\n [{:name \"returns an archive list without its final item\"\n   :test (fn [] (helper/drop-last-one '(:left :right :tail)))\n   :expected '(:left :right)}])\n\n^{:refer lang.base.emit-helper/emit-typed-allowed-args}\n(Test/check\n [{:name \"turns an allowed non-symbol target into an assigned typed argument\"\n   :test (fn []\n           (helper/emit-typed-allowed-args\n            [[] {:modifiers [#{:target}]}]\n            {:default {:common {}}\n             :allow {:assign #{:set}}}))\n   :expected [[] {:modifiers [] :symbol #{:target} :assign true}]}])\n\n^{:refer lang.base.emit-helper/emit-typed-args-loop}\n(Test/check\n [{:name \"builds declaration records in source order\"\n   :test (fn []\n           (helper/emit-typed-args-loop {:default {:common {}}}\n                                        false [] {:modifiers []} ['left 'right]))\n   :expected [{:modifiers [] :symbol 'left}\n              {:modifiers [] :symbol 'right}]}])\n\n^{:refer lang.base.emit-helper/emit-typed-args}\n(Test/check\n [{:name \"supports shorthand declaration values\"\n   :test (fn []\n           (helper/emit-typed-args ['left 1] {:default {:common {}}} {:shorthand true}))\n   :expected [{:modifiers [] :symbol 'left :value 1}]}])\n\n^{:refer lang.base.emit-helper/replace-symbol-parts}\n(Test/check\n [{:name \"applies every target symbol replacement\"\n   :test (fn [] (helper/replace-symbol-parts \"item-name?\" {\\- \"_\" \\? \"p\"}))\n   :expected \"item_namep\"}])\n\n^{:refer lang.base.emit-helper/emit-symbol-full}\n(Test/check\n [{:name \"emits full target symbols with namespace and token replacements\"\n   :test (fn []\n           (helper/emit-symbol-full\n            'item-name 'module.part\n            {:default {:common {:namespace-full \"____\"}\n                       :symbol {:full {:replace {\\. \"_\"}}}}\n             :token {:symbol {:replace {\\- \"_\"}}}}))\n   :expected \"module_part____item_name\"}])\n\n^{:refer lang.base.emit-helper/emit-type-record}\n(Test/check\n [{:name \"formats typed argument records with default target spelling\"\n   :test (fn [] (helper/emit-type-record {:symbol 'item-name :modifiers [:int]}))\n   :expected {:symbol \"item_name\" :type \"int\"}}])\n",
 :native/remaining-scope
 "Full emitter/runtime pipeline and pointer contract remain pending.",
 :adaptation/test-evidence
 [{:index 0,
   :before
   "(ns tahto.base.emit-helper-test\n  (:require [tahto.base.emit-common :as common]\n            [tahto.base.emit-helper :as helper :refer :all]\n            [tahto.base.grammar :as grammar])\n  (:use code.test))",
   :after
   "(ns tahto.base.emit-helper-test\n  (:require [lang.base.emit-common :as common]\n            [tahto.base.emit-helper :as helper :refer :all]\n            [lang.base.grammar :as grammar])\n  (:use code.test))"}
  {:index 27,
   :before
   "^{:refer tahto.base.emit-helper/emit-typed-allowed-args :added \"4.0\"}\n(fact \"allowed declared args other than symbols\"\n  (emit-typed-allowed-args [[{:modifiers [:int]}] {:modifiers [:int]}] {:allow {:assign #{:symbol}}})\n  => throws)",
   :after
   "^{:added \"4.0\", :refer tahto.base.emit-helper/emit-typed-allowed-args}\n(fact\n  \"allowed declared args other than symbols\"\n  (emit-typed-allowed-args\n    [[{:modifiers [:int]}] {:modifiers [:int]}]\n    {:allow {:assign #{:symbol}}})\n  =>\n  (throws))"}
  {:index 40,
   :before
   "^{:refer tahto.base.emit-helper/rest-arg-form? :added \"4.1\"}\n(fact \"recognizes only the canonical final rest argument shape\"\n  (rest-arg-form? '(:.. args)) => true\n  (rest-arg-form? '(:.. \"args\")) => false\n  (rest-arg-form? '(:.. args extra)) => false\n  (rest-arg-form? '[:.. args]) => false)",
   :after
   "^{:added \"4.1\", :refer tahto.base.emit-helper/rest-arg-form?}\n(fact\n  \"recognizes only the canonical final rest argument shape\"\n  (rest-arg-form? '(:.. args))\n  =>\n  true\n  (rest-arg-form? '(:.. \"args\"))\n  =>\n  false\n  (rest-arg-form? '(:.. args extra))\n  =>\n  false\n  (rest-arg-form? (vector :.. 'args))\n  =>\n  false)"}],
 :native/retained-helper-repair :empty-drop-last,
 :source/before
 "(ns lang.base.emit-helper\n  (:require [lang.base.util :as util]))\n\n(defn default-emit-fn\n  \"the default emit function\"\n  {:added \"4.0\"}\n  [form grammar mopts]\n  (pr-str form))\n\n(defn pr-single\n  \"prints a single quoted string\"\n  {:added \"3.0\"}\n  [s]\n  (let [escaped0 (str/replace s \"\\\\\" \"\\\\\\\\\")\n        escaped1 (str/replace escaped0 \"'\" \"\\\\'\")\n        escaped2 (str/replace escaped1 \"\\n\" \"\\\\n\")\n        escaped3 (str/replace escaped2 \"\\r\" \"\\\\r\")\n        escaped4 (str/replace escaped3 \"\\t\" \"\\\\t\")]\n    (str \"'\" escaped4 \"'\")))\n\n(def +sym-replace+\n  {\\- \"_\"\n   \\* \"__\"\n   \\? \"p\"\n   \\! \"f\"\n   \\= \"_eq\"\n   \\< \"_lt\"\n   \\> \"_gt\"})\n\n(def +default+\n  {:type :std/algol\n   :banned #{:set :keyword}\n   :allow {:assign #{:symbol}}\n   :highlight '#{return break set= br}\n   :default\n   {:modifier {:vector-last true}\n    :comment {:prefix \"//\" :suffix \"\"}\n    :common {:start \"(\"\n             :end \")\"\n             :space \" \"\n             :assign \"=\"\n             :statement \";\"\n             :range \":\"\n             :sep \",\"\n             :namespace \".\"\n             :namespace-full \"____\"\n             :access \".\"\n             :static \".\"\n             :apply \".\"\n             :line-spacing 1}\n    :symbol {:full {:replace {\\. \"_\"}\n                    :sep \"_\"}\n             :global identity}\n    :index {:start \"[\" :end \"]\"\n            :offset 0 :end-inclusive false}\n    :invoke {:space \"\"}\n    :macro {:statement \"\"}\n    :block {:statement \"\"\n            :parameter {:start \"(\" :end \")\"}\n            :body {:start \"{\" :end \"}\"}}\n    :typehint {:enabled false :assign \"->\" :space \" \" :after false}\n    :function {:raw \"function\"\n               :args {:start \"(\" :end \")\" :space \"\"}\n               :body {:start \"{\" :end \"}\"}}\n    :module {:key-fn #'util/sym-default-str\n             :allow '#{defn def defgen defclass defabstract}}}\n   :token {:string {:quote nil}\n           :symbol {:replace +sym-replace+}}\n   :data {:map {:start \"{\" :end \"}\" :space \"\"}\n          :map-entry {:start \"\" :end \"\" :assign \":\" :space \"\" :keyword :string}\n          :vector {:start \"[\" :end \"]\" :space \"\"}\n          :tuple {:start \"(\" :end \")\" :space \"\" :tighten true}\n          :free {:start \"\" :end \"\" :space \"\"}}\n   :block {:branch {:control {:elseif {:raw \"else if\"}}}\n           :switch {:control {:default {:parameter {:start \" \" :end \"\"}\n                                        :body {:start \":\" :end \"\"}}}}}\n   :define {:defglobal {:raw \"global\"}\n            :def {:raw \"def\"}}})\n\n(defn get-option\n  \"gets either the path option or the default one\"\n  {:added \"3.0\"}\n  [grammar path option]\n  (or (get-in grammar (vec (concat path [option])))\n      (get-in grammar [:default :common option])))\n\n(defn get-options\n  \"gets the path option merged with defaults\"\n  {:added \"3.0\"}\n  [grammar path]\n  (merge (get-in grammar [:default :common])\n         (get-in grammar path)))\n\n(defn form-key-base\n  \"gets the key for a form\n\n   (form-key-base :a)\n   => [:keyword :token true]\n\n   (form-key-base ())\n   => :expression\"\n  {:added \"4.0\"}\n  [form]\n  (cond (char? form) [:char :token true]\n        (keyword? form) [:keyword :token true]\n        (symbol? form) [:symbol :token true]\n        (std.foundation/form? form) :expression\n        (number? form) [:number :token]\n        (boolean? form) [:boolean :token]\n        (regexp? form) [:regex :token]\n        (string? form) [:string :token]\n        (nil? form) [:nil :token]\n        (uuid? form) [:uuid :token]\n        (map? form) [:map :data]\n        (pair? form) [:map-entry :data]\n        (vector? form) [:vector :data]\n        (set? form) [:set :data]\n        :else\n        (throw\n         (ex :argument {:input form\n                   :type (type form)} :ex/message \"Not Valid\"))))\n\n(defn basic-typed-args\n  \"typed args without grammar checks\n\n   (map (juxt meta identity)\n         (basic-typed-args '(:int i, :const :int j)))\n   => '[[{:- [:int]} i]\n        [{:- [:const :int]} j]]\"\n  {:added \"4.0\"}\n  [args]\n  (loop [modifiers []\n         all []\n         remaining (vec args)]\n    (if (= 0 (count remaining))\n      all\n      (let [current (first remaining)]\n        (if (symbol? current)\n          (recur []\n                 (conj all\n                       (IObjType/with-meta\n                        current\n                        {:- modifiers}))\n                 (vec (drop 1 remaining)))\n          (recur (conj modifiers current)\n                 all\n                 (vec (drop 1 remaining))))))))\n\n(defn rest-arg-form?\n  \"checks for the canonical xtalk rest argument form `(:.. args)`\"\n  {:added \"4.1\"}\n  [form]\n  (and (list? form)\n       (= 2 (count form))\n       (= :.. (first form))\n       (symbol? (second form))))\n\n(defn rest-arg-symbol\n  \"returns the symbol bound by a canonical xtalk rest argument form\"\n  {:added \"4.1\"}\n  [form]\n  (if (rest-arg-form? form)\n    (second form)\n    nil))\n\n(defn key-member?\n  [values key]\n  (reduce (fn [found value]\n            (or found (= key value)))\n          false\n          values))\n\n(defn index-of-value\n  [values target]\n  (loop [index 0\n         remaining (vec values)]\n    (if (= 0 (count remaining))\n      -1\n      (if (= target (first remaining))\n        index\n        (recur (+ index 1)\n               (vec (drop 1 remaining)))))))\n\n(defn drop-last-one\n  [values]\n  (apply list (vec (take (- (count values) 1) values))))\n\n(defn emit-typed-allowed-args\n  \"allowed declared args other than symbols\"\n  {:added \"4.0\"}\n  [state grammar]\n  (let [all (first state)\n        current (second state)\n        modifiers (:modifiers current)\n        argument (last modifiers)\n        allowed (get-option grammar [:allow] :assign)\n        key (first (if (vector? (form-key-base argument))\n                     (form-key-base argument)\n                     [(form-key-base argument)]))]\n    (if (key-member? allowed key)\n      [all\n       (assoc current\n              :symbol argument\n              :modifiers (vec (drop-last-one modifiers))\n              :assign true)]\n      (throw\n       (ex :generic {:curr current :all all} :ex/message \"Cannot assign to non symbol\")))))\n\n(defn emit-typed-args-loop\n  [grammar shorthand all current remaining]\n  (if (= 0 (count remaining))\n    (if (:symbol current)\n      (conj all current)\n      all)\n    (let [value (first remaining)\n          more (vec (drop 1 remaining))]\n      (cond\n             (= := value)\n             (if (:symbol current)\n               (emit-typed-args-loop\n                grammar shorthand all\n                (assoc current :assign true :force true)\n                more)\n               (let [allowed (emit-typed-allowed-args [all current] grammar)]\n                 (emit-typed-args-loop\n                  grammar shorthand\n                  (first allowed)\n                  (assoc (second allowed) :force true)\n                  more)))\n\n             (= :% value)\n             (if (:symbol current)\n               (emit-typed-args-loop\n                grammar shorthand\n                (conj all current)\n                {:modifiers [(first more)]}\n                (vec (drop 1 more)))\n               (emit-typed-args-loop\n                grammar shorthand all\n                (assoc current\n                       :modifiers\n                       (conj (:modifiers current)\n                             (first more)))\n                (vec (drop 1 more))))\n\n             (:assign current)\n             (emit-typed-args-loop\n              grammar shorthand\n              (conj all (assoc current :value value))\n              {:modifiers []}\n              more)\n\n             (rest-arg-form? value)\n             (let [next-all (if (:symbol current)\n                              (conj all current)\n                              all)]\n               (if (or (> (count more) 0)\n                       (and (not (:symbol current))\n                            (> (count (:modifiers current)) 0)))\n                 (throw\n                  (ex :argument {:input value\n                            :args remaining\n                            :curr current} :ex/message \"Rest argument must be the final declared argument\"))\n                 (emit-typed-args-loop\n                  grammar shorthand next-all\n                  {:modifiers []\n                   :symbol (rest-arg-symbol value)\n                   :rest true}\n                  more)))\n\n             (and (not (:symbol current))\n                  (list? value)\n                  (not (= -1 (index-of-value value :=))))\n             (let [parts (vec (filter (fn [item] (not (= := item))) value))]\n               (emit-typed-args-loop\n                grammar shorthand\n                (conj all\n                      (assoc current\n                             :symbol (first parts)\n                             :value (second parts)))\n                {:modifiers []}\n                more))\n\n             (or (and (symbol? value)\n                      (not (and shorthand\n                                (:symbol current)\n                                (= 0 (count more)))))\n                 (and (set? value)\n                      (:set (get-in grammar [:allow :assign])))\n                 (and (map? value)\n                      (:map (get-in grammar [:allow :assign])))\n                 (and (vector? value)\n                      (:vector (get-in grammar [:allow :assign])))\n                 (and (list? value)\n                      (= 'quote (first value))\n                      (:quote (get-in grammar [:allow :assign])))\n                 (and (list? value)\n                      (= := (first value))))\n             (if (:symbol current)\n               (emit-typed-args-loop\n                grammar shorthand\n                (conj all current)\n                {:modifiers [] :symbol value}\n                more)\n               (emit-typed-args-loop\n                grammar shorthand all\n                (assoc current :symbol value)\n                more))\n\n             (and (list? value)\n                  (keyword? (first value)))\n             (if (:symbol current)\n               (emit-typed-args-loop\n                grammar shorthand\n                (conj all current)\n                {:type (drop-last-one value)\n                 :symbol (last value)}\n                more)\n               (emit-typed-args-loop\n                grammar shorthand all\n                (assoc current\n                       :type (drop-last-one value)\n                       :symbol (last value))\n                more))\n\n             (and shorthand\n                  (:symbol current)\n                  (= 0 (count more))\n                  (or (vector? value)\n                      (map? value)\n                      (set? value)))\n             (emit-typed-args-loop\n              grammar shorthand\n              (conj all (assoc current :value value))\n              {:modifiers []}\n              more)\n\n             (or (keyword? value) (vector? value))\n             (if (:symbol current)\n               (emit-typed-args-loop\n                grammar shorthand\n                (conj all current)\n                {:modifiers [value]}\n                more)\n               (emit-typed-args-loop\n                grammar shorthand all\n                (assoc current\n                       :modifiers\n                       (conj (:modifiers current) value))\n                more))\n\n             (:symbol current)\n             (emit-typed-args-loop\n              grammar shorthand\n              (conj all (assoc current :value value))\n              {:modifiers []}\n              more)\n\n             :else\n             (throw\n              (ex :generic {:input value\n                        :all all\n                        :curr current} :ex/message \"Not a valid input\"))))))\n\n(defn emit-typed-args\n  \"create types args from declarationns\"\n  {:added \"3.0\"}\n  ([args grammar]\n   (emit-typed-args args grammar nil))\n  ([args grammar options]\n   (emit-typed-args-loop grammar\n                         (:shorthand options)\n                         []\n                         {:modifiers []}\n                         (vec args))))\n\n(defn replace-symbol-parts\n  [value replacements]\n  (reduce (fn [result pair]\n            (str/replace result\n                            (str (first pair))\n                            (second pair)))\n          value\n          replacements))\n\n(defn emit-symbol-full\n  \"emits a full symbol\"\n  {:added \"4.0\"}\n  [sym ns grammar]\n  (let [common-options (get-in grammar [:default :common])\n        symbol-options (get-in grammar [:default :symbol :full])\n        token-options (get-in grammar [:token :symbol])\n        symbol-string (if ns\n                        (str ns\n                             (:namespace-full common-options)\n                             (name sym))\n                        (name sym))\n        namespaced (str/replace\n                    symbol-string\n                    \".\"\n                    (or (:namespace-sep common-options) \"_\"))]\n    (replace-symbol-parts\n     namespaced\n     (merge (:replace symbol-options)\n            (:replace token-options)))))\n\n(defn emit-type-record\n  \"formats to standard\"\n  {:added \"4.0\"}\n  [argument]\n  {:symbol (util/sym-default-str (:symbol argument))\n   :type (name (first (:modifiers argument)))})\n",
 :adaptation/source-evidence
 [{:index 0,
   :before
   "(ns tahto.base.emit-helper\n  (:require [clojure.string]\n            [tahto.base.util :as ut]\n            [std.lib.collection :as collection]\n            [std.lib.foundation :as f]))",
   :after
   "(ns tahto.base.emit-helper\n  (:require [lang.base.util :as ut]))"}
  {:index 6,
   :before
   "(defn pr-single\n  \"prints a single quoted string\"\n  {:added \"3.0\"}\n  ([s]\n   (-> (pr-str s)\n       (clojure.string/replace #\"'\" \"\\\\\\\\'\")\n       (clojure.string/replace #\"^\\\"\" \"'\")\n       (clojure.string/replace #\"\\\"$\" \"'\")\n       (clojure.string/replace #\"\\\\\\\"\" \"\\\"\"))))",
   :after
   "(defn pr-single\n  \"prints a single quoted string\"\n  {:added \"3.0\"}\n  ([s]\n    (let [printed (pr-str s)\n          body (subs printed 1 (dec (count printed)))]\n      (str\n        \"'\"\n        (str/replace (str/replace body \"'\" \"\\\\'\") \"\\\\\\\"\" \"\\\"\")\n        \"'\"))))"}
  {:index 28,
   :before
   "(defn form-key-base\n  \"gets the key for a form\n \n   (form-key-base :a)\n   => [:keyword :token true]\n \n   (form-key-base ())\n   => :expression\"\n  {:added \"4.0\"}\n  [form]\n  (cond (char? form)      [:char :token true]\n        (keyword? form)   [:keyword :token true]\n        (symbol? form)    [:symbol :token true]\n        \n        (collection/form? form)    :expression\n\n        (number? form)    [:number :token]\n        (boolean? form)   [:boolean :token]\n        (f/regexp? form)  [:regex :token]\n        \n        (string? form)    [:string :token]\n        (nil? form)       [:nil :token]\n        (uuid? form)      [:uuid :token]\n        \n        (map? form)       [:map :data]\n        (map-entry? form) [:map-entry :data]\n        (vector? form)    [:vector :data]\n        (set? form)       [:set :data]\n\n        :else (f/error \"Not Valid\" {:input form\n                                    :type (type form)})))",
   :after
   "(defn form-key-base\n  \"gets the key for a form\\n \\n   (form-key-base :a)\\n   => [:keyword :token true]\\n \\n   (form-key-base ())\\n   => :expression\"\n  {:added \"4.0\"}\n  [form]\n  (cond\n    (char? form) [:char :token true]\n    (keyword? form) [:keyword :token true]\n    (symbol? form) [:symbol :token true]\n    (form? form) :expression\n    (number? form) [:number :token]\n    (boolean? form) [:boolean :token]\n    (regexp? form) [:regex :token]\n    (string? form) [:string :token]\n    (nil? form) [:nil :token]\n    (uuid? form) [:uuid :token]\n    (map? form) [:map :data]\n    (pair? form) [:map-entry :data]\n    (vector? form) [:vector :data]\n    (set? form) [:set :data]\n    :else (throw\n            (ex\n              :generic\n              {:type (type form), :input form}\n              :ex/message\n              \"Not Valid\"))))"}
  {:index 41,
   :before
   "(defn rest-arg-form?\n  \"checks for the canonical xtalk rest argument form `(:.. args)`\"\n  {:added \"4.1\"}\n  [form]\n  (and (collection/form? form)\n       (= 2 (count form))\n       (= :.. (first form))\n       (symbol? (second form))))",
   :after
   "(defn rest-arg-form?\n  \"checks for the canonical xtalk rest argument form `(:.. args)`\"\n  {:added \"4.1\"}\n  [form]\n  (and\n    (form? form)\n    (= 2 (count form))\n    (= :.. (first form))\n    (symbol? (second form))))"}
  {:index 47,
   :before
   "(defn emit-typed-allowed-args\n  \"allowed declared args other than symbols\"\n  {:added \"4.0\"}\n  [[all curr] grammar]\n  (let [{:keys [modifiers]} curr\n        arg (last modifiers)]\n    (if (contains? (get-option grammar [:allow] :assign)\n                   (first (collection/seqify (form-key-base arg))))\n      [all (-> curr\n               (assoc :symbol arg\n                      :modifiers (vec (butlast modifiers))\n                      :assign true))]\n      (f/error \"Cannot assign to non symbol\" {:curr curr :all all}))))",
   :after
   "(defn emit-typed-allowed-args\n  \"allowed declared args other than symbols\"\n  {:added \"4.0\"}\n  [[all curr] grammar]\n  (let [{:keys [modifiers]} curr arg (last modifiers)]\n    (if (contains?\n          (get-option grammar [:allow] :assign)\n          (first\n            ((fn [value] (if (sequential? value) value [value]))\n              (form-key-base arg))))\n      [all\n       (-> curr\n        (assoc\n          :symbol\n          arg\n          :modifiers\n          (vec (butlast modifiers))\n          :assign\n          true))]\n      (throw\n        (ex\n          :generic\n          {:all all, :curr curr}\n          :ex/message\n          \"Cannot assign to non symbol\")))))"}
  {:index 50,
   :before
   "(defn emit-typed-args\n  \"create types args from declarationns\"\n  {:added \"3.0\"}\n  ([args grammar]\n   (emit-typed-args args grammar nil))\n  ([args grammar {:keys [shorthand]}]\n   (loop [all  []\n          curr {:modifiers []}\n          [sym & more :as args] args]\n     (cond (empty? args)\n           (if (:symbol curr)\n             (conj all curr)\n             all)\n\n           (= := sym)\n           (if (:symbol curr)\n             (recur all (assoc curr :assign true :force true) more)\n             (let [[all curr] (emit-typed-allowed-args [all curr] grammar)]\n               (recur all (assoc curr :force true) more)))\n\n           (= :% sym)\n           (if (:symbol curr)\n             (recur (conj all curr) {:modifiers [(first more)]} (rest more))\n             (recur all (update curr :modifiers conj (first more)) (rest more)))\n\n           (:assign curr)\n           (recur (conj all (assoc curr :value sym))\n                  {:modifiers []}\n                  more)\n\n           (rest-arg-form? sym)\n           (let [all (if (:symbol curr)\n                       (conj all curr)\n                       all)]\n             (when (or (not-empty more)\n                       (and (not (:symbol curr))\n                            (not-empty (:modifiers curr))))\n               (f/error \"Rest argument must be the final declared argument\"\n                        {:input sym\n                         :args args\n                         :curr curr}))\n             (recur all\n                    {:modifiers []\n                     :symbol (rest-arg-symbol sym)\n                     :rest true}\n                    more))\n\n           (and (not (:symbol curr))\n                (list? sym)\n                (not (neg? (collection/index-at #{:=} sym))))\n           (let [[symbol val] (remove #{:=} sym)]\n             (recur (conj all (assoc curr :symbol symbol :value val))\n                    {:modifiers []}\n                    more))\n\n           (or (and (symbol? sym)\n                    (not (and shorthand\n                              (:symbol curr)\n                              (empty? more))))\n               (and (set? sym)\n                    (-> grammar\n                        :allow\n                        :assign\n                        :set))\n               (and (map? sym)\n                    (-> grammar\n                        :allow\n                        :assign\n                        :map))\n               (and (vector? sym)\n                    (-> grammar\n                        :allow\n                        :assign\n                        :vector))\n               (and (collection/form? sym)\n                    (= 'quote (first sym))\n                    (-> grammar\n                        :allow\n                        :assign\n                        :quote))\n               (and (collection/form? sym)\n                    (= := (first sym))))\n           (if (:symbol curr)\n             (recur (conj all curr) {:modifiers [] :symbol sym} more)\n             (recur all (assoc curr :symbol sym) more))\n\n           (and (collection/form? sym)\n                (keyword? (first sym)))\n           (if (:symbol curr)\n             (recur (conj all curr) {:type   (butlast sym)\n                                     :symbol (last sym)}\n                    more)\n             (recur all (assoc curr\n                               :symbol sym\n                               :type   (butlast sym)\n                               :symbol (last sym))\n                    more))\n\n           (and shorthand\n                (:symbol curr)\n                (empty? more)\n                (or (vector? sym)\n                    (map? sym)\n                    (set? sym)))\n           (recur (conj all (assoc curr :value sym))\n                  {:modifiers []}\n                  more)\n\n           (or (keyword? sym) (vector? sym))\n           (if (:symbol curr)\n             (recur (conj all curr) {:modifiers [sym]} more)\n             (recur all (update curr :modifiers conj sym) more))\n\n           (:symbol curr)\n           (recur (conj all (assoc curr :value sym)) {:modifiers []} more)\n\n           :else\n           (f/error \"Not a valid input\" {:input sym\n                                         :all all\n                                         :curr curr})))))",
   :after
   "(defn emit-typed-args\n  \"create types args from declarationns\"\n  {:added \"3.0\"}\n  ([args grammar] (emit-typed-args args grammar nil))\n  ([args grammar options]\n    (let [shorthand (:shorthand options)]\n      (loop [all [] curr {:modifiers []} args args]\n        (let [sym (first args) more (rest args)]\n          (cond\n            (empty? args) (if (:symbol curr) (conj all curr) all)\n            (= := sym) (if (:symbol curr)\n                         (recur\n                           all\n                           (assoc curr :assign true :force true)\n                           more)\n                         (let [[all curr] (emit-typed-allowed-args\n                                            [all curr]\n                                            grammar)]\n                           (recur all (assoc curr :force true) more)))\n            (= :% sym) (if (:symbol curr)\n                         (recur\n                           (conj all curr)\n                           {:modifiers [(first more)]}\n                           (rest more))\n                         (recur\n                           all\n                           (update curr :modifiers conj (first more))\n                           (rest more)))\n            (:assign curr) (recur\n                             (conj all (assoc curr :value sym))\n                             {:modifiers []}\n                             more)\n            (rest-arg-form? sym) (let\n                                   [all\n                                    (if\n                                      (:symbol curr)\n                                      (conj all curr)\n                                      all)]\n                                   (when\n                                     (or\n                                       (not-empty more)\n                                       (and\n                                         (not (:symbol curr))\n                                         (not-empty\n                                           (:modifiers curr))))\n                                     (throw\n                                       (ex\n                                         :generic\n                                         {:args args,\n                                          :curr curr,\n                                          :input sym}\n                                         :ex/message\n                                         \"Rest argument must be the final declared argument\")))\n                                   (recur\n                                     all\n                                     {:symbol (rest-arg-symbol sym),\n                                      :modifiers [],\n                                      :rest true}\n                                     more))\n            (and\n              (not (:symbol curr))\n              (list? sym)\n              (not (neg? (index-of-value sym :=)))) (let\n                                                      [[symbol val]\n                                                       (remove\n                                                         #{:=}\n                                                         sym)]\n                                                      (recur\n                                                        (conj\n                                                          all\n                                                          (assoc\n                                                            curr\n                                                            :symbol\n                                                            symbol\n                                                            :value\n                                                            val))\n                                                        {:modifiers []}\n                                                        more))\n            (or\n              (and\n                (symbol? sym)\n                (not (and shorthand (:symbol curr) (empty? more))))\n              (and (set? sym) (-> grammar :allow :assign :set))\n              (and (map? sym) (-> grammar :allow :assign :map))\n              (and (vector? sym) (-> grammar :allow :assign :vector))\n              (and\n                (form? sym)\n                (= 'quote (first sym))\n                (-> grammar :allow :assign :quote))\n              (and (form? sym) (= := (first sym)))) (if\n                                                      (:symbol curr)\n                                                      (recur\n                                                        (conj all curr)\n                                                        {:symbol sym,\n                                                         :modifiers []}\n                                                        more)\n                                                      (recur\n                                                        all\n                                                        (assoc\n                                                          curr\n                                                          :symbol\n                                                          sym)\n                                                        more))\n            (and (form? sym) (keyword? (first sym))) (if\n                                                       (:symbol curr)\n                                                       (recur\n                                                         (conj\n                                                           all\n                                                           curr)\n                                                         {:symbol\n                                                          (last sym),\n                                                          :type\n                                                          (butlast\n                                                            sym)}\n                                                         more)\n                                                       (recur\n                                                         all\n                                                         (assoc\n                                                           curr\n                                                           :symbol\n                                                           sym\n                                                           :type\n                                                           (butlast\n                                                             sym)\n                                                           :symbol\n                                                           (last sym))\n                                                         more))\n            (and\n              shorthand\n              (:symbol curr)\n              (empty? more)\n              (or (vector? sym) (map? sym) (set? sym))) (recur\n                                                          (conj\n                                                            all\n                                                            (assoc\n                                                              curr\n                                                              :value\n                                                              sym))\n                                                          {:modifiers\n                                                           []}\n                                                          more)\n            (or (keyword? sym) (vector? sym)) (if\n                                                (:symbol curr)\n                                                (recur\n                                                  (conj all curr)\n                                                  {:modifiers [sym]}\n                                                  more)\n                                                (recur\n                                                  all\n                                                  (update\n                                                    curr\n                                                    :modifiers\n                                                    conj\n                                                    sym)\n                                                  more))\n            (:symbol curr) (recur\n                             (conj all (assoc curr :value sym))\n                             {:modifiers []}\n                             more)\n            :else (throw\n                    (ex\n                      :generic\n                      {:all all, :curr curr, :input sym}\n                      :ex/message\n                      \"Not a valid input\"))))))))"}
  {:index 53,
   :before
   "(defn emit-symbol-full\n  \"emits a full symbol\"\n  {:added \"4.0\"}\n  [sym ns grammar]\n  (let [dopts (get-in grammar [:default :common])\n        sopts (get-in grammar [:default :symbol :full])\n        topts (get-in grammar [:token :symbol])\n        sym-str  (if ns\n                   (str ns\n                        (:namespace-full dopts)\n                        (name sym))\n                   (name sym))]\n    (cond->> (. sym-str (replaceAll \"\\\\.\" (or (:namespace-sep dopts)\n                                               \"_\")))\n      :then   (replace (merge (:replace sopts)\n                              (:replace topts)))\n      :then   (apply str))))",
   :after
   "(defn emit-symbol-full\n  \"emits a full symbol\"\n  {:added \"4.0\"}\n  [sym ns grammar]\n  (let [dopts (get-in grammar [:default :common])\n        sopts (get-in grammar [:default :symbol :full])\n        topts (get-in grammar [:token :symbol])\n        sym-str (if ns\n                  (str ns (:namespace-full dopts) (name sym))\n                  (name sym))]\n    (let [replacements (merge (:replace sopts) (:replace topts))]\n      (apply\n        str\n        (map\n          (fn [ch] (get replacements ch ch))\n          (seq\n            (str/replace\n              sym-str\n              \".\"\n              (or (:namespace-sep dopts) \"_\"))))))))"}],
 :source/before-checksum
 "6528295d90dcd3bcfd2a748a1e98ce24f4a81300855030f3502347d707800157",
 :test/after-checksum
 "dea1abd2b5c242a45ee3569acd50f452daa579e3aa1e84570453d53b30250538"}
