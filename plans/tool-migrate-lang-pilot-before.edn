{:source-revision "fe54cc866473a888bfbd4ce2d3de6d8011f09f72",
 :generator
 "reference/foundation-base/resources/code/migrate/lang.edn",
 :files
 [{:path "src/lang/base/emit_template.hal",
   :before
   "(ns lang.base.emit-template\n  (:require [lang.base.emit-preprocess :as preprocess]\n            [lang.base.emit-rewrite :as rewrite]\n            [lang.base.grammar-xtalk-system :as xtalk-system]))\n\n(def +code-state-keys+\n  [:form :deps :deps-fragment :deps-native\n   :xtalk-ops :xtalk-profiles :polyfill-modules])\n\n(def ^:dynamic *computing-entries* #{})\n\n(defn member? [values target]\n  (reduce (fn [found value] (or found (= value target))) false values))\n\n(defn entry-reserved\n  \"Gets the reserved grammar entry for a code entry.\"\n  [grammar entry]\n  (get-in grammar [:reserved (:op entry)]))\n\n(defn code-state-computing?\n  \"Returns true when an entry is currently being staged.\"\n  [entry]\n  (member? *computing-entries* [(:lang entry) (:module entry) (:id entry)]))\n\n(defn create-code-state\n  \"Hydrates, stages, rewrites, and scans one code entry.\"\n  ([entry reserved grammar modules]\n   (create-code-state entry reserved grammar modules {}))\n  ([entry reserved grammar modules mopts]\n   (let [key [(:lang entry) (:module entry) (:id entry)]]\n     (if (member? *computing-entries* key)\n       (throw (ex :state {:entry key :stack (vec *computing-entries*)}\n                  :ex/message \"Recursive code-state computation\"))\n       (binding [*computing-entries* (conj *computing-entries* key)]\n         (let [module (or (:module mopts) (get modules (:module entry)))\n               context (merge {:module module :entry entry} mopts)\n               hydrate (:hydrate reserved)\n               hydrated (if hydrate\n                          (hydrate (:form-input entry) grammar context)\n                          [nil (:form-input entry)])\n               hmeta (first hydrated)\n               staged (preprocess/to-staging (second hydrated) grammar modules context)\n               form (rewrite/rewrite-stage :staging (nth staged 0) grammar context)\n               scan (xtalk-system/scan-xtalk form grammar)\n               deps (reduce conj (or (nth staged 1) #{})\n                            (xtalk-system/xtalk-ops-polyfill-symbols\n                             (:ops scan) grammar))]\n           {:hmeta hmeta\n            :form form\n            :deps deps\n            :deps-fragment (nth staged 2)\n            :deps-native (nth staged 3)\n            :xtalk-ops (:ops scan)\n            :xtalk-profiles (:profiles scan)\n            :polyfill-modules (:polyfill-modules scan)}))))))\n\n(defn cached-code-state\n  \"Returns a language-specific cached code state.\n\n   The per-entry cache is populated by one atom transition, matching the\n   Foundation memoization boundary and avoiding duplicate work on a cache hit.\"\n  ([entry reserved grammar modules]\n   (cached-code-state entry reserved grammar modules {}))\n  ([entry reserved grammar modules mopts]\n   (let [lang (or (:lang mopts) (:lang entry))\n         cache (:static/code.cache entry)]\n     (if (and cache lang)\n       (get (swap! cache\n                   (fn [states]\n                     (if (has? states lang)\n                       states\n                       (assoc states lang\n                              (create-code-state entry reserved grammar modules mopts)))))\n            lang)\n       (create-code-state entry reserved grammar modules mopts)))))\n\n(defn clear-code-state-cache!\n  \"Idempotently clears an entry's per-language code-state cache.\"\n  [entry]\n  (let [cache (:static/code.cache entry)]\n    (if cache (reset! cache {}) nil)\n    entry))\n\n(defn materialize-code-entry\n  \"Materializes a code entry for a target language.\"\n  ([book entry]\n   (materialize-code-entry book entry {}))\n  ([book entry mopts]\n   (let [grammar (:grammar book)\n         modules (:modules book)\n         reserved (entry-reserved grammar entry)\n         context (merge {:lang (or (:lang mopts) (:lang book))\n                         :module (or (:module mopts) (get modules (:module entry)))}\n                        mopts)]\n     (if (and reserved modules)\n       (let [state (cached-code-state entry reserved grammar modules context)\n             materialized (merge entry (dissoc state :hmeta) (:hmeta state))\n             hook (:hydrate-hook reserved)]\n         (if hook (or (hook materialized) materialized) materialized))\n       entry))))\n\n(defn cached-entry-deps\n  \"Returns current staged dependencies for an entry.\"\n  [book entry]\n  (:deps (cached-code-state entry\n                            (entry-reserved (:grammar book) entry)\n                            (:grammar book)\n                            (:modules book)\n                            {:lang (:lang book)})))\n",
   :before-checksum
   "35f83f50d3c89ada046ad1b44468d9cd9890173056e78d5ae8346b34b55d6bb5",
   :after-checksum
   "4104b02131e1ee2477c63a9e7ff2b7704669f36e56f9718669e64b97af1c66e0"}
  {:path "test/lang/base/emit_template_test.hal",
   :before
   "(ns lang.base.emit-template-test\n  (:require [lang.base.emit-template :as template]))\n\n(def fixture-entry\n  {:lang :fixture\n   :module 'fixture.core\n   :id 'run\n   :op :plain\n   :form-input '(outer 1)\n   :static/code.cache (atom {})})\n\n(def fixture-grammar {:reserved {:plain {}}})\n(def fixture-modules {'fixture.core {:id 'fixture.core}})\n(def fixture-book {:lang :fixture :grammar fixture-grammar :modules fixture-modules})\n\n^{:refer lang.base.emit-template/member?}\n(Test/check [{:name \"tests membership without assuming a host collection implementation\"\n              :test (fn [] [(template/member? #{:a :b} :b) (template/member? #{:a} :missing)])\n              :expected [true false]}])\n\n^{:refer lang.base.emit-template/entry-reserved}\n(Test/check [{:name \"locates the reserved grammar operation for an entry\"\n              :test (fn [] (template/entry-reserved fixture-grammar fixture-entry))\n              :expected {}}])\n\n^{:refer lang.base.emit-template/code-state-computing?}\n(Test/check [{:name \"reports only the active dynamic computation coordinate\"\n              :test (fn [] [(template/code-state-computing? fixture-entry)\n                            (binding [template/*computing-entries* #{[:fixture 'fixture.core 'run]}]\n                              (template/code-state-computing? fixture-entry))])\n              :expected [false true]}])\n\n^{:refer lang.base.emit-template/create-code-state}\n(Test/check [{:name \"hydrates, stages, and restores the recursion guard after failure\"\n              :test (fn [] (let [state (template/create-code-state\n                                         fixture-entry {} fixture-grammar fixture-modules)\n                                recursive (binding [template/*computing-entries* #{[:fixture 'fixture.core 'run]}]\n                                            (try (template/create-code-state\n                                                  fixture-entry {} fixture-grammar fixture-modules)\n                                                 false\n                                                 (catch error true)))]\n                            [(select-keys state template/+code-state-keys+)\n                             recursive\n                             (template/code-state-computing? fixture-entry)]))\n              :expected [{:form '(outer 1) :deps #{} :deps-fragment #{} :deps-native {}\n                          :xtalk-ops #{} :xtalk-profiles #{} :polyfill-modules #{}}\n                         true false]}])\n\n^{:refer lang.base.emit-template/cached-code-state}\n(Test/check [{:name \"memoizes one code state per language and recomputes after cache reset\"\n              :test (fn [] (let [calls (atom 0)\n                                entry (assoc fixture-entry :static/code.cache (atom {}))\n                                reserved {:hydrate (fn [form grammar context]\n                                                     (swap! calls inc)\n                                                     [nil form])}\n                                first-state (template/cached-code-state entry reserved fixture-grammar fixture-modules)\n                                second-state (template/cached-code-state entry reserved fixture-grammar fixture-modules)\n                                _ (template/clear-code-state-cache! entry)\n                                third-state (template/cached-code-state entry reserved fixture-grammar fixture-modules)]\n                            [(= first-state second-state)\n                             (= first-state third-state)\n                             (deref calls)\n                             (keys (deref (:static/code.cache entry)))]))\n              :expected [true true 2 [:fixture]]}])\n\n^{:refer lang.base.emit-template/clear-code-state-cache!}\n(Test/check [{:name \"clears a present cache idempotently and accepts entries without one\"\n              :test (fn [] (let [entry (assoc fixture-entry :static/code.cache (atom {:fixture :state}))]\n                            [(= entry (template/clear-code-state-cache! entry))\n                             (= entry (template/clear-code-state-cache! entry))\n                             (deref (:static/code.cache entry))\n                             (template/clear-code-state-cache! (dissoc entry :static/code.cache))]))\n              :expected [true true {} {:lang :fixture :module 'fixture.core :id 'run\n                                                         :op :plain :form-input '(outer 1)}]}])\n\n^{:refer lang.base.emit-template/materialize-code-entry}\n(Test/check [{:name \"merges staged state and hydration metadata into materialized entries\"\n              :test (fn [] (let [entry (assoc fixture-entry :static/code.cache (atom {}))\n                            book (assoc fixture-book\n                                        :grammar {:reserved {:plain {:hydrate (fn [form grammar context]\n                                                                                [{:hydrated true} form])\n                                                                       :hydrate-hook (fn [value] (assoc value :hooked true))}}})]\n                        (select-keys (template/materialize-code-entry book entry)\n                                     [:form :hydrated :hooked :deps :xtalk-ops])))\n              :expected {:form '(outer 1) :hydrated true :hooked true :deps #{} :xtalk-ops #{}}}])\n\n^{:refer lang.base.emit-template/cached-entry-deps}\n(Test/check [{:name \"returns the cached dependency set for the book language\"\n              :test (fn [] (template/cached-entry-deps\n                            fixture-book\n                            (assoc fixture-entry :static/code.cache (atom {}))))\n              :expected #{}}])\n",
   :before-checksum
   "8c50c3ae45f0c1803b03645d1912783d33ab891f1ae442c91cc16c70eb0e1f7f",
   :after-checksum
   "0ec8b7afc9d33b29cf1ef5abcb2db56408d51c586a013cdd8e18cd04477ff867"}]}
