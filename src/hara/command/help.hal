(ns hara.command.help
  (:config {:role :internal}))

(def +usage+ "Usage: hara [--version|help|headless|run NAMESPACE [ARGS...]|test [SELECTOR...]|specs test TARGET]")

(defn response
  [_]
  {:stdout (str +usage+ "\n")
   :stderr ""
   :exit 0})
