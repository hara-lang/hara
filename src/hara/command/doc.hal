(ns hara.command.doc
  (:config {:role :internal})
  (:require [code.doc.command :as command]))

(defn ^{:public true} response
  "Forwards one parsed native Hara CLI request to code.doc."
  [request]
  (command/response request))
