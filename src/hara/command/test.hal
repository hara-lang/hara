(ns hara.command.test
  (:config {:role :internal})
  (:require [code.test.command :as command]))

(defn ^{:public true} response
  "Forwards a parsed Hara CLI request to the code.test command response."
  [request]
  (command/response request))
