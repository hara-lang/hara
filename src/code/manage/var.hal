(ns code.manage.var
  (:config {:role :internal})
  (:require [code.manage.unit :as unit]))

(defn find-usages
  [code-unit options]
  (if (:var options)
    (unit/grep
     code-unit
     (str (:var options)))
    []))

