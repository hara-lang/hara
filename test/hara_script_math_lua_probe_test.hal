(ns hara.script-math-lua-probe-test
  (:require [lang.core :as l])
  (:use code.test))

(l/script- :lua
  {:runtime :basic
   :test-mode true
   :require [[xt.lang.common-math :as xtm]
             [xt.lang.spec-base :as xt]]})

(fact:global
 {:setup [(l/rt:restart)]
  :teardown [(l/rt:stop)]})

(fact "starts the common math Lua graph"
  (!.lua (xtm/abs -5))
  => 5)
