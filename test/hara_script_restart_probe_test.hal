(ns hara.script-restart-probe-test
  (:require [lang.core :as l])
  (:use code.test))

(l/script- :js {:runtime :basic})

(fact:global
 {:setup [(l/rt:restart)]
  :teardown [(l/rt:stop)]})

(fact "starts a deferred JavaScript runtime"
  (!.js (+ 1 1))
  => 2)
