(ns code.manage.unit.require
  (:require [code.manage.unit :as unit]))

(defn require-file
  [code-unit]
  (vec
   (map
    (fn [entry]
      (symbol (:name entry)))
    (filter
     (fn [entry]
       (not=
        (:kind entry)
        :private-function))
     (unit/extract code-unit)))))
