(ns code.manage.automation
  (:require [code.manage.unit :as unit]))

(defn incomplete-report
  [units]
  (into {}
        (map
         (fn [code-unit]
           [(:path code-unit)
            (unit/incomplete code-unit)])
         units)))
