(ns code.deploy.runtime
  (:require [tool.sh :as sh]))

(defn run
  "Runs one argv vector through the canonical capability-gated process boundary."
  ([argv]
   (run argv {}))
  ([argv options]
   (sh/ensure! "Hara package command failed"
               (sh/run argv (assoc options :trim false)))))
