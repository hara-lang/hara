(ns std.lib.stream)

(defn ^{:inline true :schema [:fn [:fn & :any] :any]}
  generate
  "Creates a lazy pull stream from a coroutine body and its initial arguments."
  [function & arguments]
  (apply Stream/generate function arguments))
