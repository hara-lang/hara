(ns hara.cli.handlers
  (:require [code.manage.cli :as manage]
            [lang.seedgen.cli :as seedgen]))

(def +registry+
  {:hara.cli.handler/manage manage/handler
   :hara.cli.handler/seedgen seedgen/handler})

(defn registry
  ([] +registry+)
  ([overrides] (merge +registry+ overrides)))
