(ns work.native.protocol)

;; Java installs the matching native protocol identities through the eager
;; HaraLibraryProvider. This source marker keeps fallback library discovery
;; valid without redeclaring any protocol in HAL.
