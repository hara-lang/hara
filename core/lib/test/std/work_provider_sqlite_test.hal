(ns std.work-provider-sqlite-test
  (:require [std.work.provider.sqlite :as sqlite]))

(defn provider-boundary-test
  []
  (= #{:runs
       :transactions
       :events
       :checkpoints
       :outbox}
     (sqlite/capabilities)))
