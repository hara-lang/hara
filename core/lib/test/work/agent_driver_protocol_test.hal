(ns work.agent-driver-protocol-test
  (:require [work.agent :as agent]
            [work.agent.protocol :as protocol]))

(def results
  [(test-check
    "OpenAI drivers implement the driver capability boundary"
    (satisfies?
     protocol/IWorkAgentDriver
     (agent/openai-driver {}))
    true)])

results
