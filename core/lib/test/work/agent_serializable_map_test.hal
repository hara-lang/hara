(ns work.agent-serializable-map-test
  (:require [work.agent :as agent]))

(def results
  [(test-check
    "Agent semantic identity is ordinary map data"
    (agent/agent-spec
     {:agent/id :compiler
      :agent/capabilities #{:compile}})
    {:agent/id :compiler
     :agent/capabilities #{:compile}})])

results
