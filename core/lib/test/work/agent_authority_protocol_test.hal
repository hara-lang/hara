(ns work.agent-authority-protocol-test
  (:require [work.agent :as agent]
            [work.agent.protocol :as protocol]))

(def results
  [(test-check
    "ephemeral authorities implement the authority capability boundary"
    (satisfies?
     protocol/IWorkAgentAuthority
     (agent/ephemeral-authority))
    true)])

results
