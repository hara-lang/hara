(ns work.agent-coordinator-protocol-test
  (:require [work.agent :as agent]
            [work.agent.protocol :as protocol]))

(def results
  [(test-check
    "ephemeral coordinators implement the coordinator capability boundary"
    (satisfies?
     protocol/IWorkAgentCoordinator
     (agent/ephemeral-coordinator))
    true)])

results
