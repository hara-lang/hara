{:format "code.manage.test-doc/2" :test-path "std/substrate/space_test.hal" :namespace std.substrate.space-test :namespace-form "(ns std.substrate.space-test\n  (:use code.test)\n  (:require [std.substrate :as substrate]\n            [std.substrate.space :as space]))" :support-forms [] :facts [{:checked true :title "constructs a space entry with defaults" :source "(fact \"constructs a space entry with defaults\"\n  (let [entry (space/space \"room/a\" {\"state\" {\"count\" 1}})]\n    [(get entry \"id\")\n     (get-in entry [\"state\" \"count\"])\n     (get entry \"meta\")])\n  => [\"room/a\" 1 {}])" :meta {} :refer-source :qualified-call :nested nil :original-refer false :refer std.substrate.space/space :refer-candidates [std.substrate.space/space] :order 137 :results nil} {:checked true :title "gets ensures lists and removes spaces" :source "(fact \"gets ensures lists and removes spaces\"\n  (let [node (substrate/node-create \"node/spaces\")\n        first-entry (space/ensure-space\n                     node \"room/a\" {\"state\" {\"count\" 1}})\n        second-entry (space/ensure-space\n                      node \"room/a\" {\"state\" {\"count\" 9}})]\n    (space/create-space node \"room/b\" nil)\n    [(get-in first-entry [\"state\" \"count\"])\n     (get-in second-entry [\"state\" \"count\"])\n     (space/list-spaces node)\n     (get (space/remove-space node \"room/a\") \"id\")\n     (nil? (space/get-space node \"room/a\"))])\n  => [1 1 [\"room/a\" \"room/b\"] \"room/a\" true])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [std.substrate/node-create std.substrate.space/ensure-space std.substrate.space/create-space std.substrate.space/list-spaces std.substrate.space/remove-space std.substrate.space/get-space] :order 353 :results nil} {:checked true :title "gets and creates default state on demand" :source "(fact \"gets and creates default state on demand\"\n  (let [node (substrate/node-create \"node/state\")]\n    (space/create-space node \"room/a\" {\"state\" {\"count\" 2}})\n    [(get-in (space/get-space-state node \"room/a\") [\"count\"])\n     (space/get-space-state node \"room/b\")])\n  => [2 {}])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [std.substrate/node-create std.substrate.space/create-space std.substrate.space/get-space-state] :order 955 :results nil} {:checked true :title "sets and updates state with entry and node context" :source "(fact \"sets and updates state with entry and node context\"\n  (let [node (substrate/node-create \"node/update\")]\n    (space/set-space-state node \"room/a\" {\"count\" 1})\n    (space/update-space-state\n     node \"room/a\"\n     (fn [state entry current-node]\n       {\"count\" (+ (get state \"count\") 4)\n        \"id\" (get entry \"id\")\n        \"node\" (:id current-node)}))\n    [(get-in (space/get-space-state node \"room/a\") [\"count\"])\n     (get-in (space/get-space-state node \"room/a\") [\"id\"])\n     (get-in (space/get-space-state node \"room/a\") [\"node\"])])\n  => [5 \"room/a\" \"node/update\"])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [std.substrate/node-create std.substrate.space/set-space-state std.substrate.space/update-space-state std.substrate.space/get-space-state] :order 1237 :results nil} {:checked true :title "nil space ids resolve to the node-wide space" :source "(fact \"nil space ids resolve to the node-wide space\"\n  (let [node (substrate/node-create \"node/root\")]\n    (space/set-space-state node nil {\"value\" 42})\n    [(space/list-spaces node)\n     (get-in (space/get-space-state node nil) [\"value\"])])\n  => [[\"__NODE__\"] 42])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [std.substrate/node-create std.substrate.space/set-space-state std.substrate.space/list-spaces std.substrate.space/get-space-state] :order 1814 :results nil}] :noncanonical 5 :unresolved 4}
