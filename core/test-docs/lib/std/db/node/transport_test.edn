{:format "code.manage.test-doc/2" :test-path "std/db/node/transport_test.hal" :namespace std.db.node.transport-test :namespace-form "(ns std.db.node.transport-test\n  (:use code.test)\n  (:require [std.db.node.client-base :as client]\n            [std.db.node.runtime :as runtime]\n            [std.db.node.transport :as transport]\n            [db.protocol :as db-protocol]\n            [std.foundation.promise :as promise]\n            [std.substrate :as substrate]))" :support-forms [{:source "(defstruct MessageConnection\n  [id closed])" :order 331} {:source "(extend-type MessageConnection db-protocol/IDatabase\n  (db-engine [connection]\n    :message-sql)\n  (db-provider [connection]\n    :message)\n  (db-info [connection]\n    {:id (:id connection)\n     :engine :message-sql\n     :provider :message})\n  (db-exec [connection sql parameters]\n    (promise/from {:affected 1\n                   :sql sql\n                   :parameters parameters}))\n  (db-query [connection sql parameters]\n    (promise/from {:columns [\"value\"]\n                   :rows [[(first parameters)]]\n                   :affected 0}))\n  (db-close [connection]\n    (reset! (:closed connection) true)\n    (promise/from true)))" :order 376} {:source "(defn message-driver\n  []\n  {:id :message\n   :engine :message-sql\n   :open (fn [options]\n           (promise/from\n            (std.db.node.transport-test/MessageConnection\n             (get options :id \"message-1\")\n             (atom false))))})" :order 1011} {:source "(defn endpoint-pair\n  []\n  (let [left-listener (atom nil)\n        right-listener (atom nil)\n        left-closed (atom false)\n        right-closed (atom false)]\n    [{:send (fn [message]\n              (let [listener @right-listener]\n                (when listener (listener message))))\n      :listen (fn [listener]\n                (reset! left-listener listener))\n      :close (fn [] (reset! left-closed true))\n      :closed left-closed}\n     {:send (fn [message]\n              (let [listener @left-listener]\n                (when listener (listener message))))\n      :listen (fn [listener]\n                (reset! right-listener listener))\n      :close (fn [] (reset! right-closed true))\n      :closed right-closed}]))" :order 1258}] :facts [{:checked true :title "portable nested values round-trip through tagged wire data" :source "(fact \"portable nested values round-trip through tagged wire data\"\n     (let [value {:kind :db/runtime\n                  :symbol 'db/value\n                  :flags #{:read :write}\n                  [:composite 1] {:nested :ok}\n                  :rows [[1 \"wombat\"]]}]\n       (transport/wire->value (transport/value->wire value)))\n     => {:kind :db/runtime\n         :symbol 'db/value\n         :flags #{:read :write}\n         [:composite 1] {:nested :ok}\n         :rows [[1 \"wombat\"]]})" :meta {} :refer-source :unresolved :nested true :original-refer false :refer nil :refer-candidates [std.db.node.transport/wire->value std.db.node.transport/value->wire] :order 1994 :results true} {:checked true :title "thrown values become clone-safe structured errors" :source "(fact \"thrown values become clone-safe structured errors\"\n     (let [encoded (transport/error->wire\n                    (ex-info \"database failed\" {:tag :db/failure}))]\n       [(get encoded \"$hara\")\n        (get encoded \"message\")])\n     => [\"error\" \"database failed\"])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer std.db.node.transport/error->wire :refer-candidates [std.db.node.transport/error->wire] :order 2484 :results true} {:checked true :title "canonical map frames round-trip through the message representation" :source "(fact \"canonical map frames round-trip through the message representation\"\n     (let [frame (substrate/node-frame :request\n                                       \"req-1\"\n                                       \"main\"\n                                       {:trace :trace/one}\n                                       \"@xt.db/query\"\n                                       [\"db/primary\" \"select 1\" [{:mode :fast}]]\n                                       nil nil nil nil nil nil)\n           decoded (transport/wire->frame (transport/frame->wire frame))]\n       [(map? decoded)\n        (substrate/frame-id decoded)\n        (substrate/frame-kind decoded)\n        (substrate/frame-space decoded)\n        (substrate/frame-meta decoded)\n        (get decoded \"action\")\n        (get decoded \"args\")])\n     => [true\n         \"req-1\"\n         \"request\"\n         \"main\"\n         {:trace :trace/one}\n         \"@xt.db/query\"\n         [\"db/primary\" \"select 1\" [{:mode :fast}]]])" :meta {} :refer-source :unresolved :nested true :original-refer false :refer nil :refer-candidates [std.substrate/node-frame std.db.node.transport/wire->frame std.db.node.transport/frame->wire std.substrate/frame-id std.substrate/frame-kind std.substrate/frame-space std.substrate/frame-meta] :order 2758 :results true} {:checked true :title "ready messages are separated from substrate frames" :source "(fact \"ready messages are separated from substrate frames\"\n     [(transport/ready-message?\n       {\"signal\" \"ready\"\n        \"transport\" \"transport\"\n        \"worker\" \"worker\"})\n      (transport/ready-message? {\"kind\" \"response\"})]\n     => [true false])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer std.db.node.transport/ready-message? :refer-candidates [std.db.node.transport/ready-message?] :order 3722 :results true} {:checked true :title "node worker adapters initialize and query across paired endpoints" :source "(fact \"node worker adapters initialize and query across paired endpoints\"\n     (let [endpoints (endpoint-pair)\n           client-endpoint (first endpoints)\n           server-endpoint (second endpoints)\n           client-node (substrate/node-create \"message-client\")\n           server-node (substrate/node-create \"message-server\")\n           client-adapter (transport/nodeworker-adapter client-endpoint)\n           server-adapter (transport/nodeworker-adapter server-endpoint)\n           config {:primary {:type :message\n                             :options {:id \"runtime-message\"}}}\n           connected-promise (runtime/nodeworker-connect\n                              client-node config {} {} client-adapter {})\n           booted (IDerefTimeout/deref-timeout\n                   (runtime/nodeworker-init-kernel\n                    server-node\n                    server-adapter\n                    {:drivers {:message (message-driver)}})\n                   2000 :timeout)\n           connected (IDerefTimeout/deref-timeout connected-promise 2000 :timeout)\n           output (IDerefTimeout/deref-timeout\n                   (client/query client-node\n                                 \"db/primary\"\n                                 \"select value\"\n                                 [42]\n                                 {})\n                   2000 :timeout)]\n       [(instance? substrate/SubstrateNode client-node)\n        (instance? substrate/SubstrateNode server-node)\n        (get booted :transport-attached)\n        (get connected :transport-attached)\n        (get (get connected :init) :status)\n        (get output :rows)])\n     => [true true true true :setup [[42]]])" :meta {} :refer-source :unresolved :nested true :original-refer false :refer nil :refer-candidates [std.substrate/node-create std.db.node.transport/nodeworker-adapter std.db.node.runtime/nodeworker-connect std.db.node.runtime/nodeworker-init-kernel std.db.node.client-base/query std.substrate/SubstrateNode] :order 3978 :results true}] :noncanonical 5 :unresolved 3}
