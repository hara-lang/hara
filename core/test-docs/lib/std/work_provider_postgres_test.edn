{:format "code.manage.test-doc/2" :test-path "std/work_provider_postgres_test.hal" :namespace std.work-provider-postgres-test :namespace-form "(ns std.work-provider-postgres-test\n  (:use code.test)\n  (:require [work.provider.postgres :as postgres]))" :support-forms [{:source "(def operations\n  {:next-run-id (fn [] \"run-1\")\n   :validate-run-id (fn [id] id)\n   :create-run (fn [run] run)\n   :load-run (fn [id] nil)\n   :list-runs (fn [query] [])\n   :transact (fn [transition] transition)\n   :list-events (fn [id] [])\n   :load-checkpoint (fn [key] nil)\n   :list-checkpoints (fn [id] [])\n   :list-outbox (fn [query] [])\n   :claim-outbox (fn [options] [])\n   :ack-outbox (fn [id options] nil)})" :order 112} {:source "(def provider\n  (postgres/postgres-store operations))" :order 527}] :facts [{:checked true :title "PostgreSQL declares the shared operational store role" :source "(fact \"PostgreSQL declares the shared operational store role\"\n     (postgres/describe)\n     => {:provider/role :work/store\n         :provider/id :postgres\n         :provider/schema-version 1\n         :provider/value-profile \"hara.hta/0-alpha\"\n         :provider/capabilities\n         #{:runs\n           :transactions\n           :events\n           :checkpoints\n           :outbox\n           :persistent\n           :distributed-claims\n           :leases\n           :fencing}})" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer work.provider.postgres/describe :refer-candidates [work.provider.postgres/describe] :order 598 :results true} {:checked true :title "the schema is application-neutral and versioned" :source "(fact \"the schema is application-neutral and versioned\"\n     [(get (postgres/schema) :migration/id)\n      (get (postgres/schema) :migration/version)\n      (get (postgres/schema) :migration/schema)\n      (get (postgres/schema) :migration/value-profile)\n      (get (postgres/schema) :migration/tables)\n      (count (get (postgres/schema) :migration/statements))]\n     => [\"std.work.postgres/1\"\n         1\n         \"std_work\"\n         \"hara.hta/0-alpha\"\n         [\"std_work.store_metadata\"\n          \"std_work.work_run\"\n          \"std_work.work_transition\"\n          \"std_work.work_event\"\n          \"std_work.work_checkpoint\"\n          \"std_work.work_outbox\"]\n         19])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer work.provider.postgres/schema :refer-candidates [work.provider.postgres/schema] :order 1077 :results true} {:checked true :title "atomic transition statements carry explicit CAS metadata" :source "(fact \"atomic transition statements carry explicit CAS metadata\"\n     [(get (postgres/query :transition/lock) :locking)\n      (get (postgres/query :transition/lock) :transactional)\n      (get (postgres/query :transition/update-run) :cas)\n      (get (postgres/query :transition/update-run) :transactional)\n      (get (postgres/query :transition/record) :transactional)]\n     => [:row true :run/revision true true])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer work.provider.postgres/query :refer-candidates [work.provider.postgres/query] :order 1752 :results true} {:checked true :title "distributed outbox claims are leased, fenced, and skip locked" :source "(fact \"distributed outbox claims are leased, fenced, and skip locked\"\n     [(get (postgres/query :outbox/claim) :locking)\n      (get (postgres/query :outbox/claim) :lease)\n      (get (postgres/query :outbox/claim) :fencing)\n      (get (postgres/query :outbox/ack) :fencing)]\n     => [:skip-locked\n         :required\n         :monotonic-generation\n         :claim-id-and-generation])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer work.provider.postgres/query :refer-candidates [work.provider.postgres/query] :order 2170 :results true} {:checked true :title "completed checkpoints and outbox intents declare immutability" :source "(fact \"completed checkpoints and outbox intents declare immutability\"\n     [(get (postgres/query :checkpoint/upsert) :completed-immutability)\n      (get (postgres/query :outbox/insert) :idempotency)\n      (get (postgres/query :event/append) :idempotency)\n      (get (postgres/query :run/create) :idempotency)]\n     => [true\n         :compare-existing-value\n         :compare-existing-value\n         :compare-existing-value])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer work.provider.postgres/query :refer-candidates [work.provider.postgres/query] :order 2557 :results true} {:checked true :title "bound PostgreSQL operations satisfy the standard store contract" :source "(fact \"bound PostgreSQL operations satisfy the standard store contract\"\n     [(:provider/role provider)\n      (:provider/id provider)\n      (:provider/schema-version provider)\n      (:provider/value-profile provider)\n      (= operations (:provider/operations provider))]\n     => [:work/store :postgres 1 \"hara.hta/0-alpha\" true])" :meta {} :refer-source :unresolved :nested true :original-refer false :refer nil :refer-candidates [] :order 2986 :results true} {:checked true :title "portable provider descriptors carry no connection authority" :source "(fact \"portable provider descriptors carry no connection authority\"\n     [(has? provider :connection)\n      (has? provider :password)\n      (has? provider :url)\n      (has? provider :endpoint)\n      (has? (postgres/describe) :connection)]\n     => [false false false false false])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer work.provider.postgres/describe :refer-candidates [work.provider.postgres/describe] :order 3320 :results true}] :noncanonical 7 :unresolved 1}
