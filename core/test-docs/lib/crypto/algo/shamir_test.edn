{:format "code.manage.test-doc/2" :test-path "crypto/algo/shamir_test.hal" :namespace std.crypto.shamir-test :namespace-form "(ns std.crypto.shamir-test\n  (:use code.test)\n  (:require [std.crypto.shamir :as shamir]\n            [std.foundation.bytes :as byte]))" :support-forms [] :facts [{:checked true :title "any threshold subset reconstructs the secret" :source "(fact \"any threshold subset reconstructs the secret\"\n  (let [secret (bytes 1 2 3 4)\n        shares (shamir/split-with-coefficients\n                secret 3 2 (bytes 17 23 42 99))]\n    [(= secret (shamir/combine [(nth shares 0) (nth shares 1)]))\n     (= secret (shamir/combine [(nth shares 0) (nth shares 2)]))\n     (= secret (shamir/combine [(nth shares 1) (nth shares 2)]))\n     (mapv byte/count shares)])\n  => [true true true [5 5 5]])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [std.crypto.shamir/split-with-coefficients std.crypto.shamir/combine std.foundation.bytes/count] :order 136 :results nil} {:checked true :title "rejects duplicate and inconsistent shares" :source "(fact \"rejects duplicate and inconsistent shares\"\n  (let [shares (shamir/split-with-coefficients\n                (bytes 1 2 3) 3 2 (bytes 7 11 13))]\n    [(try (shamir/combine [(nth shares 0) (nth shares 0)]) false\n          (catch Throwable error true))\n     (try (shamir/combine [(nth shares 0)\n                           (byte/slice (nth shares 1) 0 2)])\n          false\n          (catch Throwable error true))])\n  => [true true])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [std.crypto.shamir/split-with-coefficients std.crypto.shamir/combine std.foundation.bytes/slice] :order 575 :results nil}] :noncanonical 2 :unresolved 2}
