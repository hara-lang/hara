{:format "code.manage.test-doc/2" :test-path "tool/cli/template_test.hal" :namespace tool.cli.template-test :namespace-form "(ns tool.cli.template-test\n  (:use code.test)\n  (:require [std.work :as work]\n            [tool.cli.handlers :as handlers]\n            [tool.cli.model :as model]\n            [tool.cli.template :as template]))" :support-forms [] :facts [{:checked true :title "template defaults are recursively applied by template type" :source "(fact \"template defaults are recursively applied by template type\"\n  (do\n    (template/install-defaults!\n     :template-test\n     {:version 7\n      :main {:argcount 1}})\n    (let [definition\n          (template/normalise-definition\n           {:id :template-test/defaults\n            :template :template-test\n            :main {:fn (fn [input] (model/success input))}})\n          output [(:version definition)\n                  (get-in definition [:main :argcount])]]\n      (template/uninstall-defaults! :template-test)\n      output))\n  => [7 1])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [tool.cli.template/install-defaults! tool.cli.template/normalise-definition tool.cli.model/success tool.cli.template/uninstall-defaults!] :order 210 :results nil} {:checked true :title "prepare main finish and validation retain stage order" :source "(fact \"prepare main finish and validation retain stage order\"\n  (let [stages (atom [])\n        handler\n        (template/handler\n         {:id :template-test/stages\n          :version 1\n          :prepare\n          (fn [request context]\n            (swap! stages conj :prepare)\n            (assoc request :prepared true))\n          :main\n          {:argcount 2\n           :fn\n           (fn [request context]\n             (swap! stages conj :main)\n             (model/success\n              {:prepared (:prepared request)}))}\n          :finish\n          (fn [result context]\n            (swap! stages conj :finish)\n            (assoc result\n                   :result/data\n                   (assoc (:result/data result)\n                          :finished true)))})\n        result (handler {})]\n    [(deref stages)\n     (:result/outcome result)\n     (:result/data result)])\n  => [[:prepare :main :finish]\n      :tool.cli.outcome/success\n      {:prepared true :finished true}])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [tool.cli.template/handler tool.cli.model/success] :order 758 :results nil} {:checked true :title "handler metadata retains only data-safe provenance" :source "(fact \"handler metadata retains only data-safe provenance\"\n  (let [handler\n        (template/handler\n         {:id :template-test/metadata\n          :version 9\n          :template :metadata-test\n          :main\n          {:argcount 1\n           :fn (fn [request] (model/success request))}})\n        metadata (meta handler)\n        result (handler {:value 42})]\n    [metadata\n     (template/handler? handler)\n     (:result/data result)])\n  => [{:tool.cli.template/schema \"tool.cli.template/0-alpha\"\n       :tool.cli.template/id :template-test/metadata\n       :tool.cli.template/version 9\n       :tool.cli.template/type :metadata-test}\n      true\n      {:value 42}])" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [tool.cli.template/handler tool.cli.model/success tool.cli.template/handler?] :order 1736 :results nil} {:checked true :title "runtime injection is evaluated once per handler invocation" :source "(fact \"runtime injection is evaluated once per handler invocation\"\n  (let [calls (atom 0)\n        handler\n        (template/handler\n         {:id :template-test/runtime\n          :version 1\n          :runtime-fn\n          (fn []\n            (swap! calls inc)\n            (work/local-runtime))\n          :main\n          {:argcount 1\n           :fn (fn [request] (model/success request))}})]\n    (handler {:value 1})\n    (handler {:value 2})\n    (deref calls))\n  => 2)" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [tool.cli.template/handler std.work/local-runtime tool.cli.model/success] :order 2402 :results nil} {:checked true :title "template does not catch command exceptions" :source "(fact \"template does not catch command exceptions\"\n  (let [handler\n        (template/handler\n         {:id :template-test/exception\n          :version 1\n          :main\n          {:argcount 1\n           :fn\n           (fn [request]\n             (throw\n              (ex-info \"template exception\" {})))}})]\n    (try\n      (do (handler {}) false)\n      (catch Throwable error\n        true)))\n  => true)" :meta {} :refer-source :qualified-call :nested nil :original-refer false :refer tool.cli.template/handler :refer-candidates [tool.cli.template/handler] :order 2870 :results nil} {:checked true :title "invalid result envelopes fail before leaving the template" :source "(fact \"invalid result envelopes fail before leaving the template\"\n  (let [handler\n        (template/handler\n         {:id :template-test/result\n          :version 1\n          :main {:argcount 1\n                 :fn (fn [request] {:invalid true})}})]\n    (try\n      (do (handler {}) false)\n      (catch Throwable error\n        true)))\n  => true)" :meta {} :refer-source :qualified-call :nested nil :original-refer false :refer tool.cli.template/handler :refer-candidates [tool.cli.template/handler] :order 3272 :results nil} {:checked true :title "every closed-registry handler is template-produced" :source "(fact \"every closed-registry handler is template-produced\"\n  (every?\n   template/handler?\n   (vals (handlers/registry)))\n  => true)" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [tool.cli.template/handler? tool.cli.handlers/registry] :order 3618 :results nil}] :noncanonical 7 :unresolved 5}
