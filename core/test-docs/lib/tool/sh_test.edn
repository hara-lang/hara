{:format "code.manage.test-doc/2" :test-path "tool/sh_test.hal" :namespace tool.sh-test :namespace-form "(ns tool.sh-test\n  (:use code.test)\n  (:require [tool.sh :as sh]))" :support-forms [{:source "(defn fake-runner\n  [argv options]\n  {:command argv\n   :stdout (or (:stdout options) \"ok\")\n   :stderr (or (:stderr options) \"\")\n   :exit (or (:exit options) 0)})" :order 68}] :facts [{:checked true :title "tool.sh validates argv and delegates through an injectable runner" :source "(fact \"tool.sh validates argv and delegates through an injectable runner\"\n     (sh/execute [\"printf\" \"ok\"] {:tool.sh/runner fake-runner})\n     => {:command [\"printf\" \"ok\"] :stdout \"ok\" :stderr \"\" :exit 0})" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer tool.sh/execute :refer-candidates [tool.sh/execute] :order 247 :results true} {:checked true :title "tool.sh run! keeps the structured failure in ex-data" :source "(fact \"tool.sh run! keeps the structured failure in ex-data\"\n     (try\n       (sh/execute! [\"false\"] {:tool.sh/runner fake-runner\n                                :stdout \"out\"\n                                :stderr \"err\"\n                                :exit 7})\n       nil\n       (catch Throwable error\n         [(ex-message error) (ex-data error)]))\n     => [\"Command failed\"\n         {:command [\"false\"] :stdout \"out\" :stderr \"err\" :exit 7}])" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer tool.sh/execute! :refer-candidates [tool.sh/execute!] :order 457 :results true} {:checked true :title "tool.sh rejects implicit command strings" :source "(fact \"tool.sh rejects implicit command strings\"\n     (try (sh/command \"echo unsafe\") false (catch Throwable error true))\n     => true)" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer tool.sh/command :refer-candidates [tool.sh/command] :order 908 :results true} {:checked true :title "process options expose only cwd and env" :source "(fact \"process options expose only cwd and env\"\n     (sh/process-options {:cwd \"/tmp\" :env {\"A\" \"1\"} :trim false :other true})\n     => {:cwd \"/tmp\" :env {\"A\" \"1\"}})" :meta {} :refer-source :qualified-call :nested true :original-refer false :refer tool.sh/process-options :refer-candidates [tool.sh/process-options] :order 1048 :results true}] :noncanonical 4 :unresolved 0}
