{:format "code.manage.test-doc/2" :test-path "code/test_facade_test.hal" :namespace code.test-facade-test :namespace-form "(ns code.test-facade-test\n  (:use code.test)\n  (:require [code.test.base.runtime :as runtime]\n            [code.test.work :as test-work]\n            [std.work :as work]))" :support-forms [{:source "(fact:template \"facade macros register through the canonical runtime\"\n  (select-keys\n   (runtime/get-fact\n    \"code.test-facade-test\"\n    \"facade macros register through the canonical runtime\")\n   [:namespace :name])\n  => {:namespace \"code.test-facade-test\"\n      :name \"facade macros register through the canonical runtime\"}\n\n  [(:op (work/work-spec +runner-work+))\n   (:op (work/work-spec test-work/+runner-work+))]\n  => [:chain :chain])" :order 507}] :facts [{:checked true :title "facade assertion calls remain available through :use and qualification" :source "(fact \"facade assertion calls remain available through :use and qualification\"\n  (select-keys (check (fn [] 42) 42) [:pass :status :actual])\n  => {:pass true :status :success :actual 42}\n\n  (select-keys (code.test/check (fn [] :ready) :ready)\n               [:pass :status :actual])\n  => {:pass true :status :success :actual :ready})" :meta {} :refer-source :unresolved :nested nil :original-refer false :refer nil :refer-candidates [] :order 172 :results nil}] :noncanonical 1 :unresolved 1}
