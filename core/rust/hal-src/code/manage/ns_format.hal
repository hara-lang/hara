(ns code.manage.ns-format
  (:require [code.manage.unit :as unit]
            [code.manage.unit.template :as template]))

(defn ns-format
  [code-unit]
  (unit/heal-code code-unit))

(def ns-format-unit
  (template/code-tasklet
   :ns-format
   (fn [code-unit options]
     (ns-format code-unit))))
