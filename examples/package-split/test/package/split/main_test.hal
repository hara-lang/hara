(ns package.split.main-test
  (:use code.test)
  (:require [package.split.main :as subject]))

^{:refer package.split.main/proof :added "0.1"}
(fact "calls public APIs from both split packages"
  (subject/proof)
  => {:runtime-lang :xtalk
      :runtime-type :default
      :checker-type :exactly})
