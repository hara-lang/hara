#!/usr/bin/env python3
"""Materialize the canonical tool.sh implementation for #645."""

from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]

FILES = {'.github/workflows/tool-sh-contract.yml': 'name: tool.sh contract\n'
                                           '\n'
                                           'on:\n'
                                           '  pull_request:\n'
                                           '    branches: [main]\n'
                                           '    paths:\n'
                                           '      - .github/workflows/tool-sh-contract.yml\n'
                                           '      - core/lib/src/tool/sh.hal\n'
                                           '      - core/lib/src/tool/sh/**\n'
                                           '      - core/lib/src/tool/runtime.hal\n'
                                           '      - core/lib/src/tool/cli/identity.hal\n'
                                           '      - core/lib/src/code/deploy/runtime.hal\n'
                                           '      - core/lib/src/lang/runtime/basic/type_oneshot.hal\n'
                                           '      - core/lib/src/lang/runtime/basic/type_verify.hal\n'
                                           '      - core/lib/test/tool/sh_test.hal\n'
                                           '      - core/lib/test/tool/sh/**\n'
                                           '      - core/lib/test-lang/lang/runtime/basic/type_verify_test.hal\n'
                                           '      - core/lib/test-lang/lang/runtime/basic/type_oneshot_test.hal\n'
                                           '      - core/rust/standard-library.namespaces\n'
                                           '      - scripts/runtime/check-tool-sh-hard-cut.py\n'
                                           '  push:\n'
                                           '    branches: [main]\n'
                                           '    paths:\n'
                                           '      - .github/workflows/tool-sh-contract.yml\n'
                                           '      - core/lib/src/tool/sh.hal\n'
                                           '      - core/lib/src/tool/sh/**\n'
                                           '      - core/lib/src/tool/runtime.hal\n'
                                           '      - core/lib/src/tool/cli/identity.hal\n'
                                           '      - core/lib/src/code/deploy/runtime.hal\n'
                                           '      - core/lib/src/lang/runtime/basic/type_oneshot.hal\n'
                                           '      - core/lib/src/lang/runtime/basic/type_verify.hal\n'
                                           '      - core/lib/test/tool/sh_test.hal\n'
                                           '      - core/lib/test/tool/sh/**\n'
                                           '      - core/lib/test-lang/lang/runtime/basic/type_verify_test.hal\n'
                                           '      - core/lib/test-lang/lang/runtime/basic/type_oneshot_test.hal\n'
                                           '      - core/rust/standard-library.namespaces\n'
                                           '      - scripts/runtime/check-tool-sh-hard-cut.py\n'
                                           '  workflow_dispatch:\n'
                                           '\n'
                                           'permissions:\n'
                                           '  contents: read\n'
                                           '\n'
                                           'jobs:\n'
                                           '  contract:\n'
                                           '    runs-on: ubuntu-latest\n'
                                           '    steps:\n'
                                           '      - name: Check out Hara\n'
                                           '        uses: actions/checkout@v4\n'
                                           '\n'
                                           '      - name: Check out specifications registry\n'
                                           '        uses: actions/checkout@v4\n'
                                           '        with:\n'
                                           '          repository: hara-lang/hara-specs-registry\n'
                                           '          path: hara-specs-registry\n'
                                           '          fetch-depth: 1\n'
                                           '\n'
                                           '      - name: Expose sibling specifications path\n'
                                           '        run: ln -sfn "$GITHUB_WORKSPACE/hara-specs-registry" "$GITHUB_WORKSPACE/../hara-specs-registry"\n'
                                           '\n'
                                           '      - name: Set up JDK 21\n'
                                           '        uses: actions/setup-java@v4\n'
                                           '        with:\n'
                                           "          java-version: '21'\n"
                                           '          distribution: temurin\n'
                                           '          cache: maven\n'
                                           '          cache-dependency-path: core/java/pom.xml\n'
                                           '\n'
                                           '      - name: Set up Rust\n'
                                           '        uses: dtolnay/rust-toolchain@stable\n'
                                           '        with:\n'
                                           '          toolchain: stable\n'
                                           '\n'
                                           '      - name: Audit tool ownership and materialize the Cargo HAL snapshot\n'
                                           '        run: |\n'
                                           '          python3 scripts/runtime/check-tool-sh-hard-cut.py\n'
                                           '          python3 scripts/runtime/sync-rust-hal-src\n'
                                           '          python3 scripts/runtime/sync-rust-hal-src --check\n'
                                           '          git diff --check\n'
                                           '\n'
                                           '      - name: Build JVM and Rust runtimes\n'
                                           '        run: |\n'
                                           '          mvn -B -f core/java/pom.xml -Ptruffle -DskipTests package\n'
                                           '          cargo build --manifest-path core/rust/Cargo.toml --bin hara\n'
                                           '\n'
                                           '      - name: Run focused portable tests\n'
                                           '        run: |\n'
                                           '          ./scripts/runtime/run-lib-tests \\\n'
                                           '            core/lib/test/tool/sh_test.hal \\\n'
                                           '            core/lib/test/tool/sh/docker_test.hal \\\n'
                                           '            core/lib/test/tool/sh/git_test.hal \\\n'
                                           '            core/lib/test/tool/sh/tmux_test.hal \\\n'
                                           '            core/lib/test/tool/sh/net_test.hal \\\n'
                                           '            core/lib/test/tool/sh/desktop_test.hal \\\n'
                                           '            core/lib/test/tool/runtime_test.hal \\\n'
                                           '            core/lib/test-lang/lang/runtime/basic/type_verify_test.hal \\\n'
                                           '            core/lib/test-lang/lang/runtime/basic/type_oneshot_test.hal\n'
                                           '\n'
                                           '      - name: Probe exact process semantics on JVM and Rust\n'
                                           '        run: |\n'
                                           "          cat > /tmp/tool-sh-contract-probe.hal <<'HAL'\n"
                                           '          (ns tool-sh-contract-probe\n'
                                           '            (:require [tool.sh :as sh]))\n'
                                           '\n'
                                           '          (let [pending (sh/execute\n'
                                           '                         ["sh" "-c"\n'
                                           '                          "printf \' out\\\\n\'; printf \' err\\\\n\' >&2; exit 7"])\n'
                                           '                async-result (deref pending)\n'
                                           '                input-result (sh/run ["cat"] {:stdin "hello\\\\n"})\n'
                                           '                cwd-result (sh/run ["pwd"] {:cwd "/tmp"})\n'
                                           '                env-result (sh/run ["printenv" "HARA_TOOL_SH_TEST"]\n'
                                           '                                   {:env {"HARA_TOOL_SH_TEST" "present"}})\n'
                                           '                failed (try\n'
                                           '                         (sh/run! ["sh" "-c" "printf out; printf err >&2; exit 9"])\n'
                                           '                         nil\n'
                                           '                         (catch Throwable error (ex-data error)))]\n'
                                           '            (pr-str\n'
                                           '             [(promise? pending)\n'
                                           '              (= {:command ["sh" "-c"\n'
                                           '                            "printf \' out\\\\n\'; printf \' err\\\\n\' >&2; exit 7"]\n'
                                           '                  :stdout " out\\\\n" :stderr " err\\\\n" :exit 7}\n'
                                           '                 async-result)\n'
                                           '              (= {:command ["cat"] :stdout "hello\\\\n" :stderr "" :exit 0}\n'
                                           '                 input-result)\n'
                                           '              (= "/tmp\\\\n" (:stdout cwd-result))\n'
                                           '              (= "present\\\\n" (:stdout env-result))\n'
                                           '              (= {:command ["sh" "-c" "printf out; printf err >&2; exit 9"]\n'
                                           '                  :stdout "out" :stderr "err" :exit 9}\n'
                                           '                 failed)]))\n'
                                           '          HAL\n'
                                           '\n'
                                           "          expected='[true true true true true true]'\n"
                                           '          actual="$(./core/hara --project core --offline run /tmp/tool-sh-contract-probe.hal)"\n'
                                           '          test "$actual" = "$expected"\n'
                                           '          actual="$(HARA_RUNTIME=rust \\\n'
                                           '            HARA_NATIVE="$GITHUB_WORKSPACE/core/rust/target/debug/hara" \\\n'
                                           '            ./core/hara --project core --offline run /tmp/tool-sh-contract-probe.hal)"\n'
                                           '          test "$actual" = "$expected"\n'
                                           '\n'
                                           '      - name: Final clean-tree audit\n'
                                           '        run: |\n'
                                           '          rm -rf hara-specs-registry\n'
                                           '          python3 scripts/runtime/check-tool-sh-hard-cut.py\n'
                                           '          python3 scripts/runtime/sync-rust-hal-src --check\n'
                                           '          git diff --check\n',
 'core/lib/src/code/deploy/runtime.hal': '(ns code.deploy.runtime\n'
                                         '  (:require [tool.sh :as sh]))\n'
                                         '\n'
                                         '(defn run\n'
                                         '  "Runs one argv vector through the canonical capability-gated process boundary."\n'
                                         '  ([argv]\n'
                                         '   (run argv {}))\n'
                                         '  ([argv options]\n'
                                         '   (sh/ensure! "Hara package command failed"\n'
                                         '               (sh/run argv options))))\n',
 'core/lib/src/lang/runtime/basic/type_oneshot.hal': '(ns lang.runtime.basic.type-oneshot\n'
                                                     '  (:require [lang.runtime.basic.type-common :as common]\n'
                                                     '            [tool.sh :as sh]))\n'
                                                     '\n'
                                                     '(defstruct RuntimeOneshot [id lang runtime program exec process])\n'
                                                     '\n'
                                                     '(defn- default-id\n'
                                                     '  [language runtime]\n'
                                                     '  (keyword (str (name language) "." (name runtime))))\n'
                                                     '\n'
                                                     '(defn rt-oneshot-setup\n'
                                                     '  "Prepares Foundation-compatible program, process and exec values."\n'
                                                     '  ([language program process exec]\n'
                                                     '   (rt-oneshot-setup language program process exec :oneshot))\n'
                                                     '  ([language program process exec context]\n'
                                                     '   (let [program (common/get-program-default language context program)\n'
                                                     '         process-options (common/get-options language context program)\n'
                                                     '         process (merge process-options process)\n'
                                                     '         exec (or exec\n'
                                                     '                  (common/get-program-exec language context program))]\n'
                                                     '     [program process exec])))\n'
                                                     '\n'
                                                     '(defn rt-oneshot:create\n'
                                                     '  "Creates a Foundation-shaped oneshot runtime."\n'
                                                     '  [options]\n'
                                                     '  (let [id (get options :id)\n'
                                                     '        lang (get options :lang)\n'
                                                     '        runtime (or (get options :runtime) :oneshot)\n'
                                                     '        exec (get options :exec)\n'
                                                     '        program (get options :program)\n'
                                                     '        process (get options :process)\n'
                                                     '        [program process exec]\n'
                                                     '        (rt-oneshot-setup lang program process exec runtime)]\n'
                                                     '    (lang.runtime.basic.type-oneshot/RuntimeOneshot\n'
                                                     '     (or id (default-id lang runtime))\n'
                                                     '     lang\n'
                                                     '     runtime\n'
                                                     '     program\n'
                                                     '     exec\n'
                                                     '     process)))\n'
                                                     '\n'
                                                     '(defn rt-oneshot\n'
                                                     '  [options]\n'
                                                     '  (rt-oneshot:create options))\n'
                                                     '\n'
                                                     '(defn- process-options\n'
                                                     '  [runtime]\n'
                                                     '  (select-keys (or (:process runtime) {}) [:cwd :env :stdin]))\n'
                                                     '\n'
                                                     '(defn oneshot-result\n'
                                                     '  "Builds the structured result returned by a one-shot target process."\n'
                                                     '  ([runtime result]\n'
                                                     '   (merge\n'
                                                     '    {:phase :eval\n'
                                                     '     :runtime/id (get runtime :id)\n'
                                                     '     :runtime/language (get runtime :lang)\n'
                                                     '     :lang (get runtime :lang)\n'
                                                     '     :runtime (get runtime :runtime)\n'
                                                     '     :program (get runtime :program)}\n'
                                                     '    result))\n'
                                                     '  ([runtime argv values]\n'
                                                     '   (oneshot-result\n'
                                                     '    runtime\n'
                                                     '    {:command argv\n'
                                                     '     :exit (nth values 2)\n'
                                                     '     :stdout (str/decode-utf8 (nth values 0))\n'
                                                     '     :stderr (str/decode-utf8 (nth values 1))})))\n'
                                                     '\n'
                                                     '(defn execute-oneshot\n'
                                                     '  "Executes emitted target source through the canonical one-shot boundary."\n'
                                                     '  [runtime source]\n'
                                                     '  (let [argv (vec (concat (get runtime :exec) [source]))]\n'
                                                     '    (try\n'
                                                     '      (oneshot-result\n'
                                                     '       runtime\n'
                                                     '       (sh/run argv (process-options runtime)))\n'
                                                     '      (catch Throwable error\n'
                                                     '        (throw\n'
                                                     '         (ex-info\n'
                                                     '          "Target runtime process could not complete"\n'
                                                     '          {:phase :eval\n'
                                                     '           :lang (get runtime :lang)\n'
                                                     '           :runtime (get runtime :runtime)\n'
                                                     '           :program (get runtime :program)\n'
                                                     '           :command argv\n'
                                                     '           :generated-fragment source\n'
                                                     '           :cause-data (ex-data error)}\n'
                                                     '          error))))))\n'
                                                     '\n'
                                                     '(defn raw-eval-oneshot\n'
                                                     '  "Evaluates emitted target source using the selected local program."\n'
                                                     '  [runtime source]\n'
                                                     '  (let [result (execute-oneshot runtime source)]\n'
                                                     '    (if (= 0 (get result :exit))\n'
                                                     '      (if (get-in runtime [:process :raw])\n'
                                                     '        result\n'
                                                     '        (str/trim-right (get result :stdout)))\n'
                                                     '      (throw\n'
                                                     '       (ex-info\n'
                                                     '        "Target runtime execution failed"\n'
                                                     '        (assoc result :generated-fragment source))))))\n',
 'core/lib/src/lang/runtime/basic/type_verify.hal': '(ns lang.runtime.basic.type-verify\n'
                                                    '  (:require [tool.sh :as sh]))\n'
                                                    '\n'
                                                    '(defn- process-options\n'
                                                    '  [runtime source]\n'
                                                    '  (assoc\n'
                                                    '   (select-keys (or (:process runtime) {}) [:cwd :env])\n'
                                                    '   :stdin source))\n'
                                                    '\n'
                                                    '(defn checker-result\n'
                                                    '  "Builds the structured result returned by a syntax checker.\n'
                                                    '\n'
                                                    '   The vector form is retained for existing pure callers; process execution\n'
                                                    '   now supplies the canonical tool.sh result map directly."\n'
                                                    '  [runtime value]\n'
                                                    '  (let [result\n'
                                                    '        (if (vector? value)\n'
                                                    '          {:command (get runtime :exec)\n'
                                                    '           :exit (nth value 2)\n'
                                                    '           :stdout (str/decode-utf8 (nth value 0))\n'
                                                    '           :stderr (str/decode-utf8 (nth value 1))}\n'
                                                    '          value)]\n'
                                                    '    (merge\n'
                                                    '     {:phase :verify\n'
                                                    '      :lang (get runtime :lang)\n'
                                                    '      :runtime (get runtime :runtime)\n'
                                                    '      :program (get runtime :program)}\n'
                                                    '     result)))\n'
                                                    '\n'
                                                    '(defn verify-result\n'
                                                    '  "Returns source after a successful check or throws a structured verify error."\n'
                                                    '  [runtime source result]\n'
                                                    '  (if (= 0 (get result :exit))\n'
                                                    '    source\n'
                                                    '    (throw\n'
                                                    '     (ex-info\n'
                                                    '      "Target source verification failed"\n'
                                                    '      (assoc result\n'
                                                    '             :generated-fragment source\n'
                                                    '             :grammar-coordinate (get runtime :grammar-coordinate)\n'
                                                    '             :grammar-version (get runtime :grammar-version))))))\n'
                                                    '\n'
                                                    '(defn verify-execute\n'
                                                    '  "Checks source through the runtime\'s stdin-based verifier."\n'
                                                    '  [runtime source]\n'
                                                    '  (let [argv (get runtime :exec)]\n'
                                                    '    (try\n'
                                                    '      (checker-result\n'
                                                    '       runtime\n'
                                                    '       (sh/run argv (process-options runtime source)))\n'
                                                    '      (catch Throwable error\n'
                                                    '        (throw\n'
                                                    '         (ex-info\n'
                                                    '          "Target source verifier could not complete"\n'
                                                    '          {:phase :verify\n'
                                                    '           :lang (get runtime :lang)\n'
                                                    '           :runtime (get runtime :runtime)\n'
                                                    '           :program (get runtime :program)\n'
                                                    '           :command argv\n'
                                                    '           :generated-fragment source\n'
                                                    '           :cause-data (ex-data error)}\n'
                                                    '          error))))))\n'
                                                    '\n'
                                                    '(defn verify-source\n'
                                                    '  "Checks emitted target source without executing it."\n'
                                                    '  [runtime source]\n'
                                                    '  (verify-result runtime source (verify-execute runtime source)))\n'
                                                    '\n'
                                                    '(defn raw-eval-verify\n'
                                                    '  "Runtime raw evaluator for the Foundation :verify coordinate."\n'
                                                    '  [runtime source]\n'
                                                    '  (verify-source runtime source))\n',
 'core/lib/src/tool/cli/identity.hal': '(ns tool.cli.identity\n'
                                       '  (:require [tool.cli.model :as model]\n'
                                       '            [tool.sh :as sh]\n'
                                       '            [std.work :as work]))\n'
                                       '\n'
                                       '(def +default-endpoint+ "https://id.hara-lang.org")\n'
                                       '\n'
                                       '(def +route-operations+\n'
                                       '  {:tool.cli.route/id-login :login\n'
                                       '   :tool.cli.route/id-enroll :enroll\n'
                                       '   :tool.cli.route/id-status :status\n'
                                       '   :tool.cli.route/id-key :key\n'
                                       '   :tool.cli.route/id-namespace :namespace})\n'
                                       '\n'
                                       '(defn operation-request [request]\n'
                                       '  (let [operation (get +route-operations+ (:request/route request))\n'
                                       '        arguments (:request/arguments request)]\n'
                                       '    (if operation\n'
                                       '      {:operation operation :arguments arguments}\n'
                                       '      (if (empty? arguments)\n'
                                       '        (throw\n'
                                       '         (ex-info "id requires login, enroll, status, namespace, or key" {}))\n'
                                       '        {:operation (keyword (first arguments))\n'
                                       '         :arguments (vec (rest arguments))}))))\n'
                                       '\n'
                                       '(defn endpoint []\n'
                                       '  (or (os/getenv "HARA_ID_ENDPOINT") +default-endpoint+))\n'
                                       '\n'
                                       '(defn trim-end-slash [value]\n'
                                       '  (if (str/ends-with? value "/")\n'
                                       '    (str/slice value 0 (dec (str/length value)))\n'
                                       '    value))\n'
                                       '\n'
                                       '(defn option [request name]\n'
                                       '  (get (:request/options request) name))\n'
                                       '\n'
                                       '(defn hex-character? [value]\n'
                                       '  (str/includes? "0123456789abcdef" value))\n'
                                       '\n'
                                       '(defn valid-hex? [value bytes]\n'
                                       '  (and (string? value)\n'
                                       '       (= (* bytes 2) (str/length value))\n'
                                       '       (every? (fn [index]\n'
                                       '                 (hex-character? (str/char-at value index)))\n'
                                       '               (range (str/length value)))))\n'
                                       '\n'
                                       '(defn canonical-enrollment [tap owner public-key challenge]\n'
                                       '  (str "{:enrollment/format \\"0.0.0-alpha\\" :enrollment/tap " (pr-str tap)\n'
                                       '       " :enrollment/provider :github :enrollment/owner " (pr-str owner)\n'
                                       '       " :enrollment/public-key " (pr-str public-key)\n'
                                       '       " :enrollment/challenge " (pr-str challenge) "}\\n"))\n'
                                       '\n'
                                       '(defn run-process [argv input]\n'
                                       '  (let [options (if input {:stdin input} {})\n'
                                       '        result (sh/run argv options)]\n'
                                       '    (if (= 0 (:exit result))\n'
                                       '      result\n'
                                       '      (throw\n'
                                       '       (ex-info (str/trim (:stderr result)) result)))))\n'
                                       '\n'
                                       '(defn request [method path body]\n'
                                       '  (let [url (str (trim-end-slash (endpoint)) path)\n'
                                       '        base ["curl" "--fail-with-body" "--silent" "--show-error"\n'
                                       '              "-X" method "-H" "accept: application/edn"]\n'
                                       '        argv (if body\n'
                                       '               (vec\n'
                                       '                (concat base\n'
                                       '                        ["-H" "content-type: application/edn"\n'
                                       '                         "--data-binary" "@-" url]))\n'
                                       '               (conj base url))]\n'
                                       '    (:stdout (run-process argv body))))\n'
                                       '\n'
                                       '(defn fetch-challenge [owner]\n'
                                       '  (let [challenge\n'
                                       '        (str/trim\n'
                                       '         (request "GET"\n'
                                       '                  (str "/v1/enrollments/challenge?owner=" owner)\n'
                                       '                  nil))]\n'
                                       '    (if (str/blank? challenge)\n'
                                       '      (throw\n'
                                       '       (ex-info "identity service returned an empty challenge" {}))\n'
                                       '      challenge)))\n'
                                       '\n'
                                       '(defn sign [source]\n'
                                       '  (let [signer (os/getenv "HARA_SIGNER")]\n'
                                       '    (if (or (nil? signer) (str/blank? signer))\n'
                                       '      (throw\n'
                                       '       (ex-info "HARA_SIGNER must name an external signer command" {}))\n'
                                       '      nil)\n'
                                       '    (let [response (edn/read\n'
                                       '                    (:stdout (run-process [signer] source)))\n'
                                       '          key-id (:key/id response)\n'
                                       '          signature (:signature response)]\n'
                                       '      (if (and (string? key-id) (string? signature))\n'
                                       '        {:key-id key-id :signature signature}\n'
                                       '        (throw\n'
                                       '         (ex-info\n'
                                       '          "signer response must contain :key/id and :signature"\n'
                                       '          {}))))))\n'
                                       '\n'
                                       '(defn enroll [request]\n'
                                       '  (let [owner (option request :owner)\n'
                                       '        tap-option (or (option request :tap) "hara")\n'
                                       '        tap (if (= tap-option "official") "hara" tap-option)\n'
                                       '        public-key (os/getenv "HARA_SIGNER_PUBLIC_KEY")]\n'
                                       '    (if (str/blank? owner)\n'
                                       '      (throw (ex-info "id enroll requires --owner" {}))\n'
                                       '      nil)\n'
                                       '    (if (valid-hex? public-key 32)\n'
                                       '      nil\n'
                                       '      (throw\n'
                                       '       (ex-info\n'
                                       '        "HARA_SIGNER_PUBLIC_KEY must be lowercase 32-byte hex"\n'
                                       '        {})))\n'
                                       '    (let [challenge (or (option request :challenge)\n'
                                       '                        (fetch-challenge owner))\n'
                                       '          source (canonical-enrollment tap owner public-key challenge)\n'
                                       '          signed (sign source)]\n'
                                       '      (if (option request :dry-run)\n'
                                       '        (model/success\n'
                                       '         signed\n'
                                       '         [(model/message\n'
                                       '           (str source\n'
                                       '                "key-id=" (:key-id signed)\n'
                                       '                " signature=" (:signature signed) "\\n"))])\n'
                                       '        (let [envelope\n'
                                       '              (str "{:enrollment/request " (pr-str source)\n'
                                       '                   " :enrollment/key-id " (pr-str (:key-id signed))\n'
                                       '                   " :enrollment/signature " (pr-str (:signature signed))\n'
                                       '                   "}\\n")\n'
                                       '              response (request "POST" "/v1/enrollments" envelope)]\n'
                                       '          (model/success response [(model/message response)]))))))\n'
                                       '\n'
                                       '(defn key-command [arguments]\n'
                                       '  (let [operation (first arguments)]\n'
                                       '    (cond\n'
                                       '      (= operation "list")\n'
                                       '      (request "GET" "/v1/keys" nil)\n'
                                       '\n'
                                       '      (= operation "rotate")\n'
                                       '      (request "POST" "/v1/keys/rotate" "{}\\n")\n'
                                       '\n'
                                       '      (= operation "revoke")\n'
                                       '      (if (= 2 (count arguments))\n'
                                       '        (request\n'
                                       '         "POST"\n'
                                       '         (str "/v1/keys/" (nth arguments 1) "/revocations")\n'
                                       '         "{:revocation/reason :publisher-request}\\n")\n'
                                       '        (throw\n'
                                       '         (ex-info "id key revoke requires KEY_ID" {})))\n'
                                       '\n'
                                       '      :else\n'
                                       '      (throw\n'
                                       '       (ex-info\n'
                                       '        "usage: hara id key <list|rotate|revoke KEY_ID>"\n'
                                       '        {})))))\n'
                                       '\n'
                                       '(defn execute [request]\n'
                                       '  (let [resolved (operation-request request)\n'
                                       '        operation (:operation resolved)\n'
                                       '        arguments (:arguments resolved)]\n'
                                       '    (cond\n'
                                       '      (= operation :login)\n'
                                       '      (let [url (str (trim-end-slash (endpoint)) "/login/github\\n")]\n'
                                       '        (model/success url [(model/message url)]))\n'
                                       '\n'
                                       '      (= operation :enroll)\n'
                                       '      (enroll request)\n'
                                       '\n'
                                       '      (= operation :status)\n'
                                       '      (let [response (request "GET" "/v1/status" nil)]\n'
                                       '        (model/success response [(model/message response)]))\n'
                                       '\n'
                                       '      (= operation :namespace)\n'
                                       '      (let [response (request "GET" "/v1/namespaces" nil)]\n'
                                       '        (model/success response [(model/message response)]))\n'
                                       '\n'
                                       '      (= operation :key)\n'
                                       '      (let [response (key-command arguments)]\n'
                                       '        (model/success response [(model/message response)]))\n'
                                       '\n'
                                       '      :else\n'
                                       '      (throw\n'
                                       '       (ex-info (str "unknown id command: " (name operation)) {})))))\n'
                                       '\n'
                                       '(def +command-work+\n'
                                       "  (work/step {:id 'tool.cli/identity :version 1}\n"
                                       '             (fn [request context] (execute request))))\n'
                                       '\n'
                                       '(defn handler [request]\n'
                                       '  (work/run (work/local-runtime) +command-work+ request))\n',
 'core/lib/src/tool/runtime.hal': '(ns tool.runtime\n'
                                  '  (:config {:override [eval]})\n'
                                  '  (:require [tool.sh :as sh]))\n'
                                  '\n'
                                  '(def +runtimes+\n'
                                  '  {:js {:runtime/id :js\n'
                                  '        :runtime/language :js\n'
                                  '        :runtime/tag :js\n'
                                  '        :runtime/name "JavaScript"\n'
                                  '        :runtime/environment "HARA_NODE"\n'
                                  '        :runtime/executable "node"\n'
                                  '        :runtime/arguments ["--input-type=module" "--eval"]}\n'
                                  '   :python {:runtime/id :python\n'
                                  '            :runtime/language :python\n'
                                  '            :runtime/tag :py\n'
                                  '            :runtime/name "Python"\n'
                                  '            :runtime/environment "HARA_PYTHON"\n'
                                  '            :runtime/executable "python3"\n'
                                  '            :runtime/arguments ["-c"]}\n'
                                  '   :lua {:runtime/id :lua\n'
                                  '         :runtime/language :lua\n'
                                  '         :runtime/tag :lua\n'
                                  '         :runtime/name "Lua"\n'
                                  '         :runtime/environment "HARA_LUA"\n'
                                  '         :runtime/executable "lua"\n'
                                  '         :runtime/arguments ["-e"]}})\n'
                                  '\n'
                                  '(defn runtime\n'
                                  '  "Returns the immutable descriptor for a supported target language."\n'
                                  '  [language]\n'
                                  '  (or (get +runtimes+ language)\n'
                                  '      (throw\n'
                                  '       (ex-info "Unknown Hara target language"\n'
                                  '                {:runtime/language language\n'
                                  '                 :runtime/supported [:js :python :lua]}))))\n'
                                  '\n'
                                  '(defn executable\n'
                                  '  "Resolves a runtime executable from options, environment, or its default."\n'
                                  '  [descriptor options]\n'
                                  '  (or (:executable options)\n'
                                  '      (os/getenv (:runtime/environment descriptor))\n'
                                  '      (:runtime/executable descriptor)))\n'
                                  '\n'
                                  '(defn command\n'
                                  '  "Builds argv for one target-language source evaluation.\n'
                                  '\n'
                                  '   :arguments may override the descriptor arguments. Tahto uses this to select\n'
                                  '   Foundation programs such as Node :nodejs, Python :cpython, or Lua :lua\n'
                                  '   without duplicating process execution."\n'
                                  '  ([language source]\n'
                                  '   (command language source {}))\n'
                                  '  ([language source options]\n'
                                  '   (let [descriptor (runtime language)\n'
                                  '         arguments (or (:arguments options)\n'
                                  '                       (:runtime/arguments descriptor))]\n'
                                  '     (vec\n'
                                  '      (concat [(executable descriptor options)]\n'
                                  '              arguments\n'
                                  '              [source])))))\n'
                                  '\n'
                                  '(defn- process-options\n'
                                  '  [options]\n'
                                  '  (select-keys (or options {}) [:cwd :env :stdin]))\n'
                                  '\n'
                                  '(defn- decode-result\n'
                                  '  [descriptor result]\n'
                                  '  (merge\n'
                                  '   {:runtime/id (:runtime/id descriptor)\n'
                                  '    :runtime/language (:runtime/language descriptor)\n'
                                  '    :runtime/tag (:runtime/tag descriptor)\n'
                                  '    :runtime/name (:runtime/name descriptor)}\n'
                                  '   result))\n'
                                  '\n'
                                  '(defn execute\n'
                                  '  "Executes JavaScript, Python, or Lua source once and returns a promise.\n'
                                  '\n'
                                  '   Runtime selection uses the Foundation language names :js, :python, and\n'
                                  '   :lua. Their grammar tags are :js, :py, and :lua. Process launch requires\n'
                                  '   the host :process capability."\n'
                                  '  ([language source]\n'
                                  '   (execute language source {}))\n'
                                  '  ([language source options]\n'
                                  '   (let [descriptor (runtime language)\n'
                                  '         argv (command language source options)]\n'
                                  '     (promise/then\n'
                                  '      (sh/execute argv (process-options options))\n'
                                  '      (fn [result]\n'
                                  '        (decode-result descriptor result))))))\n'
                                  '\n'
                                  '(defn run\n'
                                  '  "Executes target source and rejects the returned promise on non-zero exit."\n'
                                  '  ([language source]\n'
                                  '   (run language source {}))\n'
                                  '  ([language source options]\n'
                                  '   (promise/then\n'
                                  '    (execute language source options)\n'
                                  '    (fn [result]\n'
                                  '      (if (= 0 (:exit result))\n'
                                  '        result\n'
                                  '        (throw\n'
                                  '         (ex-info "Target runtime execution failed"\n'
                                  '                  result)))))))\n'
                                  '\n'
                                  '(defn eval\n'
                                  '  "Compatibility spelling for run."\n'
                                  '  ([language source]\n'
                                  '   (run language source))\n'
                                  '  ([language source options]\n'
                                  '   (run language source options)))\n'
                                  '\n'
                                  '(defn oneshot\n'
                                  '  "Compatibility spelling for execute."\n'
                                  '  ([language source]\n'
                                  '   (execute language source))\n'
                                  '  ([language source options]\n'
                                  '   (execute language source options)))\n',
 'core/lib/src/tool/sh.hal': ';; ============================================================================\n'
                             ';; tool.sh — canonical argv-only host command boundary.\n'
                             ';;\n'
                             ';; Command strings are never interpreted implicitly. Development hosts provide\n'
                             ';; std.native.Process; browser/Wasm hosts fail through structured ex-info.\n'
                             ';; ============================================================================\n'
                             '(ns tool.sh)\n'
                             '\n'
                             '(def +option-keys+ #{:cwd :env :stdin})\n'
                             '\n'
                             '(defn command\n'
                             '  "Validates and returns one argv vector."\n'
                             '  [argv]\n'
                             '  (if (and (vector? argv)\n'
                             '           (not (empty? argv))\n'
                             '           (every? string? argv))\n'
                             '    argv\n'
                             '    (throw\n'
                             '     (ex-info "tool.sh requires a non-empty argv vector of strings"\n'
                             '              {:tool.sh/error :invalid-command\n'
                             '               :command argv}))))\n'
                             '\n'
                             '(defn- string-map?\n'
                             '  [value]\n'
                             '  (and (map? value)\n'
                             '       (every? (fn [key]\n'
                             '                 (and (string? key)\n'
                             '                      (string? (get value key))))\n'
                             '               (keys value))))\n'
                             '\n'
                             '(defn options\n'
                             '  "Validates the closed process option map.\n'
                             '\n'
                             '   Supported keys are :cwd, :env, and :stdin. :stdin accepts a string or\n'
                             '   Bytes; it is consumed only by execute/run/run! and never by spawn."\n'
                             '  [value]\n'
                             '  (let [value (or value {})]\n'
                             '    (if (not (map? value))\n'
                             '      (throw\n'
                             '       (ex-info "tool.sh options must be a map"\n'
                             '                {:tool.sh/error :invalid-options\n'
                             '                 :options value})))\n'
                             '    (let [unknown (vec (sort-by str\n'
                             '                               (filter\n'
                             '                                (fn [key]\n'
                             '                                  (not (has? +option-keys+ key)))\n'
                             '                                (keys value))))]\n'
                             '      (if (not (empty? unknown))\n'
                             '        (throw\n'
                             '         (ex-info "tool.sh received unknown process options"\n'
                             '                  {:tool.sh/error :unknown-options\n'
                             '                   :options value\n'
                             '                   :unknown unknown}))))\n'
                             '    (if (and (has? value :cwd)\n'
                             '             (not (string? (:cwd value))))\n'
                             '      (throw\n'
                             '       (ex-info "tool.sh :cwd must be a string"\n'
                             '                {:tool.sh/error :invalid-cwd\n'
                             '                 :options value})))\n'
                             '    (if (and (has? value :env)\n'
                             '             (not (string-map? (:env value))))\n'
                             '      (throw\n'
                             '       (ex-info "tool.sh :env must be a string-to-string map"\n'
                             '                {:tool.sh/error :invalid-env\n'
                             '                 :options value})))\n'
                             '    (if (and (has? value :stdin)\n'
                             '             (not (or (string? (:stdin value))\n'
                             '                      (Bytes/instance? (:stdin value)))))\n'
                             '      (throw\n'
                             '       (ex-info "tool.sh :stdin must be a string or Bytes"\n'
                             '                {:tool.sh/error :invalid-stdin\n'
                             '                 :options value})))\n'
                             '    value))\n'
                             '\n'
                             '(defn process-options\n'
                             '  "Selects the options understood by Process/spawn."\n'
                             '  [value]\n'
                             '  (select-keys (options value) [:cwd :env]))\n'
                             '\n'
                             '(defn- process-failure\n'
                             '  [operation argv error]\n'
                             '  (throw\n'
                             '   (ex-info "tool.sh process access is unavailable or failed"\n'
                             '            {:tool.sh/error :process-unavailable\n'
                             '             :tool.sh/operation operation\n'
                             '             :command argv\n'
                             '             :cause/message (ex-message error)\n'
                             '             :cause/data (ex-data error)}\n'
                             '            error)))\n'
                             '\n'
                             '(defn spawn\n'
                             '  "Starts one argv vector and returns the native process without closing stdin."\n'
                             '  ([argv]\n'
                             '   (spawn argv {}))\n'
                             '  ([argv value]\n'
                             '   (let [argv (command argv)\n'
                             '         selected (process-options value)]\n'
                             '     (try\n'
                             '       (if (empty? selected)\n'
                             '         (Process/spawn argv)\n'
                             '         (Process/spawn argv selected))\n'
                             '       (catch Throwable error\n'
                             '         (process-failure :spawn argv error))))))\n'
                             '\n'
                             '(defn stdout\n'
                             '  "Returns the process stdout Promise."\n'
                             '  [process]\n'
                             '  (Process/stdout process))\n'
                             '\n'
                             '(defn stderr\n'
                             '  "Returns the process stderr Promise."\n'
                             '  [process]\n'
                             '  (Process/stderr process))\n'
                             '\n'
                             '(defn wait\n'
                             '  "Returns the process exit-status Promise."\n'
                             '  [process]\n'
                             '  (Process/wait process))\n'
                             '\n'
                             '(defn write\n'
                             '  "Writes string or Bytes input to a process without closing stdin."\n'
                             '  [process value]\n'
                             '  (let [bytes (if (string? value)\n'
                             '                (str/encode-utf8 value)\n'
                             '                value)]\n'
                             '    (if (Bytes/instance? bytes)\n'
                             '      (Process/write process bytes)\n'
                             '      (throw\n'
                             '       (ex-info "tool.sh/write requires a string or Bytes"\n'
                             '                {:tool.sh/error :invalid-stdin\n'
                             '                 :stdin value})))))\n'
                             '\n'
                             '(defn close-input\n'
                             '  "Closes process stdin."\n'
                             '  [process]\n'
                             '  (Process/close-input process))\n'
                             '\n'
                             '(defn alive?\n'
                             '  "Returns true while the native process is alive."\n'
                             '  [process]\n'
                             '  (Process/alive? process))\n'
                             '\n'
                             '(defn kill\n'
                             '  "Terminates the native process."\n'
                             '  [process]\n'
                             '  (Process/kill process))\n'
                             '\n'
                             '(defn- result\n'
                             '  [argv values]\n'
                             '  {:command argv\n'
                             '   :exit (nth values 2)\n'
                             '   :stdout (str/decode-utf8 (nth values 0))\n'
                             '   :stderr (str/decode-utf8 (nth values 1))})\n'
                             '\n'
                             '(defn execute\n'
                             '  "Runs argv asynchronously and returns a Promise of the complete result.\n'
                             '\n'
                             '   Optional stdin is written and closed before stdout, stderr, and exit status\n'
                             '   are joined. Output is decoded as UTF-8 without trimming."\n'
                             '  ([argv]\n'
                             '   (execute argv {}))\n'
                             '  ([argv value]\n'
                             '   (let [argv (command argv)\n'
                             '         value (options value)]\n'
                             '     (try\n'
                             '       (let [process (spawn argv value)]\n'
                             '         (if (has? value :stdin)\n'
                             '           (write process (:stdin value)))\n'
                             '         (close-input process)\n'
                             '         (-> (promise/all [(stdout process)\n'
                             '                           (stderr process)\n'
                             '                           (wait process)])\n'
                             '             (promise/then (fn [values] (result argv values)))\n'
                             '             (promise/catch\n'
                             '              (fn [error]\n'
                             '                (process-failure :execute argv error)))))\n'
                             '       (catch Throwable error\n'
                             '         (if (= :process-unavailable (:tool.sh/error (ex-data error)))\n'
                             '           (throw error)\n'
                             '           (process-failure :execute argv error)))))))\n'
                             '\n'
                             '(defn run\n'
                             '  "Runs argv synchronously and returns the complete result."\n'
                             '  ([argv]\n'
                             '   (run argv {}))\n'
                             '  ([argv value]\n'
                             '   (deref (execute argv value))))\n'
                             '\n'
                             '(defn ensure!\n'
                             '  "Returns a successful command result or throws ex-info containing it."\n'
                             '  ([value]\n'
                             '   (ensure! "Command failed" value))\n'
                             '  ([message value]\n'
                             '   (if (= 0 (:exit value))\n'
                             '     value\n'
                             '     (throw (ex-info message value)))))\n'
                             '\n'
                             '(defn run!\n'
                             '  "Runs argv and throws a structured exception on non-zero exit."\n'
                             '  ([argv]\n'
                             '   (run! argv {}))\n'
                             '  ([argv value]\n'
                             '   (ensure! (run argv value))))\n',
 'core/lib/src/tool/sh/desktop.hal': ';; ============================================================================\n'
                                     ';; tool.sh.desktop — explicit desktop command builders and operations.\n'
                                     ';; ============================================================================\n'
                                     '(ns tool.sh.desktop\n'
                                     '  (:require [tool.sh :as sh]))\n'
                                     '\n'
                                     '(defn- unsupported\n'
                                     '  [operation platform]\n'
                                     '  (throw\n'
                                     '   (ex-info "Desktop operation is unsupported on this platform"\n'
                                     '            {:tool.sh/error :unsupported-platform\n'
                                     '             :tool.sh/operation operation\n'
                                     '             :platform platform})))\n'
                                     '\n'
                                     '(defn- platform\n'
                                     '  [value]\n'
                                     '  (or (:platform (or value {})) (OS/platform)))\n'
                                     '\n'
                                     '(defn- execution-options\n'
                                     '  [value]\n'
                                     '  (select-keys (or value {}) [:cwd :env :stdin]))\n'
                                     '\n'
                                     '(defn- execution-runner\n'
                                     '  [value]\n'
                                     '  (or (:runner (or value {})) sh/run!))\n'
                                     '\n'
                                     '(defn- execute\n'
                                     '  [argv value]\n'
                                     '  ((execution-runner value) argv (execution-options value)))\n'
                                     '\n'
                                     '(defn say-command\n'
                                     '  [platform phrase]\n'
                                     '  (cond\n'
                                     '    (= platform :macos) ["say" phrase]\n'
                                     '    (= platform :linux) ["spd-say" phrase]\n'
                                     '    :else (unsupported :say platform)))\n'
                                     '\n'
                                     '(defn notify-command\n'
                                     '  [platform title message]\n'
                                     '  (cond\n'
                                     '    (= platform :macos)\n'
                                     '    ["alerter" "-message" message "-title" title "-sound" "BELL"]\n'
                                     '\n'
                                     '    (= platform :linux)\n'
                                     '    ["notify-send" title message]\n'
                                     '\n'
                                     '    :else\n'
                                     '    (unsupported :notify platform)))\n'
                                     '\n'
                                     '(defn terminal-command\n'
                                     '  [platform command]\n'
                                     '  (if (not (vector? command))\n'
                                     '    (throw\n'
                                     '     (ex-info "Terminal commands must be argv vectors"\n'
                                     '              {:command command})))\n'
                                     '  (cond\n'
                                     '    (= platform :macos)\n'
                                     '    (into ["ttab"] command)\n'
                                     '\n'
                                     '    (= platform :linux)\n'
                                     '    (into ["x-terminal-emulator" "-e"] command)\n'
                                     '\n'
                                     '    :else\n'
                                     '    (unsupported :run-terminal platform)))\n'
                                     '\n'
                                     '(defn beep-command\n'
                                     '  [platform]\n'
                                     '  (cond\n'
                                     '    (= platform :macos)\n'
                                     '    ["afplay" "/System/Library/Sounds/Funk.aiff"]\n'
                                     '\n'
                                     '    (= platform :linux)\n'
                                     '    ["paplay" "/usr/share/sounds/freedesktop/stereo/bell.oga"]\n'
                                     '\n'
                                     '    :else\n'
                                     '    (unsupported :beep platform)))\n'
                                     '\n'
                                     '(defn clipboard-write-command\n'
                                     '  [platform]\n'
                                     '  (cond\n'
                                     '    (= platform :macos) ["pbcopy"]\n'
                                     '    (= platform :linux) ["xclip" "-selection" "clipboard"]\n'
                                     '    :else (unsupported :clip platform)))\n'
                                     '\n'
                                     '(defn clipboard-read-command\n'
                                     '  [platform]\n'
                                     '  (cond\n'
                                     '    (= platform :macos) ["pbpaste"]\n'
                                     '    (= platform :linux) ["xclip" "-selection" "clipboard" "-o"]\n'
                                     '    :else (unsupported :paste platform)))\n'
                                     '\n'
                                     '(defn say\n'
                                     '  "Speaks a phrase. A final map supplies :platform and :runner."\n'
                                     '  [& values]\n'
                                     '  (let [value (if (and (not (empty? values))\n'
                                     '                       (map? (last values)))\n'
                                     '                (last values)\n'
                                     '                {})\n'
                                     '        phrase (if (and (not (empty? values))\n'
                                     '                        (map? (last values)))\n'
                                     '                 (butlast values)\n'
                                     '                 values)]\n'
                                     '    (execute\n'
                                     '     (say-command (platform value)\n'
                                     '                  (str/join " " (map str phrase)))\n'
                                     '     value)))\n'
                                     '\n'
                                     '(defn notify\n'
                                     '  ([title message]\n'
                                     '   (notify title message {}))\n'
                                     '  ([title message value]\n'
                                     '   (execute\n'
                                     '    (notify-command (platform value) title message)\n'
                                     '    value)))\n'
                                     '\n'
                                     '(defn run-terminal\n'
                                     '  ([command]\n'
                                     '   (run-terminal command {}))\n'
                                     '  ([command value]\n'
                                     '   (execute\n'
                                     '    (terminal-command (platform value) command)\n'
                                     '    value)))\n'
                                     '\n'
                                     '(defn beep\n'
                                     '  ([] (beep {}))\n'
                                     '  ([value]\n'
                                     '   (execute (beep-command (platform value)) value)))\n'
                                     '\n'
                                     '(defn clip\n'
                                     '  "Writes a value to the desktop clipboard and returns its text."\n'
                                     '  ([value]\n'
                                     '   (clip value {}))\n'
                                     '  ([value options]\n'
                                     '   (let [text (if (string? value) value (pr-str value))\n'
                                     '         options (assoc (or options {}) :stdin text)]\n'
                                     '     (execute\n'
                                     '      (clipboard-write-command (platform options))\n'
                                     '      options)\n'
                                     '     text)))\n'
                                     '\n'
                                     '(defn clip:nil\n'
                                     '  "Writes a value to the desktop clipboard and returns nil."\n'
                                     '  ([value]\n'
                                     '   (clip:nil value {}))\n'
                                     '  ([value options]\n'
                                     '   (clip value options)\n'
                                     '   nil))\n'
                                     '\n'
                                     '(defn paste\n'
                                     '  "Reads exact text from the desktop clipboard."\n'
                                     '  ([] (paste {}))\n'
                                     '  ([value]\n'
                                     '   (:stdout\n'
                                     '    (execute (clipboard-read-command (platform value)) value))))\n',
 'core/lib/src/tool/sh/docker.hal': ';; ============================================================================\n'
                                    ';; tool.sh.docker — deterministic Docker argv builders and lifecycle wrappers.\n'
                                    ';; ============================================================================\n'
                                    '(ns tool.sh.docker\n'
                                    '  (:require [tool.sh :as sh]))\n'
                                    '\n'
                                    '(defn- execution-options\n'
                                    '  [value]\n'
                                    '  (select-keys (or value {}) [:cwd :env :stdin]))\n'
                                    '\n'
                                    '(defn- execution-runner\n'
                                    '  [value]\n'
                                    '  (or (:runner (or value {})) sh/run))\n'
                                    '\n'
                                    '(defn command-result\n'
                                    '  "Executes Docker argv through an optional final :runner."\n'
                                    '  ([argv]\n'
                                    '   (command-result argv {}))\n'
                                    '  ([argv value]\n'
                                    '   ((execution-runner value) argv (execution-options value))))\n'
                                    '\n'
                                    '(defn command!\n'
                                    '  "Executes Docker argv and rejects non-zero results."\n'
                                    '  ([argv]\n'
                                    '   (command! argv {}))\n'
                                    '  ([argv value]\n'
                                    '   (sh/ensure! "Docker command failed" (command-result argv value))))\n'
                                    '\n'
                                    '(defn- key-name\n'
                                    '  [key]\n'
                                    '  (cond (string? key) key\n'
                                    '        (keyword? key) (name key)\n'
                                    '        :else (str key)))\n'
                                    '\n'
                                    '(defn container-name\n'
                                    '  [container]\n'
                                    '  (str (key-name (or (:group container) "testing")) "_"\n'
                                    '       (key-name\n'
                                    '        (or (:id container)\n'
                                    '            (throw\n'
                                    '             (ex-info "Docker container id is required"\n'
                                    '                      {:container container}))))))\n'
                                    '\n'
                                    '(defn append-map\n'
                                    '  [arguments flag values]\n'
                                    '  (reduce (fn [out key]\n'
                                    '            (conj (conj out flag)\n'
                                    '                  (str (key-name key) "=" (get values key))))\n'
                                    '          arguments\n'
                                    '          (sort-by key-name (keys (or values {})))))\n'
                                    '\n'
                                    '(defn append-volumes\n'
                                    '  [arguments volumes]\n'
                                    '  (reduce (fn [out source]\n'
                                    '            (conj (conj out "-v")\n'
                                    '                  (str source ":" (get volumes source))))\n'
                                    '          arguments\n'
                                    '          (sort-by str (keys (or volumes {})))))\n'
                                    '\n'
                                    '(defn expose-value\n'
                                    '  [value]\n'
                                    '  (if (vector? value)\n'
                                    '    (str/join ":" (map str value))\n'
                                    '    (str value)))\n'
                                    '\n'
                                    '(defn append-expose\n'
                                    '  [arguments expose]\n'
                                    '  (reduce (fn [out value]\n'
                                    '            (conj (conj out "-p") (expose-value value)))\n'
                                    '          arguments\n'
                                    '          (or expose [])))\n'
                                    '\n'
                                    '(defn inspect-command\n'
                                    '  [container]\n'
                                    '  ["docker" "inspect" (container-name container)])\n'
                                    '\n'
                                    '(defn container-running?\n'
                                    '  ([container]\n'
                                    '   (container-running? container container))\n'
                                    '  ([container value]\n'
                                    '   (= 0 (:exit (command-result (inspect-command container) value)))))\n'
                                    '\n'
                                    '(defn container-ip-command\n'
                                    '  [name]\n'
                                    '  ["docker" "inspect" "-f"\n'
                                    '   "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}"\n'
                                    '   name])\n'
                                    '\n'
                                    '(defn container-ip\n'
                                    '  ([name]\n'
                                    '   (container-ip name {}))\n'
                                    '  ([name value]\n'
                                    '   (str/trim\n'
                                    '    (:stdout (command! (container-ip-command name) value)))))\n'
                                    '\n'
                                    '(defn published-port-command\n'
                                    '  [name port]\n'
                                    '  ["docker" "port" name (str port)])\n'
                                    '\n'
                                    '(defn published-port\n'
                                    '  ([name port]\n'
                                    '   (published-port name port {}))\n'
                                    '  ([name port value]\n'
                                    '   (let [output (str/trim\n'
                                    '                 (:stdout (command! (published-port-command name port) value)))\n'
                                    '         parts (str/split output ":")]\n'
                                    '     (read-string (last parts)))))\n'
                                    '\n'
                                    '(defn run-command\n'
                                    '  "Builds docker run argv without invoking a shell."\n'
                                    '  [container]\n'
                                    '  (let [name (container-name container)\n'
                                    '        published (if (empty? (:expose container))\n'
                                    '                    (map (fn [port] (str "127.0.0.1::" port))\n'
                                    '                         (:ports container))\n'
                                    '                    (:expose container))\n'
                                    '        arguments (append-volumes ["docker" "run"] (:volumes container))\n'
                                    '        arguments (append-map arguments "-l" (:labels container))\n'
                                    '        arguments (append-map arguments "-e" (:environment container))\n'
                                    '        arguments (append-expose arguments published)\n'
                                    '        arguments (if (false? (:detached container))\n'
                                    '                    arguments\n'
                                    '                    (conj arguments "-d"))\n'
                                    '        arguments (if (false? (:remove container))\n'
                                    '                    arguments\n'
                                    '                    (conj arguments "--rm"))\n'
                                    '        arguments (if (:no-host container)\n'
                                    '                    arguments\n'
                                    '                    (conj arguments\n'
                                    '                          "--add-host=host.docker.internal:host-gateway"))\n'
                                    '        arguments (into arguments (or (:flags container) []))\n'
                                    '        arguments (into arguments ["--name" name (:image container)])]\n'
                                    '    (into arguments (or (:cmd container) []))))\n'
                                    '\n'
                                    '(defn start-container\n'
                                    '  "Starts or attaches to a named Docker container."\n'
                                    '  ([container]\n'
                                    '   (start-container container container))\n'
                                    '  ([container value]\n'
                                    '   (let [name (container-name container)\n'
                                    '         reused? (container-running? container value)\n'
                                    '         published (if (empty? (:expose container))\n'
                                    '                     (map (fn [port] (str "127.0.0.1::" port))\n'
                                    '                          (:ports container))\n'
                                    '                     (:expose container))\n'
                                    '         started (if reused?\n'
                                    '                   nil\n'
                                    '                   (command! (run-command container) value))\n'
                                    '         internal-port (or (first (:ports container)) 5432)\n'
                                    '         exposed? (not (empty? published))\n'
                                    '         host (if exposed?\n'
                                    '                "127.0.0.1"\n'
                                    '                (container-ip name value))\n'
                                    '         port (if exposed?\n'
                                    '                (published-port name internal-port value)\n'
                                    '                internal-port)]\n'
                                    '     (merge (dissoc container :runner :cwd :env :stdin)\n'
                                    '            {:container-name name\n'
                                    '             :container-id (if started\n'
                                    '                             (str/trim (:stdout started))\n'
                                    '                             name)\n'
                                    '             :container-ip host\n'
                                    '             :host host\n'
                                    '             :port port\n'
                                    '             :owned? (not reused?)\n'
                                    '             :secondary reused?}))))\n'
                                    '\n'
                                    '(defn kill-command\n'
                                    '  [container]\n'
                                    '  ["docker" "kill" (:container-name container)])\n'
                                    '\n'
                                    '(defn stop-container\n'
                                    '  ([container]\n'
                                    '   (stop-container container container))\n'
                                    '  ([container value]\n'
                                    '   (if (and container\n'
                                    '            (:owned? container)\n'
                                    '            (not (:secondary container))\n'
                                    '            (not (:permanent container)))\n'
                                    '     (command! (kill-command container) value))\n'
                                    '   nil))\n'
                                    '\n'
                                    '(defn start-runtime\n'
                                    '  "Attaches a configured container to a runtime. Core HAL intentionally omits Ryuk."\n'
                                    '  ([runtime container]\n'
                                    '   (start-runtime runtime container container))\n'
                                    '  ([runtime container value]\n'
                                    '   (let [id (or (:id container)\n'
                                    '                (str (name (or (:runtime runtime) :runtime)) "-"\n'
                                    '                     (name (or (:id runtime) :default))))\n'
                                    '         labels (merge {"rt/lang" (name (:lang runtime))\n'
                                    '                        "rt/module" (str (or (:module runtime) ""))}\n'
                                    '                       (:labels container))\n'
                                    '         started (start-container\n'
                                    '                  (assoc container :id id :labels labels)\n'
                                    '                  value)]\n'
                                    '     (assoc runtime\n'
                                    '            :host (:host started)\n'
                                    '            :port (:port started)\n'
                                    '            :container started))))\n'
                                    '\n'
                                    '(defn stop-runtime\n'
                                    '  ([runtime container]\n'
                                    '   (stop-runtime runtime container container))\n'
                                    '  ([runtime container value]\n'
                                    '   (stop-container container value)\n'
                                    '   (assoc runtime :container nil)))\n',
 'core/lib/src/tool/sh/git.hal': ';; ============================================================================\n'
                                 ';; tool.sh.git — Foundation-compatible Git operations over canonical tool.sh.\n'
                                 ';; ============================================================================\n'
                                 '(ns tool.sh.git\n'
                                 '  (:require [tool.sh :as sh]))\n'
                                 '\n'
                                 '(defn- arguments\n'
                                 '  [value]\n'
                                 '  (if (vector? value) value [value]))\n'
                                 '\n'
                                 '(defn- execution-options\n'
                                 '  [value]\n'
                                 '  (let [value (or value {})\n'
                                 '        cwd (or (:cwd value)\n'
                                 '                (if (:root value) (str (:root value)) nil))\n'
                                 '        selected (select-keys value [:env :stdin])]\n'
                                 '    (if cwd (assoc selected :cwd cwd) selected)))\n'
                                 '\n'
                                 '(defn- execution-runner\n'
                                 '  [value]\n'
                                 '  (or (:runner (or value {})) sh/run))\n'
                                 '\n'
                                 '(defn command\n'
                                 '  "Builds Git argv. Legacy :root is normalized to process :cwd."\n'
                                 '  ([args]\n'
                                 '   (command args {}))\n'
                                 '  ([args options]\n'
                                 '   (vec (concat ["git"] (arguments args)))))\n'
                                 '\n'
                                 '(defn git-result\n'
                                 '  "Runs Git and returns the standard tool.sh result keys."\n'
                                 '  ([args]\n'
                                 '   (git-result args {}))\n'
                                 '  ([args options]\n'
                                 '   (let [result ((execution-runner options)\n'
                                 '                 (command args options)\n'
                                 '                 (execution-options options))]\n'
                                 '     (assoc result :stdout (str/trim (:stdout result))))))\n'
                                 '\n'
                                 '(defn git-run\n'
                                 '  "Runs Git and optionally rejects non-zero results."\n'
                                 '  ([args]\n'
                                 '   (git-run args {}))\n'
                                 '  ([args options]\n'
                                 '   (let [options (or options {})\n'
                                 '         ignore-errors (if (has? options :ignore-errors)\n'
                                 '                         (:ignore-errors options)\n'
                                 '                         true)\n'
                                 '         result (git-result args options)]\n'
                                 '     (if (and (not= 0 (:exit result))\n'
                                 '              (not ignore-errors))\n'
                                 '       (throw (ex-info "Git command failed" result))\n'
                                 '       result))))\n'
                                 '\n'
                                 '(defn git\n'
                                 '  ([args]\n'
                                 '   (git-run args {}))\n'
                                 '  ([args options]\n'
                                 '   (git-run args options)))\n'
                                 '\n'
                                 '(defn init\n'
                                 '  ([] (init {}))\n'
                                 '  ([options] (git ["init"] options)))\n'
                                 '\n'
                                 '(defn clone\n'
                                 '  ([url] (clone url {}))\n'
                                 '  ([url options]\n'
                                 '   (let [args (if (:dir options)\n'
                                 '                ["clone" url (str (:dir options))]\n'
                                 '                ["clone" url])]\n'
                                 '     (git args (dissoc options :dir)))))\n'
                                 '\n'
                                 '(defn- staged-code?\n'
                                 '  [value]\n'
                                 '  (has? #{"M" "A" "D" "R" "C"} value))\n'
                                 '\n'
                                 '(defn status\n'
                                 '  ([] (status {}))\n'
                                 '  ([options]\n'
                                 '   (let [lines (str/split-lines\n'
                                 '                (:stdout (git ["status" "--porcelain" "-b"] options)))]\n'
                                 '     (reduce\n'
                                 '      (fn [output line]\n'
                                 '        (let [code (str/slice line 0 2)\n'
                                 '              value (str/trim (str/slice line 3))]\n'
                                 '          (cond\n'
                                 '            (= code "##")\n'
                                 '            (let [branch-info value\n'
                                 '                  branch (if (str/starts-with?\n'
                                 '                              branch-info\n'
                                 '                              "No commits yet on ")\n'
                                 '                           (str/slice\n'
                                 '                            branch-info\n'
                                 '                            (str/length "No commits yet on "))\n'
                                 '                           (first (str/split branch-info "...")))]\n'
                                 '              (assoc output :branch branch))\n'
                                 '\n'
                                 '            (= code "??")\n'
                                 '            (update output :untracked conj value)\n'
                                 '\n'
                                 '            :else\n'
                                 '            (let [x (str/char-at code 0)\n'
                                 '                  y (str/char-at code 1)\n'
                                 '                  output (if (staged-code? x)\n'
                                 '                           (update output :staged conj value)\n'
                                 '                           output)]\n'
                                 '              (if (staged-code? y)\n'
                                 '                (update output :changed conj value)\n'
                                 '                output)))))\n'
                                 '      {:branch "HEAD" :changed [] :staged [] :untracked []}\n'
                                 '      lines))))\n'
                                 '\n'
                                 '(defn add\n'
                                 '  ([value]\n'
                                 '   (if (map? value)\n'
                                 '     (add nil value)\n'
                                 '     (add value {})))\n'
                                 '  ([value options]\n'
                                 '   (let [args (cond (:all options) ["add" "--all"]\n'
                                 '                    (vector? value) (into ["add"] value)\n'
                                 '                    (string? value) ["add" value]\n'
                                 '                    :else ["add"])]\n'
                                 '     (git args options))))\n'
                                 '\n'
                                 '(defn commit\n'
                                 '  ([value]\n'
                                 '   (if (map? value)\n'
                                 '     (commit nil value)\n'
                                 '     (commit value {})))\n'
                                 '  ([message options]\n'
                                 '   (let [message (or message (:message options))\n'
                                 '         args (if message ["commit" "-m" message] ["commit"])\n'
                                 '         args (if (:amend options) (conj args "--amend") args)\n'
                                 '         args (if (:no-verify options) (conj args "--no-verify") args)\n'
                                 '         args (if (:allow-empty options) (conj args "--allow-empty") args)]\n'
                                 '     (git args options))))\n'
                                 '\n'
                                 '(defn log\n'
                                 '  ([] (log {}))\n'
                                 '  ([options]\n'
                                 '   (let [args ["log" "--pretty=format:%H|%an|%ae|%at|%s"]\n'
                                 '         args (if (:n options) (conj args (str "-" (:n options))) args)\n'
                                 '         lines (str/split-lines (:stdout (git args options)))]\n'
                                 '     (mapv (fn [line]\n'
                                 '             (let [parts (str/split line "|")]\n'
                                 '               (if (>= (count parts) 5)\n'
                                 '                 {:hash (nth parts 0)\n'
                                 '                  :author (nth parts 1)\n'
                                 '                  :email (nth parts 2)\n'
                                 '                  :time (try\n'
                                 '                          (read-string (nth parts 3))\n'
                                 '                          (catch Throwable error 0))\n'
                                 '                  :subject (str/join "|" (drop 4 parts))}\n'
                                 '                 {:raw line})))\n'
                                 '           lines))))\n'
                                 '\n'
                                 '(defn branch\n'
                                 '  ([] (branch nil {}))\n'
                                 '  ([value]\n'
                                 '   (if (map? value)\n'
                                 '     (branch nil value)\n'
                                 '     (branch value {})))\n'
                                 '  ([name options]\n'
                                 '   (cond\n'
                                 '     (nil? name)\n'
                                 '     (mapv (fn [line]\n'
                                 '             (str/trim (str/replace line "*" "")))\n'
                                 '           (str/split-lines (:stdout (git ["branch"] options))))\n'
                                 '\n'
                                 '     (:delete options)\n'
                                 '     (git ["branch" "-D" name] options)\n'
                                 '\n'
                                 '     :else\n'
                                 '     (git ["branch" name] options))))\n'
                                 '\n'
                                 '(defn checkout\n'
                                 '  ([name] (checkout name {}))\n'
                                 '  ([name options]\n'
                                 '   (let [args ["checkout"]\n'
                                 '         args (if (:create options) (conj args "-b") args)\n'
                                 '         args (if (:force options) (conj args "-f") args)]\n'
                                 '     (git (conj args name) options))))\n'
                                 '\n'
                                 '(defn merge-branch\n'
                                 '  ([name] (merge-branch name {}))\n'
                                 '  ([name options] (git ["merge" name] options)))\n'
                                 '\n'
                                 '(defn pull\n'
                                 '  ([] (pull {}))\n'
                                 '  ([options]\n'
                                 '   (let [args ["pull"]\n'
                                 '         args (if (:rebase options) (conj args "--rebase") args)\n'
                                 '         args (if (:force options) (conj args "--force") args)]\n'
                                 '     (git args options))))\n'
                                 '\n'
                                 '(defn push\n'
                                 '  ([] (push {}))\n'
                                 '  ([options]\n'
                                 '   (let [args ["push"]\n'
                                 '         args (if (:force options) (conj args "--force") args)\n'
                                 '         args (if (:upstream options)\n'
                                 '                (into args ["-u" (str (:upstream options))])\n'
                                 '                args)]\n'
                                 '     (git args options))))\n'
                                 '\n'
                                 '(defn fetch\n'
                                 '  ([] (fetch {}))\n'
                                 '  ([options]\n'
                                 '   (let [args ["fetch"]\n'
                                 '         args (if (:all options) (conj args "--all") args)\n'
                                 '         args (if (:prune options) (conj args "--prune") args)]\n'
                                 '     (git args options))))\n'
                                 '\n'
                                 '(defn reset\n'
                                 '  ([commit] (reset commit {}))\n'
                                 '  ([commit options]\n'
                                 '   (let [mode (cond (= (:mode options) :soft) "--soft"\n'
                                 '                    (= (:mode options) :hard) "--hard"\n'
                                 '                    (= (:mode options) :mixed) "--mixed"\n'
                                 '                    :else nil)\n'
                                 '         args (if mode\n'
                                 '                ["reset" mode commit]\n'
                                 '                ["reset" commit])]\n'
                                 '     (git args options))))\n'
                                 '\n'
                                 '(defn rev-parse\n'
                                 '  ([reference] (rev-parse reference {}))\n'
                                 '  ([reference options]\n'
                                 '   (str/trim (:stdout (git ["rev-parse" reference] options)))))\n'
                                 '\n'
                                 '(defn squash\n'
                                 '  [options]\n'
                                 '  (let [target (or (:base options)\n'
                                 '                   (if (:n options)\n'
                                 '                     (str "HEAD~" (:n options))\n'
                                 '                     nil))]\n'
                                 '    (if (nil? target)\n'
                                 '      (throw\n'
                                 '       (ex-info "Git squash requires :n or :base"\n'
                                 '                {:options options})))\n'
                                 '    (reset target\n'
                                 '           (assoc options :mode :soft :ignore-errors false))\n'
                                 '    (commit (or (:message options) "squashed commit")\n'
                                 '            (assoc options :ignore-errors false))))\n'
                                 '\n'
                                 '(defn rebase\n'
                                 '  ([upstream] (rebase upstream {}))\n'
                                 '  ([upstream options]\n'
                                 '   (let [result (git-run ["rebase" upstream]\n'
                                 '                         (assoc options :ignore-errors true))]\n'
                                 '     (if (= 0 (:exit result))\n'
                                 '       result\n'
                                 '       (do\n'
                                 '         (git-run ["rebase" "--abort"] options)\n'
                                 '         (throw\n'
                                 '          (ex-info "Git rebase failed and was aborted" result)))))))\n'
                                 '\n'
                                 '(defn submodule\n'
                                 '  ([action] (submodule action {}))\n'
                                 '  ([action options]\n'
                                 '   (let [args (cond (= action :update)\n'
                                 '                    ["submodule" "update" "--init" "--recursive"]\n'
                                 '                    (= action :init)\n'
                                 '                    ["submodule" "init"]\n'
                                 '                    (= action :sync)\n'
                                 '                    ["submodule" "sync"]\n'
                                 '                    (vector? action)\n'
                                 '                    (into ["submodule"] action)\n'
                                 '                    :else\n'
                                 '                    ["submodule" (name action)])]\n'
                                 '     (git args options))))\n',
 'core/lib/src/tool/sh/net.hal': ';; ============================================================================\n'
                                ';; tool.sh.net — explicit network command builders over tool.sh.\n'
                                ';; ============================================================================\n'
                                '(ns tool.sh.net\n'
                                '  (:require [tool.sh :as sh]))\n'
                                '\n'
                                '(defn- execution-options\n'
                                '  [value]\n'
                                '  (select-keys (or value {}) [:cwd :env :stdin]))\n'
                                '\n'
                                '(defn- execution-runner\n'
                                '  [value]\n'
                                '  (or (:runner (or value {})) sh/run!))\n'
                                '\n'
                                '(defn- execute\n'
                                '  [argv value]\n'
                                '  ((execution-runner value) argv (execution-options value)))\n'
                                '\n'
                                '(defn wget-command\n'
                                '  "Builds one wget argv vector."\n'
                                '  [root-url file output arguments]\n'
                                '  (let [command ["wget" (str root-url file)]\n'
                                '        command (if output\n'
                                '                  (into command ["-O" output])\n'
                                '                  command)]\n'
                                '    (into command (or arguments []))))\n'
                                '\n'
                                '(defn- descriptor\n'
                                '  [value]\n'
                                '  (cond\n'
                                '    (string? value)\n'
                                '    [value nil]\n'
                                '\n'
                                '    (and (vector? value)\n'
                                '         (= 2 (count value))\n'
                                '         (string? (nth value 0))\n'
                                '         (or (nil? (nth value 1))\n'
                                '             (string? (nth value 1))))\n'
                                '    value\n'
                                '\n'
                                '    :else\n'
                                '    (throw\n'
                                '     (ex-info "wget file descriptors must be strings or [source output]"\n'
                                '              {:descriptor value}))))\n'
                                '\n'
                                '(defn download\n'
                                '  ([url]\n'
                                '   (download url {}))\n'
                                '  ([url value]\n'
                                '   (execute\n'
                                '    (wget-command "" url (:output value) (:arguments value))\n'
                                '    value)))\n'
                                '\n'
                                '(defn wget-bulk\n'
                                '  "Downloads Foundation-style file descriptors with explicit cwd and argv."\n'
                                '  ([root-url root-path files]\n'
                                '   (wget-bulk root-url root-path files [] {}))\n'
                                '  ([root-url root-path files arguments]\n'
                                '   (wget-bulk root-url root-path files arguments {}))\n'
                                '  ([root-url root-path files arguments value]\n'
                                '   (let [value (assoc (or value {})\n'
                                '                      :cwd (or (:cwd value)\n'
                                '                               (str "resources/" root-path)))]\n'
                                '     (mapv\n'
                                '      (fn [file]\n'
                                '        (let [[source output] (descriptor file)]\n'
                                '          (execute\n'
                                '           (wget-command root-url source output arguments)\n'
                                '           value)))\n'
                                '      files))))\n',
 'core/lib/src/tool/sh/tmux.hal': ';; ============================================================================\n'
                                 ';; tool.sh.tmux — deterministic tmux argv builders and injectable execution.\n'
                                 ';; ============================================================================\n'
                                 '(ns tool.sh.tmux\n'
                                 '  (:require [tool.sh :as sh]))\n'
                                 '\n'
                                 '(defn- env-name\n'
                                 '  [key]\n'
                                 '  (str/upper (if (keyword? key) (name key) (str key))))\n'
                                 '\n'
                                 '(defn- execution-runner\n'
                                 '  [value]\n'
                                 '  (or (:runner (or value {})) sh/run))\n'
                                 '\n'
                                 '(defn- command-result\n'
                                 '  [argv value]\n'
                                 '  ((execution-runner value) argv {}))\n'
                                 '\n'
                                 '(defn- command!\n'
                                 '  [argv value]\n'
                                 '  (sh/ensure! "tmux command failed" (command-result argv value)))\n'
                                 '\n'
                                 '(defn with-args\n'
                                 '  "Adds tmux start-directory and environment arguments deterministically."\n'
                                 '  [command value]\n'
                                 '  (let [value (or value {})\n'
                                 '        root (or (:cwd value) (:root value))\n'
                                 '        command (if root\n'
                                 '                  (into command ["-c" (str root)])\n'
                                 '                  command)]\n'
                                 '    (reduce\n'
                                 '     (fn [out key]\n'
                                 '       (into out\n'
                                 '             ["-e"\n'
                                 '              (str (env-name key) "=" (get (:env value) key))]))\n'
                                 '     command\n'
                                 '     (sort-by env-name (keys (or (:env value) {}))))))\n'
                                 '\n'
                                 '(def tmux-with-args with-args)\n'
                                 '\n'
                                 '(defn- append-direct-command\n'
                                 '  [argv command]\n'
                                 '  (cond\n'
                                 '    (nil? command) argv\n'
                                 '    (vector? command) (into argv (into ["--"] command))\n'
                                 '    (string? command) (conj argv command)\n'
                                 '    :else\n'
                                 '    (throw\n'
                                 '     (ex-info "tmux command must be a string or argv vector"\n'
                                 '              {:command command}))))\n'
                                 '\n'
                                 '(defn kill-server-command\n'
                                 '  []\n'
                                 '  ["tmux" "kill-server"])\n'
                                 '\n'
                                 '(defn list-sessions-command\n'
                                 '  []\n'
                                 '  ["tmux" "list-sessions" "-F" "#{session_name}"])\n'
                                 '\n'
                                 '(defn has-session-command\n'
                                 '  [session]\n'
                                 '  ["tmux" "has-session" "-t" (str session)])\n'
                                 '\n'
                                 '(defn new-session-command\n'
                                 '  ([session]\n'
                                 '   (new-session-command session {}))\n'
                                 '  ([session value]\n'
                                 '   (append-direct-command\n'
                                 '    (with-args\n'
                                 '     ["tmux" "new-session" "-d" "-s" (str session) "-n" "CMD"]\n'
                                 '     value)\n'
                                 '    (:command value))))\n'
                                 '\n'
                                 '(defn kill-session-command\n'
                                 '  [session]\n'
                                 '  ["tmux" "kill-session" "-t" (str session)])\n'
                                 '\n'
                                 '(defn list-windows-command\n'
                                 '  [session]\n'
                                 '  ["tmux" "list-windows" "-t" (str session)\n'
                                 '   "-F" "#{window_name}"])\n'
                                 '\n'
                                 '(defn send-keys-command\n'
                                 '  "Builds command dispatch without collapsing argv vectors into shell text.\n'
                                 '\n'
                                 '   String commands retain the historical send-keys behaviour. Vector commands\n'
                                 '   use respawn-pane, whose multiple command arguments are executed directly."\n'
                                 '  [session key command]\n'
                                 '  (let [target (str session ":" key)]\n'
                                 '    (cond\n'
                                 '      (vector? command)\n'
                                 '      (into ["tmux" "respawn-pane" "-k" "-t" target "--"]\n'
                                 '            command)\n'
                                 '\n'
                                 '      (string? command)\n'
                                 '      ["tmux" "send-keys" "-t" target command "C-m"]\n'
                                 '\n'
                                 '      :else\n'
                                 '      (throw\n'
                                 '       (ex-info "tmux command must be a string or argv vector"\n'
                                 '                {:command command})))))\n'
                                 '\n'
                                 '(defn new-window-command\n'
                                 '  ([session key]\n'
                                 '   (new-window-command session key {}))\n'
                                 '  ([session key value]\n'
                                 '   (append-direct-command\n'
                                 '    (with-args\n'
                                 '     ["tmux" "new-window" "-t" (str session) "-n" (str key)]\n'
                                 '     value)\n'
                                 '    (:command value))))\n'
                                 '\n'
                                 '(defn kill-window-command\n'
                                 '  [session key]\n'
                                 '  ["tmux" "kill-window" "-t" (str session ":" key)])\n'
                                 '\n'
                                 '(defn kill-server\n'
                                 '  ([] (kill-server {}))\n'
                                 '  ([value] (command! (kill-server-command) value)))\n'
                                 '\n'
                                 '(defn list-sessions\n'
                                 '  ([] (list-sessions {}))\n'
                                 '  ([value]\n'
                                 '   (str/split-lines\n'
                                 '    (:stdout (command! (list-sessions-command) value)))))\n'
                                 '\n'
                                 '(defn has-session?\n'
                                 '  ([session] (has-session? session {}))\n'
                                 '  ([session value]\n'
                                 '   (= 0 (:exit\n'
                                 '         (command-result (has-session-command session) value)))))\n'
                                 '\n'
                                 '(defn new-session\n'
                                 '  ([session] (new-session session {}))\n'
                                 '  ([session value]\n'
                                 '   (command! (new-session-command session value) value)))\n'
                                 '\n'
                                 '(defn kill-session\n'
                                 '  ([session] (kill-session session {}))\n'
                                 '  ([session value]\n'
                                 '   (command! (kill-session-command session) value)))\n'
                                 '\n'
                                 '(defn list-windows\n'
                                 '  ([session] (list-windows session {}))\n'
                                 '  ([session value]\n'
                                 '   (str/split-lines\n'
                                 '    (:stdout (command! (list-windows-command session) value)))))\n'
                                 '\n'
                                 '(defn has-window?\n'
                                 '  ([session key] (has-window? session key {}))\n'
                                 '  ([session key value]\n'
                                 '   (has? (set (list-windows session value)) (str key))))\n'
                                 '\n'
                                 '(defn run-command\n'
                                 '  ([session key command]\n'
                                 '   (run-command session key command {}))\n'
                                 '  ([session key command value]\n'
                                 '   (command! (send-keys-command session key command) value)))\n'
                                 '\n'
                                 '(defn new-window\n'
                                 '  ([session key] (new-window session key {}))\n'
                                 '  ([session key value]\n'
                                 '   (if (has-window? session key value)\n'
                                 '     :window-already-present\n'
                                 '     (command! (new-window-command session key value) value))))\n'
                                 '\n'
                                 '(defn kill-window\n'
                                 '  ([session key] (kill-window session key {}))\n'
                                 '  ([session key value]\n'
                                 '   (command! (kill-window-command session key) value)))\n',
 'core/lib/test-lang/lang/runtime/basic/type_oneshot_test.hal': '(ns lang.runtime.basic.type-oneshot-test\n'
                                                                  '  (:require [lang.runtime.basic.type-oneshot :as oneshot]))\n'
                                                                  '\n'
                                                                  '(def runtime\n'
                                                                  '  {:id :shell.oneshot\n'
                                                                  '   :lang :shell\n'
                                                                  '   :runtime :oneshot\n'
                                                                  '   :program :sh\n'
                                                                  '   :exec ["sh" "-c"]\n'
                                                                  '   :process {}})\n'
                                                                  '\n'
                                                                  '(def results\n'
                                                                  '  [(test-check\n'
                                                                  '    "oneshot-result retains its existing vector projection"\n'
                                                                  '    (oneshot/oneshot-result\n'
                                                                  '     runtime\n'
                                                                  '     ["sh" "-c" "printf ok"]\n'
                                                                  '     [(str/encode-utf8 "ok")\n'
                                                                  '      (str/encode-utf8 "")\n'
                                                                  '      0])\n'
                                                                  '    {:phase :eval\n'
                                                                  '     :runtime/id :shell.oneshot\n'
                                                                  '     :runtime/language :shell\n'
                                                                  '     :lang :shell\n'
                                                                  '     :runtime :oneshot\n'
                                                                  '     :program :sh\n'
                                                                  '     :command ["sh" "-c" "printf ok"]\n'
                                                                  '     :exit 0\n'
                                                                  '     :stdout "ok"\n'
                                                                  '     :stderr ""})\n'
                                                                  '\n'
                                                                  '   (test-check\n'
                                                                  '    "execute-oneshot delegates one-shot execution to tool.sh"\n'
                                                                  '    (oneshot/execute-oneshot runtime "printf \'value\\\\n\'")\n'
                                                                  '    {:phase :eval\n'
                                                                  '     :runtime/id :shell.oneshot\n'
                                                                  '     :runtime/language :shell\n'
                                                                  '     :lang :shell\n'
                                                                  '     :runtime :oneshot\n'
                                                                  '     :program :sh\n'
                                                                  '     :command ["sh" "-c" "printf \'value\\\\n\'"]\n'
                                                                  '     :exit 0\n'
                                                                  '     :stdout "value\\n"\n'
                                                                  '     :stderr ""})\n'
                                                                  '\n'
                                                                  '   (test-check\n'
                                                                  '    "raw-eval-oneshot retains its explicit semantic trimming"\n'
                                                                  '    (oneshot/raw-eval-oneshot runtime "printf \'value\\\\n\'")\n'
                                                                  '    "value")])\n'
                                                                  '\n'
                                                                  'results\n',
 'core/lib/test-lang/lang/runtime/basic/type_verify_test.hal': '(ns lang.runtime.basic.type-verify-test\n'
                                                                 '  (:require [lang.runtime.basic.type-verify :as verify]))\n'
                                                                 '\n'
                                                                 '(def runtime\n'
                                                                 '  {:lang :lua\n'
                                                                 '   :runtime :verify\n'
                                                                 '   :program :lua\n'
                                                                 '   :exec ["lua" "-e" "checker"]\n'
                                                                 '   :grammar-coordinate [:lua :default]\n'
                                                                 '   :grammar-version 1})\n'
                                                                 '\n'
                                                                 '(def results\n'
                                                                 '  [(test-check\n'
                                                                 '    "checker-result retains the existing pure vector contract"\n'
                                                                 '    (verify/checker-result\n'
                                                                 '     runtime\n'
                                                                 '     [(str/encode-utf8 "out")\n'
                                                                 '      (str/encode-utf8 "err")\n'
                                                                 '      3])\n'
                                                                 '    {:phase :verify\n'
                                                                 '     :lang :lua\n'
                                                                 '     :runtime :verify\n'
                                                                 '     :program :lua\n'
                                                                 '     :command ["lua" "-e" "checker"]\n'
                                                                 '     :exit 3\n'
                                                                 '     :stdout "out"\n'
                                                                 '     :stderr "err"})\n'
                                                                 '\n'
                                                                 '   (test-check\n'
                                                                 '    "checker-result accepts canonical tool.sh result maps"\n'
                                                                 '    (verify/checker-result\n'
                                                                 '     runtime\n'
                                                                 '     {:command ["lua" "-e" "checker"]\n'
                                                                 '      :exit 0\n'
                                                                 '      :stdout ""\n'
                                                                 '      :stderr ""})\n'
                                                                 '    {:phase :verify\n'
                                                                 '     :lang :lua\n'
                                                                 '     :runtime :verify\n'
                                                                 '     :program :lua\n'
                                                                 '     :command ["lua" "-e" "checker"]\n'
                                                                 '     :exit 0\n'
                                                                 '     :stdout ""\n'
                                                                 '     :stderr ""})\n'
                                                                 '\n'
                                                                 '   (test-check\n'
                                                                 '    "verify-result returns verified source unchanged"\n'
                                                                 '    (verify/verify-result runtime "return 42" {:exit 0})\n'
                                                                 '    "return 42")\n'
                                                                 '\n'
                                                                 '   (test-check\n'
                                                                 '    "verify-result preserves runtime and grammar provenance"\n'
                                                                 '    (try\n'
                                                                 '      (verify/verify-result\n'
                                                                 '       runtime\n'
                                                                 '       "function {"\n'
                                                                 '       {:phase :verify\n'
                                                                 '        :lang :lua\n'
                                                                 '        :runtime :verify\n'
                                                                 '        :program :lua\n'
                                                                 '        :command ["lua" "-e" "checker"]\n'
                                                                 '        :exit 1\n'
                                                                 '        :stdout ""\n'
                                                                 '        :stderr "syntax error"})\n'
                                                                 '      nil\n'
                                                                 '      (catch Throwable error\n'
                                                                 '        (let [data (ex-data error)]\n'
                                                                 '          [(ex-message error)\n'
                                                                 '           (get data :phase)\n'
                                                                 '           (get data :lang)\n'
                                                                 '           (get data :runtime)\n'
                                                                 '           (get data :program)\n'
                                                                 '           (get data :grammar-coordinate)\n'
                                                                 '           (get data :grammar-version)\n'
                                                                 '           (get data :generated-fragment)\n'
                                                                 '           (get data :stderr)])))\n'
                                                                 '    ["Target source verification failed"\n'
                                                                 '     :verify\n'
                                                                 '     :lua\n'
                                                                 '     :verify\n'
                                                                 '     :lua\n'
                                                                 '     [:lua :default]\n'
                                                                 '     1\n'
                                                                 '     "function {"\n'
                                                                 '     "syntax error"])\n'
                                                                 '\n'
                                                                 '   (test-check\n'
                                                                 '    "verify-execute writes exact source through canonical stdin"\n'
                                                                 '    (verify/verify-execute\n'
                                                                 '     {:lang :shell\n'
                                                                 '      :runtime :verify\n'
                                                                 '      :program :cat\n'
                                                                 '      :exec ["cat"]}\n'
                                                                 '     "source\\n")\n'
                                                                 '    {:phase :verify\n'
                                                                 '     :lang :shell\n'
                                                                 '     :runtime :verify\n'
                                                                 '     :program :cat\n'
                                                                 '     :command ["cat"]\n'
                                                                 '     :exit 0\n'
                                                                 '     :stdout "source\\n"\n'
                                                                 '     :stderr ""})])\n'
                                                                 '\n'
                                                                 'results\n',
 'core/lib/test/tool/sh/desktop_test.hal': '(ns tool.sh.desktop-test\n'
                                            '  (:require [tool.sh.desktop :as desktop]))\n'
                                            '\n'
                                            '(def calls (atom []))\n'
                                            '\n'
                                            '(defn fake-runner\n'
                                            '  [argv options]\n'
                                            '  (swap! calls conj [argv options])\n'
                                            '  {:command argv\n'
                                            '   :stdout (if (= argv ["pbpaste"]) "clipboard\\n" "")\n'
                                            '   :stderr ""\n'
                                            '   :exit 0})\n'
                                            '\n'
                                            '(def results\n'
                                            '  [(test-check\n'
                                            '    "desktop builders preserve supported macOS and Linux commands"\n'
                                            '    [(desktop/say-command :macos "hello")\n'
                                            '     (desktop/say-command :linux "hello")\n'
                                            '     (desktop/notify-command :linux "Title" "Message")\n'
                                            '     (desktop/terminal-command :linux ["printf" "%s" "a b"])\n'
                                            '     (desktop/beep-command :macos)\n'
                                            '     (desktop/clipboard-write-command :linux)\n'
                                            '     (desktop/clipboard-read-command :macos)]\n'
                                            '    [["say" "hello"]\n'
                                            '     ["spd-say" "hello"]\n'
                                            '     ["notify-send" "Title" "Message"]\n'
                                            '     ["x-terminal-emulator" "-e" "printf" "%s" "a b"]\n'
                                            '     ["afplay" "/System/Library/Sounds/Funk.aiff"]\n'
                                            '     ["xclip" "-selection" "clipboard"]\n'
                                            '     ["pbpaste"]])\n'
                                            '\n'
                                            '   (test-check\n'
                                            '    "unsupported platforms fail with structured ex-info"\n'
                                            '    (try\n'
                                            '      (desktop/notify-command :unknown "Title" "Message")\n'
                                            '      nil\n'
                                            '      (catch Throwable error\n'
                                            '        (select-keys (ex-data error)\n'
                                            '                     [:tool.sh/error :tool.sh/operation :platform])))\n'
                                            '    {:tool.sh/error :unsupported-platform\n'
                                            '     :tool.sh/operation :notify\n'
                                            '     :platform :unknown})\n'
                                            '\n'
                                            '   (test-check\n'
                                            '    "clipboard operations use stdin and preserve exact returned text"\n'
                                            '    (do\n'
                                            '      (reset! calls [])\n'
                                            '      [(desktop/clip "copied\\n"\n'
                                            '                     {:platform :macos\n'
                                            '                      :runner fake-runner})\n'
                                            '       (desktop/clip:nil "discard"\n'
                                            '                         {:platform :macos\n'
                                            '                          :runner fake-runner})\n'
                                            '       (desktop/paste {:platform :macos\n'
                                            '                       :runner fake-runner})\n'
                                            '       (deref calls)])\n'
                                            '    ["copied\\n"\n'
                                            '     nil\n'
                                            '     "clipboard\\n"\n'
                                            '     [[["pbcopy"] {:stdin "copied\\n"}]\n'
                                            '      [["pbcopy"] {:stdin "discard"}]\n'
                                            '      [["pbpaste"] {}]]])\n'
                                            '\n'
                                            '   (test-check\n'
                                            '    "say accepts a final execution map without joining argv itself"\n'
                                            '    (do\n'
                                            '      (reset! calls [])\n'
                                            '      (desktop/say "hello" "there"\n'
                                            '                   {:platform :macos\n'
                                            '                    :runner fake-runner})\n'
                                            '      (deref calls))\n'
                                            '    [[["say" "hello there"] {}]])])\n'
                                            '\n'
                                            'results\n',
 'core/lib/test/tool/sh/docker_test.hal': '(ns tool.sh.docker-test\n'
                                           '  (:require [tool.sh.docker :as docker]))\n'
                                           '\n'
                                           '(def calls (atom []))\n'
                                           '\n'
                                           '(defn fake-runner\n'
                                           '  [argv options]\n'
                                           '  (swap! calls conj [argv options])\n'
                                           '  {:command argv :stdout "ok\\n" :stderr "" :exit 0})\n'
                                           '\n'
                                           '(defn lifecycle-runner\n'
                                           '  [argv options]\n'
                                           '  (cond\n'
                                           '    (= "inspect" (nth argv 1))\n'
                                           '    {:command argv :stdout "" :stderr "" :exit 1}\n'
                                           '\n'
                                           '    (= "run" (nth argv 1))\n'
                                           '    {:command argv :stdout "container-id\\n" :stderr "" :exit 0}\n'
                                           '\n'
                                           '    (= "port" (nth argv 1))\n'
                                           '    {:command argv :stdout "0.0.0.0:49152\\n" :stderr "" :exit 0}\n'
                                           '\n'
                                           '    :else\n'
                                           '    {:command argv :stdout "" :stderr "" :exit 0}))\n'
                                           '\n'
                                           '(def results\n'
                                           '  [(test-check\n'
                                           '    "Docker map options are emitted deterministically"\n'
                                           '    (docker/append-map ["docker" "run"] "-e"\n'
                                           '                       {"Z" "9" "A" "1"})\n'
                                           '    ["docker" "run" "-e" "A=1" "-e" "Z=9"])\n'
                                           '\n'
                                           '   (test-check\n'
                                           '    "Docker run is an argv-only builder"\n'
                                           '    (docker/run-command\n'
                                           '     {:id :pg\n'
                                           '      :image "postgres:16"\n'
                                           '      :environment {"POSTGRES_USER" "hara"}\n'
                                           '      :ports [5432]\n'
                                           '      :cmd ["postgres" "-c" "fsync=off"]})\n'
                                           '    ["docker" "run"\n'
                                           '     "-e" "POSTGRES_USER=hara"\n'
                                           '     "-p" "127.0.0.1::5432"\n'
                                           '     "-d" "--rm"\n'
                                           '     "--add-host=host.docker.internal:host-gateway"\n'
                                           '     "--name" "testing_pg" "postgres:16"\n'
                                           '     "postgres" "-c" "fsync=off"])\n'
                                           '\n'
                                           '   (test-check\n'
                                           '    "Docker execution strips domain options before an injected runner"\n'
                                           '    (do\n'
                                           '      (reset! calls [])\n'
                                           '      (docker/command-result\n'
                                           '       ["docker" "version"]\n'
                                           '       {:runner fake-runner\n'
                                           '        :cwd "/tmp"\n'
                                           '        :container-only true})\n'
                                           '      (deref calls))\n'
                                           '    [[["docker" "version"] {:cwd "/tmp"}]])\n'
                                           '\n'
                                           '   (test-check\n'
                                           '    "Docker lifecycle uses one runner and trims semantic identifiers"\n'
                                           '    (select-keys\n'
                                           '     (docker/start-container\n'
                                           '      {:id :pg\n'
                                           '       :image "postgres:16"\n'
                                           '       :ports [5432]}\n'
                                           '      {:runner lifecycle-runner})\n'
                                           '     [:container-id :host :port :owned? :secondary])\n'
                                           '    {:container-id "container-id"\n'
                                           '     :host "127.0.0.1"\n'
                                           '     :port 49152\n'
                                           '     :owned? true\n'
                                           '     :secondary false})])\n'
                                           '\n'
                                           'results\n',
 'core/lib/test/tool/sh/git_test.hal': '(ns tool.sh.git-test\n'
                                        '  (:require [tool.sh.git :as git]))\n'
                                        '\n'
                                        '(def calls (atom []))\n'
                                        '\n'
                                        '(defn fake-runner\n'
                                        '  [argv options]\n'
                                        '  (swap! calls conj [argv options])\n'
                                        '  {:command argv\n'
                                        '   :stdout " value\\n"\n'
                                        '   :stderr ""\n'
                                        '   :exit 0})\n'
                                        '\n'
                                        '(defn failing-runner\n'
                                        '  [argv options]\n'
                                        '  {:command argv :stdout "out\\n" :stderr "err\\n" :exit 7})\n'
                                        '\n'
                                        '(def results\n'
                                        '  [(test-check\n'
                                        '    "legacy :root is normalized to cwd rather than git -C"\n'
                                        '    (do\n'
                                        '      (reset! calls [])\n'
                                        '      (git/git-result ["status"]\n'
                                        '                      {:root "/repo"\n'
                                        '                       :env {"A" "1"}\n'
                                        '                       :runner fake-runner\n'
                                        '                       :force true})\n'
                                        '      (deref calls))\n'
                                        '    [[["git" "status"]\n'
                                        '      {:cwd "/repo" :env {"A" "1"}}]])\n'
                                        '\n'
                                        '   (test-check\n'
                                        '    "Git results use standard keys and explicit Git output trimming"\n'
                                        '    (git/git-result ["rev-parse" "HEAD"]\n'
                                        '                    {:runner fake-runner})\n'
                                        '    {:command ["git" "rev-parse" "HEAD"]\n'
                                        '     :stdout "value"\n'
                                        '     :stderr ""\n'
                                        '     :exit 0})\n'
                                        '\n'
                                        '   (test-check\n'
                                        '    "legacy helper arities retain their argv"\n'
                                        '    (do\n'
                                        '      (reset! calls [])\n'
                                        '      (git/init {:runner fake-runner})\n'
                                        '      (git/clone "repo" {:dir "target" :runner fake-runner})\n'
                                        '      (git/add {:all true :runner fake-runner})\n'
                                        '      (git/commit {:message "message"\n'
                                        '                   :amend true\n'
                                        '                   :runner fake-runner})\n'
                                        '      (git/checkout "feature" {:create true\n'
                                        '                               :force true\n'
                                        '                               :runner fake-runner})\n'
                                        '      (git/submodule :update {:runner fake-runner})\n'
                                        '      (mapv first (deref calls)))\n'
                                        '    [["git" "init"]\n'
                                        '     ["git" "clone" "repo" "target"]\n'
                                        '     ["git" "add" "--all"]\n'
                                        '     ["git" "commit" "-m" "message" "--amend"]\n'
                                        '     ["git" "checkout" "-b" "-f" "feature"]\n'
                                        '     ["git" "submodule" "update" "--init" "--recursive"]])\n'
                                        '\n'
                                        '   (test-check\n'
                                        '    "non-zero Git results remain data unless ignore-errors is false"\n'
                                        '    [(git/git-run ["status"] {:runner failing-runner})\n'
                                        '     (try\n'
                                        '       (git/git-run ["status"]\n'
                                        '                    {:runner failing-runner\n'
                                        '                     :ignore-errors false})\n'
                                        '       nil\n'
                                        '       (catch Throwable error\n'
                                        '         (ex-data error)))]\n'
                                        '    [{:command ["git" "status"]\n'
                                        '      :stdout "out"\n'
                                        '      :stderr "err\\n"\n'
                                        '      :exit 7}\n'
                                        '     {:command ["git" "status"]\n'
                                        '      :stdout "out"\n'
                                        '      :stderr "err\\n"\n'
                                        '      :exit 7}])])\n'
                                        '\n'
                                        'results\n',
 'core/lib/test/tool/sh/net_test.hal': '(ns tool.sh.net-test\n'
                                        '  (:require [tool.sh.net :as net]))\n'
                                        '\n'
                                        '(def calls (atom []))\n'
                                        '\n'
                                        '(defn fake-runner\n'
                                        '  [argv options]\n'
                                        '  (swap! calls conj [argv options])\n'
                                        '  {:command argv :stdout "" :stderr "" :exit 0})\n'
                                        '\n'
                                        '(def results\n'
                                        '  [(test-check\n'
                                        '    "wget argv keeps output and additional arguments explicit"\n'
                                        '    (net/wget-command\n'
                                        '     "https://example.test/"\n'
                                        '     "asset.bin"\n'
                                        '     "local.bin"\n'
                                        '     ["--quiet"])\n'
                                        '    ["wget" "https://example.test/asset.bin"\n'
                                        '     "-O" "local.bin"\n'
                                        '     "--quiet"])\n'
                                        '\n'
                                        '   (test-check\n'
                                        '    "wget-bulk preserves descriptors, cwd, and injected execution"\n'
                                        '    (do\n'
                                        '      (reset! calls [])\n'
                                        '      (net/wget-bulk\n'
                                        '       "https://example.test/"\n'
                                        '       "fixtures"\n'
                                        '       ["a.txt" ["b.txt" "renamed.txt"]]\n'
                                        '       ["--quiet"]\n'
                                        '       {:runner fake-runner})\n'
                                        '      (deref calls))\n'
                                        '    [[["wget" "https://example.test/a.txt" "--quiet"]\n'
                                        '      {:cwd "resources/fixtures"}]\n'
                                        '     [["wget" "https://example.test/b.txt"\n'
                                        '       "-O" "renamed.txt" "--quiet"]\n'
                                        '      {:cwd "resources/fixtures"}]])\n'
                                        '\n'
                                        '   (test-check\n'
                                        '    "malformed wget descriptors are rejected"\n'
                                        '    (try\n'
                                        '      (net/wget-bulk\n'
                                        '       "" "."\n'
                                        '       [[:not-a-string nil]]\n'
                                        '       []\n'
                                        '       {:runner fake-runner})\n'
                                        '      false\n'
                                        '      (catch Throwable error true))\n'
                                        '    true)])\n'
                                        '\n'
                                        'results\n',
 'core/lib/test/tool/sh/tmux_test.hal': '(ns tool.sh.tmux-test\n'
                                         '  (:require [tool.sh.tmux :as tmux]))\n'
                                         '\n'
                                         '(def calls (atom []))\n'
                                         '\n'
                                         '(defn fake-runner\n'
                                         '  [argv options]\n'
                                         '  (swap! calls conj [argv options])\n'
                                         '  {:command argv :stdout "" :stderr "" :exit 0})\n'
                                         '\n'
                                         '(def results\n'
                                         '  [(test-check\n'
                                         '    "tmux start options are deterministic"\n'
                                         '    (tmux/with-args\n'
                                         '     ["tmux" "new-session"]\n'
                                         '     {:root "/repo"\n'
                                         '      :env {:z "9" :a "1"}})\n'
                                         '    ["tmux" "new-session"\n'
                                         '     "-c" "/repo"\n'
                                         '     "-e" "A=1"\n'
                                         '     "-e" "Z=9"])\n'
                                         '\n'
                                         '   (test-check\n'
                                         '    "vector dispatch preserves argv boundaries through respawn-pane"\n'
                                         '    (tmux/send-keys-command\n'
                                         '     "DEV" "build"\n'
                                         '     ["printf" "%s" "a b"])\n'
                                         '    ["tmux" "respawn-pane" "-k" "-t" "DEV:build" "--"\n'
                                         '     "printf" "%s" "a b"])\n'
                                         '\n'
                                         '   (test-check\n'
                                         '    "explicit string dispatch retains send-keys semantics"\n'
                                         '    (tmux/send-keys-command "DEV" "shell" "echo hello")\n'
                                         '    ["tmux" "send-keys" "-t" "DEV:shell" "echo hello" "C-m"])\n'
                                         '\n'
                                         '   (test-check\n'
                                         '    "new session and window builders execute vector commands directly"\n'
                                         '    [(tmux/new-session-command\n'
                                         '      "DEV" {:command ["sleep" "10"]})\n'
                                         '     (tmux/new-window-command\n'
                                         '      "DEV" "worker"\n'
                                         '      {:cwd "/repo"\n'
                                         '       :command ["node" "worker.js" "--name" "a b"]})]\n'
                                         '    [["tmux" "new-session" "-d" "-s" "DEV" "-n" "CMD"\n'
                                         '      "--" "sleep" "10"]\n'
                                         '     ["tmux" "new-window" "-t" "DEV" "-n" "worker"\n'
                                         '      "-c" "/repo"\n'
                                         '      "--" "node" "worker.js" "--name" "a b"]])\n'
                                         '\n'
                                         '   (test-check\n'
                                         '    "tmux operations strip command-domain options before runners"\n'
                                         '    (do\n'
                                         '      (reset! calls [])\n'
                                         '      (tmux/kill-window\n'
                                         '       "DEV" "worker"\n'
                                         '       {:runner fake-runner\n'
                                         '        :root "/repo"\n'
                                         '        :env {"A" "1"}})\n'
                                         '      (deref calls))\n'
                                         '    [[["tmux" "kill-window" "-t" "DEV:worker"] {}]])])\n'
                                         '\n'
                                         'results\n',
 'core/lib/test/tool/sh_test.hal': '(ns tool.sh-test\n'
                                    '  (:require [tool.sh :as sh]))\n'
                                    '\n'
                                    '(def results\n'
                                    '  [(test-check\n'
                                    '    "execute returns a Promise and preserves exact output"\n'
                                    '    (let [pending\n'
                                    '          (sh/execute\n'
                                    '           ["sh" "-c"\n'
                                    '            "printf \' out\\n\'; printf \' err\\n\' >&2; exit 7"])]\n'
                                    '      [(promise? pending) (deref pending)])\n'
                                    '    [true\n'
                                    '     {:command\n'
                                    '      ["sh" "-c"\n'
                                    '       "printf \' out\\n\'; printf \' err\\n\' >&2; exit 7"]\n'
                                    '      :stdout " out\\n"\n'
                                    '      :stderr " err\\n"\n'
                                    '      :exit 7}])\n'
                                    '\n'
                                    '   (test-check\n'
                                    '    "stdin strings are UTF-8 encoded, written, and closed"\n'
                                    '    (sh/run ["cat"] {:stdin "hello\\n"})\n'
                                    '    {:command ["cat"]\n'
                                    '     :stdout "hello\\n"\n'
                                    '     :stderr ""\n'
                                    '     :exit 0})\n'
                                    '\n'
                                    '   (test-check\n'
                                    '    "cwd and string environment entries reach the process builder"\n'
                                    '    [(sh/run ["pwd"] {:cwd "/tmp"})\n'
                                    '     (sh/run ["printenv" "HARA_TOOL_SH_TEST"]\n'
                                    '             {:env {"HARA_TOOL_SH_TEST" "present"}})]\n'
                                    '    [{:command ["pwd"]\n'
                                    '      :stdout "/tmp\\n"\n'
                                    '      :stderr ""\n'
                                    '      :exit 0}\n'
                                    '     {:command ["printenv" "HARA_TOOL_SH_TEST"]\n'
                                    '      :stdout "present\\n"\n'
                                    '      :stderr ""\n'
                                    '      :exit 0}])\n'
                                    '\n'
                                    '   (test-check\n'
                                    '    "spawn leaves stdin open and lifecycle wrappers remain explicit"\n'
                                    '    (let [process (sh/spawn ["cat"])\n'
                                    '          written (sh/write process "stream")\n'
                                    '          alive (sh/alive? process)\n'
                                    '          _ (sh/close-input process)\n'
                                    '          values (deref\n'
                                    '                  (promise/all\n'
                                    '                   [(sh/stdout process)\n'
                                    '                    (sh/stderr process)\n'
                                    '                    (sh/wait process)]))]\n'
                                    '      [written alive\n'
                                    '       (str/decode-utf8 (nth values 0))\n'
                                    '       (str/decode-utf8 (nth values 1))\n'
                                    '       (nth values 2)])\n'
                                    '    [6 true "stream" "" 0])\n'
                                    '\n'
                                    '   (test-check\n'
                                    '    "run! preserves the failed result as ex-data"\n'
                                    '    (try\n'
                                    '      (sh/run! ["sh" "-c" "printf out; printf err >&2; exit 9"])\n'
                                    '      nil\n'
                                    '      (catch Throwable error\n'
                                    '        [(ex-message error) (ex-data error)]))\n'
                                    '    ["Command failed"\n'
                                    '     {:command ["sh" "-c" "printf out; printf err >&2; exit 9"]\n'
                                    '      :stdout "out"\n'
                                    '      :stderr "err"\n'
                                    '      :exit 9}])\n'
                                    '\n'
                                    '   (test-check\n'
                                    '    "implicit command strings are rejected"\n'
                                    '    (try\n'
                                    '      (sh/command "echo unsafe")\n'
                                    '      false\n'
                                    '      (catch Throwable error\n'
                                    '        (= :invalid-command\n'
                                    '           (:tool.sh/error (ex-data error)))))\n'
                                    '    true)\n'
                                    '\n'
                                    '   (test-check\n'
                                    '    "the process option map is closed and typed"\n'
                                    '    [(try\n'
                                    '       (sh/options {:trim true})\n'
                                    '       nil\n'
                                    '       (catch Throwable error\n'
                                    '         (:tool.sh/error (ex-data error))))\n'
                                    '     (try\n'
                                    '       (sh/options {:cwd :tmp})\n'
                                    '       nil\n'
                                    '       (catch Throwable error\n'
                                    '         (:tool.sh/error (ex-data error))))\n'
                                    '     (try\n'
                                    '       (sh/options {:env {:A "1"}})\n'
                                    '       nil\n'
                                    '       (catch Throwable error\n'
                                    '         (:tool.sh/error (ex-data error))))\n'
                                    '     (try\n'
                                    '       (sh/options {:stdin 42})\n'
                                    '       nil\n'
                                    '       (catch Throwable error\n'
                                    '         (:tool.sh/error (ex-data error))))]\n'
                                    '    [:unknown-options :invalid-cwd :invalid-env :invalid-stdin])])\n'
                                    '\n'
                                    'results\n',
 'scripts/runtime/check-tool-sh-hard-cut.py': '#!/usr/bin/env python3\n'
                                                  '"""Enforce the canonical tool.sh ownership and hard cuts."""\n'
                                                  '\n'
                                                  'from __future__ import annotations\n'
                                                  '\n'
                                                  'from pathlib import Path\n'
                                                  'import sys\n'
                                                  '\n'
                                                  'ROOT = Path(__file__).resolve().parents[2]\n'
                                                  '\n'
                                                  'REQUIRED_SOURCES = {\n'
                                                  '    "tool.sh": ROOT / "core/lib/src/tool/sh.hal",\n'
                                                  '    "tool.sh.desktop": ROOT / "core/lib/src/tool/sh/desktop.hal",\n'
                                                  '    "tool.sh.docker": ROOT / "core/lib/src/tool/sh/docker.hal",\n'
                                                  '    "tool.sh.git": ROOT / "core/lib/src/tool/sh/git.hal",\n'
                                                  '    "tool.sh.net": ROOT / "core/lib/src/tool/sh/net.hal",\n'
                                                  '    "tool.sh.tmux": ROOT / "core/lib/src/tool/sh/tmux.hal",\n'
                                                  '}\n'
                                                  '\n'
                                                  'FORBIDDEN_PATHS = (\n'
                                                  '    ROOT / "core/lib/src/lib/docker.hal",\n'
                                                  '    ROOT / "core/lib/src/tool/sh/misc.hal",\n'
                                                  '    ROOT / "core/lib/src/tool/sh/network.hal",\n'
                                                  ')\n'
                                                  '\n'
                                                  'FORBIDDEN_TEXT = (\n'
                                                  '    "lib.docker",\n'
                                                  '    "tool.sh.misc",\n'
                                                  '    "tool.sh.network",\n'
                                                  '    ":tool.sh/runner",\n'
                                                  ')\n'
                                                  '\n'
                                                  'ACTIVE_ROOTS = (\n'
                                                  '    ROOT / "core/lib/src",\n'
                                                  '    ROOT / "core/lib/src-lang",\n'
                                                  '    ROOT / "core/lib/test",\n'
                                                  '    ROOT / "core/lib/test-lang",\n'
                                                  '    ROOT / "core/lib/integration",\n'
                                                  ')\n'
                                                  '\n'
                                                  '\n'
                                                  'def fail(message: str) -> None:\n'
                                                  '    print(message, file=sys.stderr)\n'
                                                  '    raise SystemExit(1)\n'
                                                  '\n'
                                                  '\n'
                                                  'def hal_files() -> list[Path]:\n'
                                                  '    files: list[Path] = []\n'
                                                  '    for root in ACTIVE_ROOTS:\n'
                                                  '        if root.exists():\n'
                                                  '            files.extend(root.rglob("*.hal"))\n'
                                                  '    return sorted(files)\n'
                                                  '\n'
                                                  '\n'
                                                  'def main() -> None:\n'
                                                  '    for namespace, path in REQUIRED_SOURCES.items():\n'
                                                  '        if not path.is_file():\n'
                                                  '            fail(f"missing canonical {namespace} source: {path.relative_to(ROOT)}")\n'
                                                  '        expected = f"(ns {namespace}"\n'
                                                  '        if expected not in path.read_text(encoding="utf-8"):\n'
                                                  '            fail(f"{path.relative_to(ROOT)} does not declare {namespace}")\n'
                                                  '\n'
                                                  '    for path in FORBIDDEN_PATHS:\n'
                                                  '        if path.exists():\n'
                                                  '            fail(f"retired tool path remains: {path.relative_to(ROOT)}")\n'
                                                  '\n'
                                                  '    files = hal_files()\n'
                                                  '    for path in files:\n'
                                                  '        text = path.read_text(encoding="utf-8")\n'
                                                  '        for token in FORBIDDEN_TEXT:\n'
                                                  '            if token in text:\n'
                                                  '                fail(f"retired tool token {token!r} remains in {path.relative_to(ROOT)}")\n'
                                                  '\n'
                                                  '    spawn_owners: list[Path] = []\n'
                                                  '    for path in files:\n'
                                                  '        if "Process/spawn" in path.read_text(encoding="utf-8"):\n'
                                                  '            spawn_owners.append(path.relative_to(ROOT))\n'
                                                  '    expected_owner = Path("core/lib/src/tool/sh.hal")\n'
                                                  '    if spawn_owners != [expected_owner]:\n'
                                                  '        fail(\n'
                                                  '            "Process/spawn ownership must be exactly tool.sh; found "\n'
                                                  '            + ", ".join(map(str, spawn_owners))\n'
                                                  '        )\n'
                                                  '\n'
                                                  '    tool_source = REQUIRED_SOURCES["tool.sh"].read_text(encoding="utf-8")\n'
                                                  '    for public in (\n'
                                                  '        "spawn",\n'
                                                  '        "execute",\n'
                                                  '        "run",\n'
                                                  '        "run!",\n'
                                                  '        "stdout",\n'
                                                  '        "stderr",\n'
                                                  '        "wait",\n'
                                                  '        "write",\n'
                                                  '        "close-input",\n'
                                                  '        "alive?",\n'
                                                  '        "kill",\n'
                                                  '    ):\n'
                                                  '        if f"(defn {public}" not in tool_source:\n'
                                                  '            fail(f"tool.sh is missing public operation {public}")\n'
                                                  '    if "str/trim" in tool_source:\n'
                                                  '        fail("tool.sh must not trim process output")\n'
                                                  '    if "promise/all" not in tool_source or "promise/then" not in tool_source:\n'
                                                  '        fail("tool.sh/execute must remain Promise-based")\n'
                                                  '    if ":runner" in tool_source or ":tool.sh/runner" in tool_source:\n'
                                                  '        fail("runner injection belongs only to command-specific namespaces")\n'
                                                  '\n'
                                                  '    tmux_source = REQUIRED_SOURCES["tool.sh.tmux"].read_text(encoding="utf-8")\n'
                                                  '    if "str/join" in tmux_source:\n'
                                                  '        fail("tool.sh.tmux must not collapse argv vectors into command strings")\n'
                                                  '    if "respawn-pane" not in tmux_source or \'"--"\' not in tmux_source:\n'
                                                  '        fail("tool.sh.tmux must retain direct argv dispatch evidence")\n'
                                                  '\n'
                                                  '    inventory_path = ROOT / "core/rust/standard-library.namespaces"\n'
                                                  '    inventory = set(inventory_path.read_text(encoding="utf-8").splitlines())\n'
                                                  '    missing = sorted(set(REQUIRED_SOURCES) - inventory)\n'
                                                  '    if missing:\n'
                                                  '        fail("standard-library inventory is missing: " + ", ".join(missing))\n'
                                                  '    retired = sorted({"tool.sh.misc", "tool.sh.network"} & inventory)\n'
                                                  '    if retired:\n'
                                                  '        fail("standard-library inventory retains: " + ", ".join(retired))\n'
                                                  '\n'
                                                  '    print("tool.sh hard-cut audit passed")\n'
                                                  '\n'
                                                  '\n'
                                                  'if __name__ == "__main__":\n'
                                                  '    main()\n'}

DELETE = ['core/lib/src/tool/sh/network.hal', 'core/lib/src/tool/sh/misc.hal', 'core/lib/test/tool/sh/domain_test.hal']

NAMESPACES = {
    "tool.sh",
    "tool.sh.desktop",
    "tool.sh.docker",
    "tool.sh.git",
    "tool.sh.net",
    "tool.sh.tmux",
}


def main() -> None:
    for relative, content in FILES.items():
        path = ROOT / relative
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(content, encoding="utf-8")

    for relative in DELETE:
        path = ROOT / relative
        if path.exists():
            path.unlink()

    inventory_path = ROOT / "core/rust/standard-library.namespaces"
    inventory = set(inventory_path.read_text(encoding="utf-8").splitlines())
    inventory.difference_update({"tool.sh.misc", "tool.sh.network"})
    inventory.update(NAMESPACES)
    inventory_path.write_text("\n".join(sorted(inventory)) + "\n", encoding="utf-8")


if __name__ == "__main__":
    main()
