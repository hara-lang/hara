diff --git a/core/lib/src/std/dom/common.hal b/core/lib/src/std/dom/common.hal
index 22115e0..c3f3831 100644
--- a/core/lib/src/std/dom/common.hal
+++ b/core/lib/src/std/dom/common.hal
@@ -11,16 +11,16 @@
 
 (defn dom-field-set!
   [dom key value]
-  (cond
-    (= key :tag) (set! (field dom :tag) value)
-    (= key :props) (set! (field dom :props) value)
-    (= key :item) (set! (field dom :item) value)
-    (= key :parent) (set! (field dom :parent) value)
-    (= key :handler) (set! (field dom :handler) value)
-    (= key :shadow) (set! (field dom :shadow) value)
-    (= key :cache) (set! (field dom :cache) value)
-    (= key :extra) (set! (field dom :extra) value)
-    :else (throw (ex-info "Unknown DOM field" {:field key})))
+  (case key
+    :tag (set! (field dom :tag) value)
+    :props (set! (field dom :props) value)
+    :item (set! (field dom :item) value)
+    :parent (set! (field dom :parent) value)
+    :handler (set! (field dom :handler) value)
+    :shadow (set! (field dom :shadow) value)
+    :cache (set! (field dom :cache) value)
+    :extra (set! (field dom :extra) value)
+    (throw (ex-info "Unknown DOM field" {:field key})))
   dom)
 
 (defn dom-fields-set!
diff --git a/core/lib/src/tool/cli/template.hal b/core/lib/src/tool/cli/template.hal
index 5cab3b3..6393243 100644
--- a/core/lib/src/tool/cli/template.hal
+++ b/core/lib/src/tool/cli/template.hal
@@ -5,6 +5,8 @@
 
 (def schema-id "tool.cli.template/0-alpha")
 
+(def +handlers+ (atom []))
+
 (def ^:dynamic *template-defaults*
   (atom {:default {}}))
 
@@ -238,15 +240,17 @@
   [raw-definition]
   (let [definition (normalise-definition raw-definition)
         command-work (work definition)
-        runtime-fn (:runtime-fn definition)]
-    (with-meta
-      (fn [request]
-        (std-work/run (runtime-fn) command-work request))
-      {:tool.cli.template/schema schema-id
-       :tool.cli.template/definition definition
-       :tool.cli.template/work command-work})))
+        runtime-fn (:runtime-fn definition)
+        handler-fn
+        (fn [request]
+          (std-work/run (runtime-fn) command-work request))]
+    (swap! +handlers+ conj handler-fn)
+    handler-fn))
 
 (defn handler?
   [value]
-  (= schema-id
-     (:tool.cli.template/schema (meta value))))
+  (and
+   (fn? value)
+   (boolean
+    (some (fn [handler-fn] (identical? value handler-fn))
+          (deref +handlers+)))))
diff --git a/core/lib/test/tool/cli/template_test.hal b/core/lib/test/tool/cli/template_test.hal
index 1c04e80..dbdacb2 100644
--- a/core/lib/test/tool/cli/template_test.hal
+++ b/core/lib/test/tool/cli/template_test.hal
@@ -108,5 +108,9 @@
    (vals (handlers/registry)))
   => true)
 
+(fact "ordinary functions are not mistaken for template handlers"
+  (template/handler? (fn [request] request))
+  => false)
+
 (pr-str
  (run {:namespace "tool.cli.template-test"}))
diff --git a/core/rust/hal-src/code/test/base/executive.hal b/core/rust/hal-src/code/test/base/executive.hal
index 6dad9e0..97289f4 100644
--- a/core/rust/hal-src/code/test/base/executive.hal
+++ b/core/rust/hal-src/code/test/base/executive.hal
@@ -132,9 +132,11 @@
   "Removes registered facts for selected namespaces without executing them."
   ([] (run:unload :all))
   ([selector-or-options]
-   (let [namespaces (namespace-selection selector-or-options)]
-     (doseq [namespace namespaces]
-       (runtime/purge-facts namespace))
+   (let [namespaces (namespace-selection selector-or-options)
+         _ (reduce (fn [_ namespace]
+                     (runtime/purge-facts namespace))
+                   nil
+                   namespaces)]
      namespaces)))
 
 (defn run-errored
diff --git a/core/rust/hal-src/tool/cli/template.hal b/core/rust/hal-src/tool/cli/template.hal
index 5cab3b3..6393243 100644
--- a/core/rust/hal-src/tool/cli/template.hal
+++ b/core/rust/hal-src/tool/cli/template.hal
@@ -5,6 +5,8 @@
 
 (def schema-id "tool.cli.template/0-alpha")
 
+(def +handlers+ (atom []))
+
 (def ^:dynamic *template-defaults*
   (atom {:default {}}))
 
@@ -238,15 +240,17 @@
   [raw-definition]
   (let [definition (normalise-definition raw-definition)
         command-work (work definition)
-        runtime-fn (:runtime-fn definition)]
-    (with-meta
-      (fn [request]
-        (std-work/run (runtime-fn) command-work request))
-      {:tool.cli.template/schema schema-id
-       :tool.cli.template/definition definition
-       :tool.cli.template/work command-work})))
+        runtime-fn (:runtime-fn definition)
+        handler-fn
+        (fn [request]
+          (std-work/run (runtime-fn) command-work request))]
+    (swap! +handlers+ conj handler-fn)
+    handler-fn))
 
 (defn handler?
   [value]
-  (= schema-id
-     (:tool.cli.template/schema (meta value))))
+  (and
+   (fn? value)
+   (boolean
+    (some (fn [handler-fn] (identical? value handler-fn))
+          (deref +handlers+)))))
diff --git a/core/rust/src/vm/bundle.rs b/core/rust/src/vm/bundle.rs
index fed6a71..393141b 100644
--- a/core/rust/src/vm/bundle.rs
+++ b/core/rust/src/vm/bundle.rs
@@ -31,25 +31,52 @@ pub struct BytecodeBundleModule {
 }
 
 pub fn embedded_standard_library_sources() -> Vec<ModuleSource<'static>> {
+    let resources = EMBEDDED_HAL_RESOURCES
+        .iter()
+        .map(|(namespace, _, source)| (*namespace, *source))
+        .collect::<std::collections::HashMap<_, _>>();
+    let mut selected = resources
+        .keys()
+        .copied()
+        .filter(|namespace| portable_standard_library_root(namespace))
+        .collect::<std::collections::HashSet<_>>();
+    let mut pending = selected.iter().copied().collect::<Vec<_>>();
+
+    // The portable library owns std.*, code.*, and lang.*. Package every eager
+    // embedded dependency those roots require as well so a source-free runtime
+    // never indexes a module whose namespace declaration cannot be satisfied.
+    while let Some(resource) = pending.pop() {
+        let source = resources
+            .get(resource)
+            .unwrap_or_else(|| panic!("missing embedded HAL resource: {resource}"));
+        let (namespace_form, _) = split_namespace_form(source)
+            .unwrap_or_else(|error| panic!("invalid embedded HAL resource {resource}: {error}"));
+        for dependency in namespace_dependencies(namespace_form)
+            .unwrap_or_else(|error| panic!("invalid namespace dependencies for {resource}: {error}"))
+        {
+            if let Some((dependency, _)) = resources.get_key_value(dependency.as_str()) {
+                if selected.insert(*dependency) {
+                    pending.push(*dependency);
+                }
+            }
+        }
+    }
+
     let ordered = std::iter::once("std.foundation")
         .chain(EAGER_HAL_RESOURCES.iter().copied())
         .chain(
-            EMBEDDED_HAL_RESOURCES
+            selected
                 .iter()
-                .map(|(namespace, _, _)| *namespace)
+                .copied()
                 .filter(|namespace| {
-                    standard_library_namespace(namespace)
-                        && *namespace != "std.foundation"
-                        && !EAGER_HAL_RESOURCES.contains(namespace)
+                    *namespace != "std.foundation" && !EAGER_HAL_RESOURCES.contains(namespace)
                 }),
         );
     let sources = ordered
-        .map(|resource| {
-            let source = EMBEDDED_HAL_RESOURCES
-                .iter()
-                .find_map(|(name, _, source)| (*name == resource).then_some(*source))
-                .unwrap_or_else(|| panic!("missing embedded HAL resource: {resource}"));
-            ModuleSource { resource, source }
+        .filter(|resource| selected.contains(resource))
+        .map(|resource| ModuleSource {
+            resource,
+            source: resources[resource],
         })
         .collect::<Vec<_>>();
     order_module_sources(&sources)
@@ -132,9 +159,6 @@ pub fn eval_bytecode_bundle(runtime: &mut Runtime, bytes: &[u8]) -> Result<(), S
     let loaded = (|| {
         for module in &modules {
             let source = runtime.resources.get(&module.resource);
-            if source.is_none() && standard_library_namespace(&module.resource) {
-                continue;
-            }
             let source_is_current = source
                 .map(|source| {
                     let digest: [u8; 32] = Sha256::digest(source.as_bytes()).into();
@@ -333,7 +357,7 @@ fn decode(bytes: &[u8]) -> Result<Vec<BytecodeBundleModule>, String> {
     Ok(modules)
 }
 
-fn standard_library_namespace(namespace: &str) -> bool {
+fn portable_standard_library_root(namespace: &str) -> bool {
     ["std.", "code.", "lang."]
         .iter()
         .any(|prefix| namespace.starts_with(prefix))
@@ -446,10 +470,9 @@ mod tests {
             let bytes = compile_embedded_standard_library_bundle()
                 .expect("compile standard library bundle");
             let mut runtime = Runtime::core();
-            for &(name, _, source) in EMBEDDED_HAL_RESOURCES {
-                runtime.register_resource(name, source);
-            }
-            eval_bytecode_bundle(&mut runtime, &bytes).expect("load foundation bundle");
+            assert!(runtime.resources.is_empty());
+            eval_bytecode_bundle(&mut runtime, &bytes)
+                .expect("load source-free foundation bundle");
             let publics = runtime
                 .eval_native("(keys (ns-publics 'std.foundation.string))")
                 .expect("inspect string namespace");
@@ -543,6 +566,33 @@ mod tests {
         );
     }
 
+    #[test]
+    fn stale_eager_bytecode_is_rejected_when_registered_source_differs() {
+        let mut compiler = Runtime::core();
+        compiler.use_namespace("example.eager");
+        let artifact = compiler
+            .compile_bytecode_artifact("(def answer 41)")
+            .expect("compile eager fixture");
+        let bytes = encode_bytecode_bundle(&[BytecodeBundleModule {
+            resource: "example.eager".into(),
+            namespace_form: "(ns example.eager)".into(),
+            source_digest: Sha256::digest(b"(ns example.eager) (def answer 41)").into(),
+            dependencies: vec![],
+            eager: true,
+            artifact,
+        }])
+        .expect("encode eager fixture");
+        let mut runtime = Runtime::core();
+        runtime.register_resource("example.eager", "(ns example.eager) (def answer 42)");
+
+        let error = eval_bytecode_bundle(&mut runtime, &bytes).unwrap_err();
+
+        assert_eq!(error, "stale eager bytecode bundle module: example.eager");
+        assert!(!runtime.bytecode_resources.contains_key("example.eager"));
+        assert!(!runtime.loaded_resources.contains("example.eager"));
+        assert!(runtime.namespace_registry.find("example.eager").is_none());
+    }
+
     #[test]
     fn eager_failure_rolls_back_the_whole_bundle() {
         let mut compiler = Runtime::core();
@@ -751,11 +801,33 @@ mod tests {
                 actual, expected,
                 "bundle inventory must be exact and ordered"
             );
-            let mut inventory = actual.clone();
-            inventory.sort_unstable();
-            assert_eq!(inventory, crate::STANDARD_LIBRARY_INVENTORY);
+            let mut portable_inventory = actual
+                .iter()
+                .copied()
+                .filter(|namespace| portable_standard_library_root(namespace))
+                .collect::<Vec<_>>();
+            portable_inventory.sort_unstable();
+            let expected_portable_inventory = crate::STANDARD_LIBRARY_INVENTORY
+                .iter()
+                .copied()
+                .filter(|namespace| portable_standard_library_root(namespace))
+                .collect::<Vec<_>>();
+            assert_eq!(portable_inventory, expected_portable_inventory);
+            let module_names = actual.iter().copied().collect::<std::collections::HashSet<_>>();
+            for module in &modules {
+                for dependency in &module.dependencies {
+                    assert!(
+                        module_names.contains(dependency.as_str()),
+                        "{} has unbundled eager dependency {dependency}",
+                        module.resource
+                    );
+                }
+            }
             assert!(modules.iter().any(|module| module.resource == "code.test"));
             assert!(modules.iter().any(|module| module.resource == "lang.core"));
+            assert!(modules
+                .iter()
+                .any(|module| module.resource == "postgres.core.impl-base"));
         });
     }
 
diff --git a/core/rust/src/vm/compiler/functions.rs b/core/rust/src/vm/compiler/functions.rs
index de9cd6c..0176efd 100644
--- a/core/rust/src/vm/compiler/functions.rs
+++ b/core/rust/src/vm/compiler/functions.rs
@@ -360,6 +360,34 @@ impl Compiler {
                             }
                         }
                         "quote" => {}
+                        "set!" => {
+                            if let Some(place) = children.get(1) {
+                                match place.form {
+                                    Form::List(forms)
+                                        if matches!(
+                                            forms.first(),
+                                            Some(Form::Symbol(operation)) if operation == "field"
+                                        ) =>
+                                    {
+                                        let place_children =
+                                            self.list_children(forms, place.span, place.children);
+                                        if let Some(receiver) = place_children.get(1) {
+                                            self.collect_free(receiver, bound, free);
+                                        }
+                                    }
+                                    Form::Symbol(_) => {}
+                                    _ => self.collect_free(place, bound, free),
+                                }
+                            }
+                            if let Some(value) = children.get(2) {
+                                self.collect_free(value, bound, free);
+                            }
+                        }
+                        "field" => {
+                            if let Some(instance) = children.get(1) {
+                                self.collect_free(instance, bound, free);
+                            }
+                        }
                         "." => {
                             if let Some(receiver) = children.get(1) {
                                 self.collect_free(receiver, bound, free);
diff --git a/core/rust/src/vm/execution_tests.rs b/core/rust/src/vm/execution_tests.rs
index 4da2474..5652a7b 100644
--- a/core/rust/src/vm/execution_tests.rs
+++ b/core/rust/src/vm/execution_tests.rs
@@ -940,6 +940,10 @@ fn defmutable_forms_use_reference_identity_and_settable_fields() {
         eval("(do (def order []) (defmutable Cursor [x]) (def cursor (Cursor 1)) (set! (field (do (set! order (conj order :receiver)) cursor) :x) (do (set! order (conj order :replacement)) 10)) [order (field cursor :x)])"),
         "[[:receiver :replacement] 10]"
     );
+    assert_eq!(
+        eval("(do (defmutable Cursor [x]) (defn move! [cursor value] (set! (field cursor :x) value)) (defn position [cursor] (field cursor :x)) (let [cursor (Cursor 1)] [(move! cursor 10) (position cursor)]))"),
+        "[10 10]"
+    );
     assert_eq!(
         eval("(do (defmutable Cursor [x]) (instance? Cursor (Cursor 1)))"),
         "true"
