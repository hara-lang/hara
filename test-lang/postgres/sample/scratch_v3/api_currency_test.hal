(ns postgres.sample.scratch-v3.api-currency-test
  (:require [postgres.sample.scratch-v3.api-currency :as api]))

^{:refer postgres.sample.scratch-v3.api-currency/descriptor}
(Test/check [{:name "registers the curated currency descriptor constructor"
              :test (fn [] (fn? api/descriptor))
              :expected true}])
