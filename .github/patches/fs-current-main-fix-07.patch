diff --git a/core/java/src/main/java/hara/truffle/HaraContext.java b/core/java/src/main/java/hara/truffle/HaraContext.java
--- a/core/java/src/main/java/hara/truffle/HaraContext.java
+++ b/core/java/src/main/java/hara/truffle/HaraContext.java
@@ -4065,21 +4065,29 @@
         path,
         null,
         provider ->
-            provider.entries(path).stream()
-                .map(HaraFileProvider.Entry::toValue)
-                .toArray(Object[]::new));
+            hara.lang.data.Vector.Standard.from(
+                null,
+                provider.entries(path).stream()
+                    .map(HaraFileProvider.Entry::toValue)
+                    .toArray()));
   }
 
   private Object fileList(Object value) {
     String path = stringValue(value, "file/list");
     return fileEffect(
-        "list", path, null, provider -> provider.list(path).toArray(String[]::new));
+        "list",
+        path,
+        null,
+        provider -> hara.lang.data.Vector.Standard.from(null, provider.list(path).toArray()));
   }
 
   private Object fileWalk(Object value) {
     String path = stringValue(value, "file/walk");
     return fileEffect(
-        "walk", path, null, provider -> provider.walk(path).toArray(String[]::new));
+        "walk",
+        path,
+        null,
+        provider -> hara.lang.data.Vector.Standard.from(null, provider.walk(path).toArray()));
   }
 
   private Object fileMkdir(Object[] values) {
