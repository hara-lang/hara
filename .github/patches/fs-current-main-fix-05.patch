diff --git a/core/java/src/main/java/hara/truffle/HaraFileProvider.java b/core/java/src/main/java/hara/truffle/HaraFileProvider.java
--- a/core/java/src/main/java/hara/truffle/HaraFileProvider.java
+++ b/core/java/src/main/java/hara/truffle/HaraFileProvider.java
@@ -87,11 +87,13 @@ final class HaraFileProvider {
 
   HaraFileProvider(Path root) {
     Path normalized = root.toAbsolutePath().normalize();
+    Path resolved;
     try {
-      this.root = normalized.toRealPath();
+      resolved = normalized.toRealPath();
     } catch (IOException ignored) {
-      this.root = normalized;
+      resolved = normalized;
     }
+    this.root = resolved;
   }
 
   byte[] read(String path) throws IOException {

diff --git a/core/java/src/test/java/hara/truffle/StdFsTest.java b/core/java/src/test/java/hara/truffle/StdFsTest.java
--- a/core/java/src/test/java/hara/truffle/StdFsTest.java
+++ b/core/java/src/test/java/hara/truffle/StdFsTest.java
@@ -133,10 +133,7 @@ public class StdFsTest {
 
   private static Context newContext(Path root) {
     IOAccess io = IOAccess.newBuilder().fileSystem(new HaraMountedFileSystem(root)).build();
-    return Context.newBuilder(HaraLanguage.ID)
-        .currentWorkingDirectory(root)
-        .allowIO(io)
-        .build();
+    return Context.newBuilder(HaraLanguage.ID).allowIO(io).build();
   }
 
   private static void deleteTree(Path root) throws IOException {

diff --git a/core/lib/src/std/fs/walk.hal b/core/lib/src/std/fs/walk.hal
--- a/core/lib/src/std/fs/walk.hal
+++ b/core/lib/src/std/fs/walk.hal
@@ -1,5 +1,6 @@
 ;; Promise-based deterministic traversal over std.native.File/entries.
 (ns std.fs.walk
+  (:config {:override [walk]})
   (:require [std.foundation.promise :as promise]
             [std.fs.path :as path]))
 
diff --git a/core/rust/hal-src/std/fs/walk.hal b/core/rust/hal-src/std/fs/walk.hal
--- a/core/rust/hal-src/std/fs/walk.hal
+++ b/core/rust/hal-src/std/fs/walk.hal
@@ -1,5 +1,6 @@
 ;; Promise-based deterministic traversal over std.native.File/entries.
 (ns std.fs.walk
+  (:config {:override [walk]})
   (:require [std.foundation.promise :as promise]
             [std.fs.path :as path]))
 
