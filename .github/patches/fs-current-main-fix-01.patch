diff --git a/core/rust/src/file.rs b/core/rust/src/file.rs
index 9f13998..7428bf1 100644
--- a/core/rust/src/file.rs
+++ b/core/rust/src/file.rs
@@ -1239,7 +1239,9 @@ impl FileProvider for MemoryFileProvider {
         if options.replace {
             let target_prefix = format!("{target}/");
             if matches!(nodes.get(&target), Some(MemoryNode::Directory { .. }))
-                && nodes.keys().any(|candidate| candidate.starts_with(&target_prefix))
+                && nodes
+                    .keys()
+                    .any(|candidate| candidate.starts_with(&target_prefix))
             {
                 return Err(FileError::DirectoryNotEmpty);
             }
