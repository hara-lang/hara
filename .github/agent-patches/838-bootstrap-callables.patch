diff --git a/core/rust/src/core/inventory.rs b/core/rust/src/core/inventory.rs
index 36b15cd..6bcb5b8 100644
--- a/core/rust/src/core/inventory.rs
+++ b/core/rust/src/core/inventory.rs
@@ -73,6 +73,7 @@ pub(crate) const RUNTIME_CALLABLE_INVENTORY: &[&str] = &[
     "bit-shift-right",
     "bit-xor",
     "boolean",
+    "boolean?",
     "bytes",
     "capture",
     "cas!",
@@ -171,6 +172,8 @@ pub(crate) const RUNTIME_CALLABLE_INVENTORY: &[&str] = &[
     "pr-str",
     "println",
     "promise",
+    "promise/delay",
+    "promise/new",
     "promise?",
     "quot",
     "range",
diff --git a/core/rust/src/core/direct_callable_catalog.rs b/core/rust/src/core/direct_callable_catalog.rs
index 43c85e5..9e12dfb 100644
--- a/core/rust/src/core/direct_callable_catalog.rs
+++ b/core/rust/src/core/direct_callable_catalog.rs
@@ -126,6 +126,12 @@ pub(crate) const DIRECT_CALLABLE_CATALOG: &[DirectCallableSpec] = &[
         BootstrapLibrary,
         Basic
     ),
+    direct!(
+        "boolean?",
+        DirectCallableArity::Exact(1),
+        BootstrapLibrary,
+        Operation(direct_predicate_operation)
+    ),
     direct!("bytes", DirectCallableArity::Any, RuntimePrimitive, Bytes),
     direct!(
         "capture",
@@ -741,6 +747,18 @@ pub(crate) const DIRECT_CALLABLE_CATALOG: &[DirectCallableSpec] = &[
         RuntimePrimitive,
         Promise
     ),
+    direct!(
+        "promise/delay",
+        DirectCallableArity::Exact(2),
+        RuntimePrimitive,
+        Operation(direct_promise_operation)
+    ),
+    direct!(
+        "promise/new",
+        DirectCallableArity::Exact(1),
+        RuntimePrimitive,
+        Operation(direct_promise_operation)
+    ),
     direct!(
         "promise?",
         DirectCallableArity::Exact(1),
diff --git a/core/rust/src/core/direct_callable_operations.rs b/core/rust/src/core/direct_callable_operations.rs
index ad6f644..7d6341d 100644
--- a/core/rust/src/core/direct_callable_operations.rs
+++ b/core/rust/src/core/direct_callable_operations.rs
@@ -48,6 +48,7 @@ fn direct_predicate_operation(
 
     let value = &arguments[0];
     let result = match specification.symbol {
+        "boolean?" => matches!(value, Value::Bool(_)),
         "char?" => matches!(value, Value::Character(_)),
         "double?" => matches!(value, Value::Float(_)),
         "even?" | "odd?" | "pos?" | "neg?" | "zero?" => {
@@ -98,6 +99,17 @@ fn direct_predicate_operation(
     Ok(Value::Bool(result))
 }
 
+fn direct_promise_operation(
+    specification: &DirectCallableSpec,
+    arguments: Vec<Value>,
+) -> Result<Value, String> {
+    let method = specification
+        .symbol
+        .strip_prefix("promise/")
+        .expect("promise catalog operation must use the promise/ prefix");
+    native_promise_values(method, arguments)
+}
+
 fn direct_function_operation(
     specification: &DirectCallableSpec,
     arguments: Vec<Value>,
diff --git a/core/rust/src/core/direct_callable_tests.rs b/core/rust/src/core/direct_callable_tests.rs
index 2256411..0ab7554 100644
--- a/core/rust/src/core/direct_callable_tests.rs
+++ b/core/rust/src/core/direct_callable_tests.rs
@@ -94,6 +94,47 @@ fn representative_direct_callables_preserve_value_behavior() {
         call_value(satisfies, vec![ifn, identity]).unwrap(),
         Value::Bool(true)
     );
+
+    let boolean_predicate = direct_callable_value("boolean?").unwrap();
+    assert_eq!(
+        call_value(boolean_predicate.clone(), vec![Value::Bool(true)]).unwrap(),
+        Value::Bool(true)
+    );
+    assert_eq!(
+        call_value(boolean_predicate, vec![Value::Number(1)]).unwrap(),
+        Value::Bool(false)
+    );
+}
+
+#[test]
+fn specs_owned_direct_callable_bootstrap_fixture_runs_before_foundation_source_loading() {
+    let registry = std::env::var_os("HARA_SPECS_REGISTRY")
+        .map(std::path::PathBuf::from)
+        .unwrap_or_else(|| {
+            std::path::Path::new(env!("CARGO_MANIFEST_DIR"))
+                .join("..")
+                .join("..")
+                .join("..")
+                .join("hara-specs-registry")
+        });
+    let path = registry.join(
+        "01-lang/004-foundation/draft/conformance/fixtures/direct_callable_bootstrap.hal",
+    );
+    let source = std::fs::read_to_string(&path).unwrap_or_else(|error| {
+        panic!(
+            "authoritative direct-callable bootstrap fixture is required at {}: {error}",
+            path.display()
+        )
+    });
+
+    let mut runtime = crate::Runtime::core();
+    let report = runtime
+        .eval_text(&(source + "\n(direct-callable-bootstrap-report)"))
+        .unwrap();
+    assert_eq!(
+        report,
+        "[true false :std.native.Promise :std.native.Promise]"
+    );
 }
 
 #[test]
diff --git a/.github/workflows/issue-838-direct-callables.yml b/.github/workflows/issue-838-direct-callables.yml
index 926c08a..61738a1 100644
--- a/.github/workflows/issue-838-direct-callables.yml
+++ b/.github/workflows/issue-838-direct-callables.yml
@@ -22,6 +22,8 @@ concurrency:
 
 env:
   SPECS_FIXTURE: 01-lang/004-foundation/draft/conformance/fixtures/foundation_behavioral.hal
+  DIRECT_CALLABLE_FIXTURE: 01-lang/004-foundation/draft/conformance/fixtures/direct_callable_bootstrap.hal
+  SPECS_REVISION: aae63f735b312e29f18e041de6925ec0ae6baaa5
 
 jobs:
   catalog:
@@ -35,6 +37,7 @@ jobs:
         uses: actions/checkout@v4
         with:
           repository: hara-lang/hara-specs-registry
+          ref: ${{ env.SPECS_REVISION }}
           path: hara-specs-registry
           fetch-depth: 1
 
@@ -43,6 +46,7 @@ jobs:
         run: |
           set -euo pipefail
           test -f "$GITHUB_WORKSPACE/hara-specs-registry/$SPECS_FIXTURE"
+          test -f "$GITHUB_WORKSPACE/hara-specs-registry/$DIRECT_CALLABLE_FIXTURE"
           ln -sfn "$GITHUB_WORKSPACE/hara-specs-registry" \
             "$GITHUB_WORKSPACE/../hara-specs-registry"
 
@@ -82,6 +86,8 @@ jobs:
             complete_ordinary_callable_catalog_never_reenters_the_evaluator --lib
           cargo test --manifest-path core/rust/Cargo.toml \
             representative_direct_callables_preserve_value_behavior --lib
+          cargo test --manifest-path core/rust/Cargo.toml \
+            specs_owned_direct_callable_bootstrap_fixture_runs_before_foundation_source_loading --lib
           cargo test --manifest-path core/rust/Cargo.toml \
             rust_native_work_handles_are_available_to_guest_hara --lib
 
@@ -103,6 +109,7 @@ jobs:
         uses: actions/checkout@v4
         with:
           repository: hara-lang/hara-specs-registry
+          ref: ${{ env.SPECS_REVISION }}
           path: hara-specs-registry
           fetch-depth: 1
 
@@ -111,6 +118,7 @@ jobs:
         run: |
           set -euo pipefail
           test -f "$GITHUB_WORKSPACE/hara-specs-registry/$SPECS_FIXTURE"
+          test -f "$GITHUB_WORKSPACE/hara-specs-registry/$DIRECT_CALLABLE_FIXTURE"
           ln -sfn "$GITHUB_WORKSPACE/hara-specs-registry" \
             "$GITHUB_WORKSPACE/../hara-specs-registry"
 
@@ -152,6 +160,7 @@ jobs:
         uses: actions/checkout@v4
         with:
           repository: hara-lang/hara-specs-registry
+          ref: ${{ env.SPECS_REVISION }}
           path: hara-specs-registry
           fetch-depth: 1
 
@@ -160,6 +169,7 @@ jobs:
         run: |
           set -euo pipefail
           test -f "$GITHUB_WORKSPACE/hara-specs-registry/$SPECS_FIXTURE"
+          test -f "$GITHUB_WORKSPACE/hara-specs-registry/$DIRECT_CALLABLE_FIXTURE"
           ln -sfn "$GITHUB_WORKSPACE/hara-specs-registry" \
             "$GITHUB_WORKSPACE/../hara-specs-registry"
 
@@ -172,6 +182,8 @@ jobs:
         run: |
           cargo test --manifest-path core/rust/Cargo.toml \
             specs_owned_foundation_behavioral_corpus_runs_in_the_native_runtime --lib
+          cargo test --manifest-path core/rust/Cargo.toml \
+            specs_owned_direct_callable_bootstrap_fixture_runs_before_foundation_source_loading --lib
           cargo test --manifest-path core/rust/Cargo.toml \
             shared_foundation_protocol_conformance_fixture_runs_in_the_native_runtime --lib
           cargo test --manifest-path core/rust/Cargo.toml \
@@ -200,6 +212,7 @@ jobs:
         uses: actions/checkout@v4
         with:
           repository: hara-lang/hara-specs-registry
+          ref: ${{ env.SPECS_REVISION }}
           path: hara-specs-registry
           fetch-depth: 1
 
@@ -208,6 +221,7 @@ jobs:
         run: |
           set -euo pipefail
           test -f "$GITHUB_WORKSPACE/hara-specs-registry/$SPECS_FIXTURE"
+          test -f "$GITHUB_WORKSPACE/hara-specs-registry/$DIRECT_CALLABLE_FIXTURE"
           ln -sfn "$GITHUB_WORKSPACE/hara-specs-registry" \
             "$GITHUB_WORKSPACE/../hara-specs-registry"
