name: std.substrate hard-cut audit

on:
  pull_request:
  push:
    branches: [main]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify canonical substrate cutover
        run: bash scripts/audit-std-substrate-hard-cut.sh
