name: GitHub Filesystem CI

on:
  push:
    paths:
      - ".github/workflows/github-filesystem.yml"
      - "core/java/src/main/java/hara/truffle/Filesystem*.java"
      - "core/java/src/main/java/hara/truffle/GitHub*.java"
      - "core/java/src/main/java/hara/truffle/IFilesystem*.java"
      - "core/java/src/main/java/hara/truffle/SessionKernel.java"
      - "core/java/src/test/java/hara/truffle/GitHub*.java"
  pull_request:
    paths:
      - ".github/workflows/github-filesystem.yml"
      - "core/java/src/main/java/hara/truffle/Filesystem*.java"
      - "core/java/src/main/java/hara/truffle/GitHub*.java"
      - "core/java/src/main/java/hara/truffle/IFilesystem*.java"
      - "core/java/src/main/java/hara/truffle/SessionKernel.java"
      - "core/java/src/test/java/hara/truffle/GitHub*.java"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-filesystem-${{ github.ref }}
  cancel-in-progress: true

jobs:
  java-conformance:
    name: Java Git object and Kernel conformance
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Check out specifications registry
        uses: actions/checkout@v4
        with:
          repository: hara-lang/hara-specs-registry
          path: hara-specs-registry
          fetch-depth: 1

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: core/java/pom.xml

      - name: Compile runtime and run focused provider tests
        run: >-
          mvn -B -f core/java/pom.xml -Ptruffle
          -Dtest=GitHubFilesystemTest,GitHubRestObjectClientTest,GitHubFilesystemSessionKernelTest test
