package hara:markdown@0.1.0;

/// Pure CommonMark rendering. This world deliberately imports no WASI or host
/// interfaces; a caller provides source text and receives rendered HTML.
world markdown {
  export render: func(source: string) -> string;
}
